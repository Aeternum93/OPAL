# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#           .o.       ooooo     ooo oooooooooooo ooooooooo.   oooooooooooo ooooo        ooooo ooooo     ooo  .oooooo..o      8      ooo        ooooo   .oooooo.                           oooooooo 
#          .888.      `888'     `8' `888'     `8 `888   `Y88. `888'     `8 `888'        `888' `888'     `8' d8P'    `Y8      8      `88.       .888'  d8P'  `Y8b                         dP""""""" 
#         .8"888.      888       8   888          888   .d88'  888          888          888   888       8  Y88bo.           8       888b     d'888  888               oooo    ooo      d88888b.   
#        .8' `888.     888       8   888oooo8     888ooo88P'   888oooo8     888          888   888       8   `"Y8888o.               8 Y88. .P  888  888                `88.  .8'           `Y88b  
#       .88ooo8888.    888       8   888    "     888`88b.     888    "     888          888   888       8       `"Y88b      8       8  `888'   888  888                 `88..8'              ]88  
#      .8'     `888.   `88.    .8'   888       o  888  `88b.   888       o  888       o  888   `88.    .8'  oo     .d8P      8       8    Y     888  `88b    ooo          `888'         o.   .88P  
#     o88o     o8888o    `YbodP'    o888ooooood8 o888o  o888o o888ooooood8 o888ooooood8 o888o    `YbodP'    8""88888P'       8      o8o        o888o  `Y8bood8P'           `8'          `8bd88P'   
#                                                                                                                                                                                             
#                                                                                                                                                                                             
#                                                                                                                                                                                             
#
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠                                                                                                                                               
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠



# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#
#       .oooooo.                          .o88o.  o8o                                                .    o8o                             8      ooooo                                  .o8  
#      d8P'  `Y8b                         888 `"  `"'                                              .o8    `"'                             8      `888'                                 "888  
#     888           .ooooo.  ooo. .oo.   o888oo  oooo   .oooooooo oooo  oooo  oooo d8b  .oooo.   .o888oo oooo   .ooooo.  ooo. .oo.        8       888          .ooooo.   .oooo.    .oooo888  
#     888          d88' `88b `888P"Y88b   888    `888  888' `88b  `888  `888  `888""8P `P  )88b    888   `888  d88' `88b `888P"Y88b               888         d88' `88b `P  )88b  d88' `888  
#     888          888   888  888   888   888     888  888   888   888   888   888      .oP"888    888    888  888   888  888   888       8       888         888   888  .oP"888  888   888  
#     `88b    ooo  888   888  888   888   888     888  `88bod8P'   888   888   888     d8(  888    888 .  888  888   888  888   888       8       888       o 888   888 d8(  888  888   888  
#      `Y8bood8P'  `Y8bod8P' o888o o888o o888o   o888o `8oooooo.   `V88V"V8P' d888b    `Y888""8o   "888" o888o `Y8bod8P' o888o o888o      8      o888ooooood8 `Y8bod8P' `Y888""8o `Y8bod88P" 
#                                                      d"     YD                                                                                                                             
#                                                      "Y88888P'                                                                         
#
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠



# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 0. START: Load + Standardize Inputs (Mode-aware)
#   - mode = "today"    : loads today's schedule file (nba_schedule_YYYYMMDD.csv)
#   - mode = "backtest" : does NOT auto-load schedule; you will call load_schedule_for_date()
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠

rm(list = setdiff(ls(), c(
  # core globals
  "current_date",
  "formatted_date",
  "formatted_year",
  "next_year_date",
  "pbp_season",
  "season_token",
  "season_token2",
  
  # paths / config
  "base_path",
  "logo_dir",
  "essential_names",
  
  # runner config + helpers
  "scripts",
  "batches",
  "run_script",
  "run_batch"
)))

library(data.table)
library(dplyr)
library(purrr)
library(tibble)
library(lubridate)
library(future)
library(future.apply)

plan(multisession, workers = 8)
options(future.rng.onMisuse = "ignore")


# -----------------------------
# Base directories (centralize paths)
# -----------------------------
pbp_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/7. MC PBP Data"
current_slate_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/0. Current Slate"
sched_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/10. NBA Schedule"
team_mc_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/1. BaseStats_Team"
player_mc_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/2. BaseStats_Player"
rot_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/12. Player Rotations"
odds_player_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/1. hoopR/11. Player Enriched Odds"
injury_dir <- "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/9. Historical Injuries (RapidAPI)"


# -----------------------------
# Helpers
# -----------------------------
standardize_bool <- function(x) {
  dplyr::case_when(
    x %in% c("TRUE", "True", "true", "1")    ~ TRUE,
    x %in% c("FALSE", "False", "false", "0") ~ FALSE,
    TRUE ~ FALSE
  )
}

coerce_date_any <- function(x) {
  # Returns Date where possible, else NA
  # Handles "YYYY-MM-DD", "YYYY/MM/DD", "YYYYMMDD"
  x <- as.character(x)
  out <- suppressWarnings(lubridate::ymd(x))
  if (all(is.na(out))) {
    out <- suppressWarnings(lubridate::ymd(gsub("/", "-", x)))
  }
  if (all(is.na(out))) {
    out <- suppressWarnings(lubridate::ymd(x, quiet = TRUE))
  }
  out
}

load_schedule_for_date <- function(date_ymd) {
  # date_ymd can be Date or "YYYY-MM-DD"
  dt <- as.Date(date_ymd)
  if (is.na(dt)) stop("load_schedule_for_date(): invalid date input.")
  ymd_str <- format(dt, "%Y%m%d")
  
  f <- file.path(current_slate_dir, paste0("nba_schedule_", ymd_str, ".csv"))
  if (!file.exists(f)) stop("Schedule file not found: ", f)
  
  df <- fread(f, colClasses = "character", encoding = "UTF-8")
  
  # standardize key cols if present
  if ("game_date" %in% names(df)) df$game_date <- as.character(df$game_date)
  if ("team_id"   %in% names(df)) df$team_id   <- as.character(df$team_id)
  if ("opp_id"    %in% names(df)) df$opp_id    <- as.character(df$opp_id)
  if ("game_id"   %in% names(df)) df$game_id   <- as.character(df$game_id)
  
  df
}

# -----------------------------
# Load pm_nbapbp MC file
# -----------------------------
pbp_path <- file.path(pbp_dir, paste0("pm_nbapbp_", season_token, ".csv"))
pbp_df <- fread(pbp_path, colClasses = "character", encoding = "UTF-8")

# Coerce key numeric fields used by MC profiling + standardize booleans
pbp_df <- pbp_df %>%
  mutate(
    team_id          = as.character(team_id),
    game_id          = as.character(game_id),
    season           = as.character(season),
    score_value      = suppressWarnings(as.numeric(score_value)),
    points_attempted = suppressWarnings(as.numeric(points_attempted)),
    scoring_play     = standardize_bool(scoring_play)
  ) %>%
  filter(!is.na(team_id), team_id != "")

# Ensure game_date exists and is usable (needed for rolling windows/backtests)
# If your pm_nbapbp already has game_date, this will just standardize it.
if ("game_date" %in% names(pbp_df)) {
  pbp_df <- pbp_df %>%
    mutate(
      game_date = as.character(game_date),
      game_date_dt = coerce_date_any(game_date)
    )
} else {
  pbp_df$game_date <- NA_character_
  pbp_df$game_date_dt <- as.Date(NA)
  warning("pbp_df is missing game_date. Rolling windows/backtests will require adding it.")
}

# -----------------------------
# Load BaseStats_Team_MC
# -----------------------------
basestatsT_mc_file <- file.path(team_mc_dir, paste0("BaseStats_Team_MC_", season_token, ".csv"))
BaseStats_Team_MC <- fread(basestatsT_mc_file, colClasses = "character", encoding = "UTF-8") %>%
  mutate(
    ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
    team_id      = as.character(ESPN_TEAM_ID),
    season       = if ("season" %in% names(.)) as.character(season) else as.character(season_token)
  )

# -----------------------------
# Load BaseStats_Player_MC (optional for team MC; keep loaded for later)
# -----------------------------
basestatsP_mc_file <- file.path(player_mc_dir, paste0("BaseStats_Player_MC_", season_token, ".csv"))
BaseStats_Player_MC <- fread(basestatsP_mc_file, colClasses = "character", encoding = "UTF-8")

# -----------------------------
# Load rotations (optional for team MC; keep loaded for later)
# -----------------------------
nba_rotation_10M_file <- file.path(rot_dir, paste0("pm_nba_player_rotations_10M_", season_token, ".csv"))
nba_rotations_10M <- fread(nba_rotation_10M_file, colClasses = "character", encoding = "UTF-8")

nba_rotation_5M_file <- file.path(rot_dir, paste0("pm_nba_player_rotations_5M_", season_token, ".csv"))
nba_rotations_5M <- fread(nba_rotation_5M_file, colClasses = "character", encoding = "UTF-8")

player_rotations_file <- file.path(rot_dir, paste0("pm_nba_player_level_rotations_", season_token, ".csv"))
player_rotations <- fread(player_rotations_file, colClasses = "character", encoding = "UTF-8")

player_impact_delta_file <- file.path(rot_dir, paste0("player_impact_assessment_delta_", season_token, ".csv"))
player_impact_delta <- fread(player_impact_delta_file, colClasses = "character", encoding = "UTF-8")


# -----------------------------
# Load injury data (optional for team MC; keep loaded for later)
# -----------------------------
injury_data_file <- file.path(injury_dir, paste0("Injury_Database_", season_token, ".csv"))
injury_data <- fread(injury_data_file, colClasses = "character", encoding = "UTF-8")


# -----------------------------
# Load schedule data (optional for team MC; keep loaded for later)
# -----------------------------
sched_file <- file.path(sched_dir, paste0("nba_schedule_", season_token, ".csv"))
nba_schedule_season <- fread(sched_file, colClasses = "character", encoding = "UTF-8")


# -----------------------------
# Load player odds data (optional for team MC; keep loaded for later)
# -----------------------------
player_odds_enrich_file <- file.path(odds_player_dir, paste0("nba_player_odds_enrich_", season_token, ".csv"))
player_odds_enrich_df <- fread(player_odds_enrich_file, colClasses = "character", encoding = "UTF-8")




# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#
#      .oooooo..o           oooo                          .        ooo        ooooo                 .o8            
#      d8P'    `Y8           `888                        .o8        `88.       .888'                "888            
#      Y88bo.       .ooooo.   888   .ooooo.   .ooooo.  .o888oo       888b     d'888   .ooooo.   .oooo888   .ooooo.  
#       `"Y8888o.  d88' `88b  888  d88' `88b d88' `"Y8   888         8 Y88. .P  888  d88' `88b d88' `888  d88' `88b 
#           `"Y88b 888ooo888  888  888ooo888 888         888         8  `888'   888  888   888 888   888  888ooo888 
#      oo     .d8P 888    .o  888  888    .o 888   .o8   888 .       8    Y     888  888   888 888   888  888    .o 
#      8""88888P'  `Y8bod8P' o888o `Y8bod8P' `Y8bod8P'   "888"      o8o        o888o `Y8bod8P' `Y8bod88P" `Y8bod8P' 
# 
#
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠                                                                                                             


# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠    
# START: Mode Selection 
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠    

# ==========================================================================================================
# MODE SELECTION
# ----------------------------------------------------------------------------------------------------------
# Controls which data structures are loaded and which workflows are executed.
#
# Modes:
#   - "production"  : Live predictions for today's games only (fast startup)
#   - "backtest"    : Load historical data for model validation/calibration
#   - "calibration" : Load historical data + run variance tuning workflows
#   - "research"    : Load everything for model development/testing
#
# Key Outputs:
#   - as_of_date         : Temporal boundary for all data (what is "known")
#   - load_backtest_data : Boolean flag controlling historical roster loading
#   - load_player_odds   : Boolean flag for player prop market data
# ==========================================================================================================

# -----------------------------
# SET MODE
# -----------------------------
mode <- "backtest"   # Options: "production", "backtest", "calibration", "research"

# -----------------------------
# MODE-SPECIFIC CONFIGURATION
# -----------------------------
if (mode == "production") {
  # Live predictions for today's slate
  as_of_date         <- as.Date("2025-12-31")
  load_backtest_data <- FALSE  # Don't load historical rosters (faster)
  load_player_odds   <- TRUE   # Load current market lines
  
} else if (mode == "backtest") {
  # Historical validation on specific date
  as_of_date         <- as.Date("2025-12-31")  # ← Set backtest date here
  load_backtest_data <- TRUE   # Load all rosters < as_of_date
  load_player_odds   <- FALSE  # Historical odds not needed for validation
  
} else if (mode == "calibration") {
  # Tune variance parameters against historical games
  as_of_date         <- as.Date("2025-12-31")  # ← Set calibration cutoff
  load_backtest_data <- TRUE   # Need historical data
  load_player_odds   <- FALSE  # Don't need odds for calibration
  
} else if (mode == "research") {
  # Model development sandbox - load everything
  as_of_date         <- as.Date("2025-12-31")
  load_backtest_data <- TRUE   # Load all available history
  load_player_odds   <- TRUE   # Load all available data
  
} else {
  stop("Invalid mode: ", mode, ". Must be one of: production, backtest, calibration, research")
}

cat("🎯 AURELIUS MODE:", mode, "\n")
cat("📅 As-of Date:", as.character(as_of_date), "\n")
cat("📊 Load Backtest Data:", load_backtest_data, "\n")
cat("💰 Load Player Odds:", load_player_odds, "\n")

# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠    
# END: Mode Selection 
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠 


# -----------------------------
# Load schedule depending on mode
# -----------------------------
nba_schedule_today <- NULL

if (mode == "today") {
  # Use your formatted_date from master runner
  nba_schedule_today <- load_schedule_for_date(as.Date(current_date))
  
  # Safety check: your downstream assumes these exist for simulation
  req_cols <- c("game_id", "team_id", "opp_id")
  missing <- setdiff(req_cols, names(nba_schedule_today))
  if (length(missing) > 0) stop("Today's schedule missing columns: ", paste(missing, collapse = ", "))
}

# ----------------------------------------
# Attach NBA_Team_ID and NBA_Opp_ID
# ----------------------------------------

team_map_path <- file.path(
  "C:/Users/Austin/OneDrive/Desktop/1/Data Analytics/NBA Data/0. Datahub (Temp)/6. Data Cleanup",
  "team_id_mapping.csv"
)

team_id_map <- fread(
  team_map_path,
  colClasses = "character",
  encoding = "UTF-8"
)

# Expecting team_id / opp_id to be ESPN team IDs
nba_schedule_season <- nba_schedule_season %>%
  mutate(
    team_id = as.character(team_id),
    opp_id  = as.character(opp_id)
  ) %>%
  left_join(
    team_id_map %>%
      select(espn_team_id, nba_team_id) %>%
      rename(NBA_Team_ID = nba_team_id),
    by = c("team_id" = "espn_team_id")
  ) %>%
  left_join(
    team_id_map %>%
      select(espn_team_id, nba_team_id) %>%
      rename(NBA_Opp_ID = nba_team_id),
    by = c("opp_id" = "espn_team_id")
  )


rm("pbp_dir",
   "pbp_path",
   "sched_dir",
   "sched_file",
   "rot_dir",
   "injury_dir",
   "current_slate_dir",
   "team_map_path",
   "player_mc_dir",
   "team_mc_dir",
   "odds_player_dir",
   "player_impact_dela_file",
   "nba_rotation_5M_file",
   "nba_rotation_10M_file",
   "injury_data_file",
   "basestatsP_mc_file",
   "basestatsT_mc_file",
   "player_rotations_file"
)

# ============================================================
# STEP 0: Canonicalize games (1 row per game_id) for calibration
#   - Assumes nba_schedule_season has TWO rows per game_id:
#       is_static == TRUE  (original schedule row, true home/away)
#       is_static == FALSE (flipped row)
#   - Outputs:
#       nba_games_canon_season  (one row per game_id)
# ============================================================

library(dplyr)

nba_games_canon_season <- nba_schedule_season %>%
  mutate(
    game_id   = as.character(game_id),
    team_id   = as.character(team_id),
    opp_id    = as.character(opp_id),
    team      = as.character(team),
    opp       = as.character(opp),
    game_date = as.character(game_date),
    is_static = as.logical(is_static)
  ) %>%
  group_by(game_id) %>%
  summarise(
    game_date = dplyr::first(na.omit(game_date)),
    
    # --- HOME side (from the static/original row) ---
    home_team_id = team_id[is_static][1],
    home_team    = team[is_static][1],
    home_score   = suppressWarnings(as.numeric(team_score[is_static][1])),
    home_winner  = as.logical(team_winner[is_static][1]),
    
    # --- AWAY side (opponent fields from the SAME static row) ---
    away_team_id = opp_id[is_static][1],
    away_team    = opp[is_static][1],
    away_score   = suppressWarnings(as.numeric(opp_score[is_static][1])),
    away_winner  = as.logical(opp_winner[is_static][1]),
    
    # --- audits ---
    n_rows_in_game = dplyr::n(),
    n_static_rows  = sum(is_static, na.rm = TRUE),
    n_flip_rows    = sum(!is_static, na.rm = TRUE),
    
    # verify the two rows are truly flipped
    t1 = as.character(team_id[1]),
    o1 = as.character(opp_id[1]),
    t2 = as.character(team_id[2]),
    o2 = as.character(opp_id[2]),
    flip_ok = (t1 == o2 && t2 == o1),
    
    .groups = "drop"
  ) %>%
  mutate(
    canon_ok = (n_rows_in_game == 2L) & (n_static_rows == 1L) & flip_ok
  ) %>%
  select(-t1, -o1, -t2, -o2)

# ---- quick sanity audit ----
cat("Canonical games:", nrow(nba_games_canon_season), "\n")
cat("canon_ok == TRUE:", sum(nba_games_canon_season$canon_ok, na.rm = TRUE), "\n")
cat("canon_ok == FALSE:", sum(!nba_games_canon_season$canon_ok, na.rm = TRUE), "\n")

bad_games <- nba_games_canon_season %>% filter(!canon_ok)
if (nrow(bad_games) > 0) {
  print(bad_games %>% select(game_id, n_rows_in_game, n_static_rows, n_flip_rows, flip_ok) %>% head(25))
}


# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 0. END: Load + Standardize
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠




# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#
#     oooooo     oooo                     o8o                                                   .oooooo.                             .                      oooo  
#      `888.     .8'                      `"'                                                  d8P'  `Y8b                          .o8                      `888  
#       `888.   .8'    .oooo.   oooo d8b oooo   .oooo.   ooo. .oo.    .ooooo.   .ooooo.       888           .ooooo.  ooo. .oo.   .o888oo oooo d8b  .ooooo.   888  
#        `888. .8'    `P  )88b  `888""8P `888  `P  )88b  `888P"Y88b  d88' `"Y8 d88' `88b      888          d88' `88b `888P"Y88b    888   `888""8P d88' `88b  888  
#         `888.8'      .oP"888   888      888   .oP"888   888   888  888       888ooo888      888          888   888  888   888    888    888     888   888  888  
#          `888'      d8(  888   888      888  d8(  888   888   888  888   .o8 888    .o      `88b    ooo  888   888  888   888    888 .  888     888   888  888  
#           `8'       `Y888""8o d888b    o888o `Y888""8o o888o o888o `Y8bod8P' `Y8bod8P'       `Y8bood8P'  `Y8bod8P' o888o o888o   "888" d888b    `Y8bod8P' o888o 
#
#
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠                                                                                                                                                            
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠                                                                                                                                                            

# ==========================================================================================================
# VARIANCE CONTROL — MONTE CARLO RANDOMNESS POLICY
# ----------------------------------------------------------------------------------------------------------
# This section defines the complete set of stochastic controls that govern how randomness is introduced
# during Monte Carlo simulation, while leaving all expected values produced by profile- and intensity-
# building stages unchanged.
#
# Variance controls determine how far simulated outcomes are allowed to deviate from empirical or modeled
# expectations (shrinkage), how much per-simulation noise is injected around those expectations (jitter),
# and how extreme outcomes are constrained through hard caps and floors.
#
# All randomness levers are centralized here, including possession volatility, shooting efficiency noise,
# turnover randomness, and per-minute rate variation for rebounds, assists, blocks, steals, three-point
# makes, and fouls.
#
# Profile-building and intensity-building stages are strictly deterministic and are responsible only for
# producing expected rates, shares, or volumes. These variance controls are applied downstream inside the
# Monte Carlo engine to perturb those expectations in a controlled, interpretable, and auditable manner.
#
# Helper functions standardize numeric coercion, percentage handling, shrinkage toward anchors, optional
# per-simulation jitter, and bounded rate adjustment to ensure numerical stability and realistic behavior.
#
# This design preserves realism and tunability by allowing natural outcome variability while preventing
# unrealistic drift, fat-tailed explosions, hidden randomness, or accidental double-counting across the
# simulation pipeline.
#
# NOTE: All jitter parameters set to 0.00 during initial development. These will be calibrated against
# actual outcome distributions once profile-building stages are validated.
# ==========================================================================================================

# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# START. VARIANCE CONTROLS (STD / randomness levers)
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠

MC_VAR <- list(
  
  # ============================================================
  # POSSESSION VARIANCE
  # ============================================================
  # poss_sd_mult: Multiplier that scales the standard deviation of possessions
  # You can make pace deviate more (>1.00) or less (<1.00) from expected
  
  poss_sd_mult = 1.00,
  
  # ============================================================
  # SHOOTING VARIANCE (BY SHOT ZONE)
  # ============================================================
  # Shrinkage factors pull shooting percentages toward league/anchor means
  # Different zones have different stability profiles:
  #   - Rim shots: Most stable (high volume, defense matters but conversion predictable)
  #   - Paint/Mid-range: Medium stability
  #   - Corner 3s: Medium stability (often open looks)
  #   - ATB 3s: Least stable (pull-ups, contested, highly variable)
  
  shoot_shrink_rim     = 0.15,  # rim shots most stable
  shoot_shrink_paint   = 0.20,  # paint shots medium-low variance
  shoot_shrink_midr    = 0.25,  # mid-range medium variance
  shoot_shrink_corner3 = 0.25,  # corner 3s medium variance
  shoot_shrink_atb3    = 0.35,  # above-the-break 3s highest variance
  
  # Per-simulation jitter (disabled during development, calibrate later)
  pct_jitter_rim     = 0.00,
  pct_jitter_paint   = 0.00,
  pct_jitter_midr    = 0.00,
  pct_jitter_corner3 = 0.00,
  pct_jitter_atb3    = 0.00,
  
  # ============================================================
  # TURNOVER VARIANCE
  # ============================================================
  # tov_rand: Turnover randomness strength (0..1)
  # Blends team turnover rate with league mean
  
  tov_rand      = 0.25,
  tov_shrink    = 0.30,
  tov_jitter_sd = 0.01,
  tov_tovpm_lo  = 0.00,
  tov_tovpm_hi  = 0.25,
  
  # ============================================================
  # REBOUNDS VARIANCE
  # ============================================================
  reb_sd_mult   = 1.00,
  reb_mean_mult = 1.00,
  reb_shrink    = 0.20,   # shrink reb/min toward anchor (0..1)
  reb_jitter_sd = 0.00,   # per-sim noise on reb/min (calibrate later)
  reb_rpm_lo    = 0.00,   # lower bound for rebounds per minute
  reb_rpm_hi    = 0.60,   # upper bound for rebounds per minute (~29 reb/48min)
  
  # ============================================================
  # ASSISTS VARIANCE
  # ============================================================
  ast_shrink    = 0.20,
  ast_jitter_sd = 0.00,
  ast_apm_lo    = 0.00,
  ast_apm_hi    = 0.35,   # sane cap (~17 ast/48min)
  
  # ============================================================
  # BLOCKS VARIANCE
  # ============================================================
  blk_shrink    = 0.25,   # pull player BLK/min toward league BLK/min
  blk_jitter_sd = 0.01,   # per-sim randomness on block rate
  blk_bpm_lo    = 0.00,   # hard floor for blocks per minute
  blk_bpm_hi    = 0.20,   # hard cap for blocks per minute
  
  # ============================================================
  # STEALS VARIANCE
  # ============================================================
  stl_shrink    = 0.25,
  stl_jitter_sd = 0.01,
  stl_spm_lo    = 0.00,
  stl_spm_hi    = 0.20,
  
  # ============================================================
  # 3-POINT MAKES VARIANCE
  # ============================================================
  # Note: This controls 3PM rate variance separately from shooting %
  # Useful for volume-dependent variance (high-volume shooters more stable)
  fg3m_shrink    = 0.25,
  fg3m_jitter_sd = 0.01,
  fg3m_pm_lo     = 0.00,
  fg3m_pm_hi     = 0.12,
  
  # ============================================================
  # FOULS VARIANCE
  # ============================================================
  fouls_shrink    = 0.25,
  fouls_jitter_sd = 0.010,
  fouls_fpm_lo    = 0.00,
  fouls_fpm_hi    = 0.30
)


# ============================================================
# HELPER FUNCTIONS (VARIANCE CONTROL UTILITIES)
# ============================================================

clamp01 <- function(x) pmin(pmax(x, 0), 1)

pct01 <- function(x, default = NA_real_) {
  x <- suppressWarnings(as.numeric(x))
  if (is.na(x)) return(default)
  if (x > 1 && x <= 100) x <- x / 100
  clamp01(x)
}

num0 <- function(x, default = 0) {
  x <- suppressWarnings(as.numeric(x))
  ifelse(is.na(x), default, x)
}

shrink_to <- function(x, anchor, w) {
  x <- suppressWarnings(as.numeric(x))
  if (is.na(x)) return(anchor)
  w <- clamp01(w)
  (1 - w) * x + w * anchor
}

apply_pct_jitter <- function(p, jitter_sd) {
  p <- suppressWarnings(as.numeric(p))
  if (is.na(p)) return(NA_real_)
  if (is.na(jitter_sd) || jitter_sd <= 0) return(clamp01(p))
  clamp01(rnorm(1, mean = p, sd = jitter_sd))
}

scale_reb_profile <- function(mu, sd, mean_mult = 1.0, sd_mult = 1.0) {
  mu <- num0(mu, 0); sd <- num0(sd, 0)
  list(mu = pmax(0, mu * mean_mult), sd = pmax(0, sd * sd_mult))
}


# ============================================================
# VARIANCE CONTROL: GENERIC RATE ADJUSTER (SIMULATION ONLY)
# ============================================================

apply_rate_variance_control <- function(
    rate,
    anchor,
    shrink_w,
    jitter_sd = 0,
    lower = 0,
    upper = Inf
) {
  rate   <- suppressWarnings(as.numeric(rate))
  anchor <- suppressWarnings(as.numeric(anchor))
  
  if (is.na(rate)) return(NA_real_)
  if (is.na(anchor)) anchor <- rate
  
  # --- shrink toward anchor ---
  w <- clamp01(shrink_w)
  adj <- (1 - w) * rate + w * anchor
  
  # --- optional jitter (per sim) ---
  if (!is.na(jitter_sd) && jitter_sd > 0) {
    adj <- rnorm(1, mean = adj, sd = jitter_sd)
  }
  
  # --- bounds ---
  adj <- pmin(upper, pmax(lower, adj))
  
  adj
}

# ============================================================
# VARIANCE CONTROL: SHOOTING BY ZONE (SIMULATION ONLY)
# ============================================================

apply_shooting_variance_by_zone <- function(
    shot_pct,
    anchor_pct,
    zone = c("rim", "paint", "midr", "corner3", "atb3")
) {
  zone <- match.arg(zone)
  
  shrink_w <- switch(zone,
                     rim     = MC_VAR$shoot_shrink_rim,
                     paint   = MC_VAR$shoot_shrink_paint,
                     midr    = MC_VAR$shoot_shrink_midr,
                     corner3 = MC_VAR$shoot_shrink_corner3,
                     atb3    = MC_VAR$shoot_shrink_atb3
  )
  
  jitter_sd <- switch(zone,
                      rim     = MC_VAR$pct_jitter_rim,
                      paint   = MC_VAR$pct_jitter_paint,
                      midr    = MC_VAR$pct_jitter_midr,
                      corner3 = MC_VAR$pct_jitter_corner3,
                      atb3    = MC_VAR$pct_jitter_atb3
  )
  
  apply_rate_variance_control(
    rate      = shot_pct,
    anchor    = anchor_pct,
    shrink_w  = shrink_w,
    jitter_sd = jitter_sd,
    lower     = 0.0,
    upper     = 1.0
  )
}

# ============================================================
# VARIANCE CONTROL: REBOUNDS PER MIN (SIMULATION ONLY)
# ============================================================

apply_reb_rate_variance_control <- function(
    reb_per_min,
    anchor_rpm,
    shrink_w  = MC_VAR$reb_shrink,
    jitter_sd = MC_VAR$reb_jitter_sd,
    lower     = MC_VAR$reb_rpm_lo,
    upper     = MC_VAR$reb_rpm_hi
) {
  apply_rate_variance_control(
    rate      = reb_per_min,
    anchor    = anchor_rpm,
    shrink_w  = shrink_w,
    jitter_sd = jitter_sd,
    lower     = lower,
    upper     = upper
  )
}


# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# END VARIANCE CONTROLS
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠



# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#     ooooooooo.   oooo                                                ooooooooo.                       .o88o.  o8o  oooo                     
#     `888   `Y88. `888                                                `888   `Y88.                     888 `"  `"'  `888                     
#      888   .d88'  888   .oooo.   oooo    ooo  .ooooo.  oooo d8b       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.   .oooo.o 
#      888ooo88P'   888  `P  )88b   `88.  .8'  d88' `88b `888""8P       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b d88(  "8 
#      888          888   .oP"888    `88..8'   888ooo888  888           888          888     888   888  888     888   888  888ooo888 `"Y88b.  
#      888          888  d8(  888     `888'    888    .o  888           888          888     888   888  888     888   888  888    .o o.  )88b 
#     o888o        o888o `Y888""8o     .8'     `Y8bod8P' d888b         o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P' 8""888P' 
#                                  .o..P'                                                                                            
#
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠



# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧
# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧
#
#
#     ooooo                 o8o                                       ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888'                 `"'                                       `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888  ooo. .oo.      oooo oooo  oooo  oooo d8b oooo    ooo       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888  `888P"Y88b     `888 `888  `888  `888""8P  `88.  .8'        888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888   888   888      888  888   888   888       `88..8'         888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888   888   888      888  888   888   888        `888'          888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888o o888o o888o     888  `V88V"V8P' d888b        .8'          o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                           888                      .o..P'                                                                                `"              "'   
#                       .o. 88P                      `Y8P'                                                                                                      
#                       `Y888P                                                                                     
#
# 
# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧
# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧 

# ==========================================================================================================
# INJURY / AVAILABILITY LAYER
# ----------------------------------------------------------------------------------------------------------
# This section constructs a deterministic, date-driven player availability layer that defines the valid
# player pool for simulation. Using a single authoritative `as_of_date`, it combines official team rosters
# with injury status to classify players as active or out on a per-team, per-game basis.
#
# Availability is built symmetrically for a single “today” snapshot at `as_of_date` and for all historical
# slate dates strictly prior to that date. The outputs are clean roster pools and audit tables that
# downstream logic can consume without re-evaluating injuries or roster membership.
#
# This layer introduces no randomness and serves as the gatekeeper for all player-level modeling.
# ==========================================================================================================

# ==========================================================================================================
# PLAYER PROJECTION STAGING
# ----------------------------------------------------------------------------------------------------------
# This section materializes deterministic player projection inputs from the active roster pools produced
# by the availability layer. It creates `player_projections_today` and `player_projections_backtest` with
# identical schemas, differing only by their date scope.
#
# Each row represents a player-game expectation placeholder, containing identifiers and projection fields
# to be populated later by minutes, usage, and rate models. No variance, simulation noise, or Monte Carlo
# logic is applied at this stage.
#
# These projection tables form the stable input surface consumed by downstream intensity modeling and
# Monte Carlo simulation.
# ==========================================================================================================

# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧
# ==========================================================================================================
# INJURY / AVAILABILITY LAYER
# ----------------------------------------------------------------------------------------------------------
# Outputs:
#   ALWAYS CREATED:
#     - roster_pool_today     : All rostered players for games on as_of_date
#     - roster_active_today   : Available players for games on as_of_date
#     - roster_out_today      : Unavailable players for games on as_of_date
#     - injury_team_summary_today_df : Audit counts by team
#
#   CONDITIONALLY CREATED (if load_backtest_data == TRUE):
#     - roster_active_backtest : Available players for all games < as_of_date
#     - roster_out_backtest    : Unavailable players for all games < as_of_date
# ==========================================================================================================

# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧
# START: Injury / Availability Layer
# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧

stopifnot(
  exists("as_of_date"),
  exists("load_backtest_data"),
  exists("BaseStats_Player_MC"),
  exists("BaseStats_Team_MC"),
  exists("injury_data"),
  exists("nba_schedule_season"),
  exists("season_token2")
)

suppressWarnings({
  library(dplyr)
  library(tibble)
  library(purrr)
  library(hoopR)
})

# -----------------------------
# 0) Standardize / bridge
# -----------------------------
# [Keep all your existing standardization code]
BaseStats_Player_MC <- BaseStats_Player_MC %>%
  mutate(
    NBA_PLAYER_ID  = as.character(NBA_PLAYER_ID),
    ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
    ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID)
  )

injury_data <- injury_data %>%
  mutate(
    ESPN_PLAYER_ID = as.character(if ("ESPN_PLAYER_ID" %in% names(.)) ESPN_PLAYER_ID else espn_player_id),
    ESPN_TEAM_ID   = as.character(if ("ESPN_TEAM_ID"   %in% names(.)) ESPN_TEAM_ID   else espn_team_id),
    status         = tolower(as.character(status))
  )

if ("date" %in% names(injury_data)) {
  injury_data <- injury_data %>% mutate(game_date_dt = as.Date(date))
} else if ("game_date" %in% names(injury_data)) {
  injury_data <- injury_data %>% mutate(game_date_dt = as.Date(game_date))
} else {
  stop("injury_data must contain `date` or `game_date`.")
}

bridge_unique <- BaseStats_Player_MC %>%
  select(NBA_PLAYER_ID, ESPN_PLAYER_ID, ESPN_TEAM_ID) %>%
  distinct(NBA_PLAYER_ID, ESPN_TEAM_ID, .keep_all = TRUE)

# -----------------------------
# 1) Roster cache
# -----------------------------
# [Keep your existing cache code]
.roster_cache <- new.env(parent = emptyenv())

get_team_roster_cached <- function(nba_team_id, season_token2) {
  key <- paste0(season_token2, "::", nba_team_id)
  if (exists(key, envir = .roster_cache)) {
    return(get(key, envir = .roster_cache))
  }
  
  raw <- nba_commonteamroster(team_id = nba_team_id, season = season_token2)
  
  roster <- raw$CommonTeamRoster %>%
    rename(
      NBA_TEAM_ID   = TeamID,
      NBA_PLAYER_ID = PLAYER_ID
    ) %>%
    mutate(
      NBA_TEAM_ID   = as.character(NBA_TEAM_ID),
      NBA_PLAYER_ID = as.character(NBA_PLAYER_ID)
    )
  
  assign(key, roster, envir = .roster_cache)
  Sys.sleep(0.5)
  roster
}

# -----------------------------
# 2) Core builder
# -----------------------------
# [Keep your existing build_availability_for_date function - no changes]
build_availability_for_date <- function(schedule_df, game_date, date_key) {
  
  game_date <- as.Date(game_date)
  
  team_keys <- schedule_df %>%
    transmute(
      NBA_TEAM_ID  = as.character(NBA_Team_ID),
      ESPN_TEAM_ID = as.character(team_id),
      date_key     = as.character(date_key),
      GAME_DATE    = game_date
    ) %>%
    distinct()
  
  roster_pool <- purrr::map_dfr(unique(team_keys$NBA_TEAM_ID), function(nba_tid) {
    roster_i <- get_team_roster_cached(nba_tid, season_token2)
    espn_tid <- team_keys %>% filter(NBA_TEAM_ID == nba_tid) %>% slice(1) %>% pull(ESPN_TEAM_ID)
    
    roster_i %>%
      mutate(
        NBA_TEAM_ID  = nba_tid,
        ESPN_TEAM_ID = espn_tid,
        date_key     = date_key,
        GAME_DATE    = game_date
      )
  }) %>%
    left_join(bridge_unique, by = c("NBA_PLAYER_ID", "ESPN_TEAM_ID"))
  
  inj_out <- injury_data %>%
    filter(game_date_dt == game_date, status == "out") %>%
    distinct(ESPN_TEAM_ID, ESPN_PLAYER_ID)
  
  roster_pool <- roster_pool %>%
    mutate(
      is_out = as.integer(
        paste0(ESPN_TEAM_ID, "::", ESPN_PLAYER_ID) %in%
          paste0(inj_out$ESPN_TEAM_ID, "::", inj_out$ESPN_PLAYER_ID)
      ),
      status = ifelse(is_out == 1L, "out", "active")
    )
  
  roster_active <- roster_pool %>% filter(is_out == 0L)
  roster_out    <- roster_pool %>% filter(is_out == 1L)
  
  team_audit <- roster_pool %>%
    group_by(date_key, GAME_DATE, NBA_TEAM_ID, ESPN_TEAM_ID) %>%
    summarise(
      roster_pool_n = n(),
      out_n         = sum(is_out),
      active_n      = sum(1L - is_out),
      out_players   = paste(ESPN_PLAYER_ID[is_out == 1L], collapse = ","),
      .groups = "drop"
    )
  
  list(
    team_keys     = team_keys,
    roster_pool   = roster_pool,
    roster_active = roster_active,
    roster_out    = roster_out,
    team_audit    = team_audit
  )
}

# -----------------------------
# 3) Resolve dates and schedules
# -----------------------------
run_date <- as.Date(as_of_date)
if (is.na(run_date)) stop("as_of_date is invalid.")

run_key <- format(run_date, "%Y%m%d")

sched_all <- nba_schedule_season %>%
  mutate(game_date_dt = as.Date(game_date)) %>%
  filter(!is.na(game_date_dt))

# ============================================================
# A) TODAY availability (ALWAYS CREATED)
# ============================================================
cat("📋 Building roster availability for", as.character(run_date), "...\n")

sched_today <- sched_all %>%
  filter(game_date_dt == run_date) %>%
  select(-game_date_dt)

if (nrow(sched_today) == 0) {
  warning("⚠️  No games scheduled for as_of_date: ", run_date)
  roster_pool_today   <- tibble()
  roster_active_today <- tibble()
  roster_out_today    <- tibble()
  injury_team_summary_today_df <- tibble()
} else {
  today_avail <- build_availability_for_date(
    schedule_df = sched_today,
    game_date   = run_date,
    date_key    = run_key
  )
  
  roster_pool_today   <- today_avail$roster_pool
  roster_active_today <- today_avail$roster_active
  roster_out_today    <- today_avail$roster_out
  injury_team_summary_today_df <- today_avail$team_audit
  
  cat("✅ TODAY:", nrow(roster_active_today), "active players,", 
      nrow(roster_out_today), "out\n")
}

# ============================================================
# B) BACKTEST availability (CONDITIONAL)
# ============================================================
if (load_backtest_data) {
  cat("📊 Building backtest rosters for all dates <", as.character(run_date), "...\n")
  
  roster_active_backtest_by_date <- list()
  roster_out_backtest_by_date    <- list()
  
  prior_dates <- sched_all %>%
    filter(game_date_dt < run_date) %>%
    distinct(game_date_dt) %>%
    arrange(game_date_dt) %>%
    pull(game_date_dt)
  
  if (length(prior_dates) == 0) {
    warning("⚠️  No prior dates found for backtest (all games are >= as_of_date)")
    roster_active_backtest <- tibble()
    roster_out_backtest    <- tibble()
  } else {
    cat("   Processing", length(prior_dates), "historical dates...\n")
    
    for (dd in prior_dates) {
      kk <- format(as.Date(dd), "%Y%m%d")
      
      sched_dd <- sched_all %>%
        filter(game_date_dt == dd) %>%
        select(-game_date_dt)
      
      if (nrow(sched_dd) == 0) next
      
      out_i <- build_availability_for_date(
        schedule_df = sched_dd,
        game_date   = dd,
        date_key    = kk
      )
      
      roster_active_backtest_by_date[[kk]] <- out_i$roster_active
      roster_out_backtest_by_date[[kk]]    <- out_i$roster_out
    }
    
    roster_active_backtest <- bind_rows(roster_active_backtest_by_date)
    roster_out_backtest    <- bind_rows(roster_out_backtest_by_date)
    
    cat("✅ BACKTEST:", nrow(roster_active_backtest), "player-game records loaded\n")
  }
  
} else {
  cat("⏭️  Skipping backtest data (load_backtest_data = FALSE)\n")
  roster_active_backtest <- tibble()
  roster_out_backtest    <- tibble()
}

# ------------------------------------------------------------
# PATCH: Ensure ESPN_TEAM_ID exists
# ------------------------------------------------------------
team_id_bridge <- BaseStats_Team_MC %>%
  select(NBA_TEAM_ID, ESPN_TEAM_ID) %>%
  distinct() %>%
  mutate(
    NBA_TEAM_ID  = as.character(NBA_TEAM_ID),
    ESPN_TEAM_ID = as.character(ESPN_TEAM_ID)
  )

patch_team_ids <- function(df) {
  if (nrow(df) == 0) return(df)
  if (!("NBA_TEAM_ID" %in% names(df))) return(df)
  if (!("ESPN_TEAM_ID" %in% names(df))) {
    df <- df %>% left_join(team_id_bridge, by = "NBA_TEAM_ID")
  }
  df
}

roster_active_today    <- patch_team_ids(roster_active_today)
roster_out_today       <- patch_team_ids(roster_out_today)
roster_active_backtest <- patch_team_ids(roster_active_backtest)
roster_out_backtest    <- patch_team_ids(roster_out_backtest)

# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧
# END: Injury / Availability Layer
# 💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧💧


# ============================================================
# Convert Stacked Rosters to By-Date Lists
# ============================================================
# The minutes section needs rosters organized by date,
# not stacked into one big dataframe

if (exists("roster_active_backtest") && nrow(roster_active_backtest) > 0) {
  
  cat("📅 Converting backtest rosters to by-date lists...\n")
  
  # Get unique dates
  unique_dates <- unique(roster_active_backtest$GAME_DATE)
  unique_dates <- unique_dates[!is.na(unique_dates)]
  
  # Create by-date lists
  roster_pool_backtest_by_date <- lapply(unique_dates, function(d) {
    # Combine active + out for full roster pool
    active <- roster_active_backtest %>% filter(GAME_DATE == d)
    out <- roster_out_backtest %>% filter(GAME_DATE == d)
    
    bind_rows(active, out) %>% distinct()
  })
  
  roster_out_backtest_by_date <- lapply(unique_dates, function(d) {
    roster_out_backtest %>% filter(GAME_DATE == d)
  })
  
  # Name the lists with date keys
  date_keys <- format(unique_dates, "%Y%m%d")
  names(roster_pool_backtest_by_date) <- date_keys
  names(roster_out_backtest_by_date) <- date_keys
  
  message(
    "✅ By-date roster lists created | dates=", length(unique_dates)
  )
  
} else {
  roster_pool_backtest_by_date <- list()
  roster_out_backtest_by_date <- list()
  message("⏭️  No backtest rosters to convert")
}

# Do the same for TODAY if needed
if (exists("roster_pool_today") && nrow(roster_pool_today) > 0) {
  roster_pool_today_by_date <- list()
  roster_pool_today_by_date[[format(as_of_date, "%Y%m%d")]] <- roster_pool_today
  
  roster_out_today_by_date <- list()
  roster_out_today_by_date[[format(as_of_date, "%Y%m%d")]] <- roster_out_today
}


# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩
# PLAYER PROJECTION STAGING (DETERMINISTIC)
#
# Purpose:
#   - Create player_projections_today and player_projections_backtest
#   - Consume roster_active_* from Availability layer
#   - Apply NO randomness, NO variance, NO simulation
#
# Contract:
#   - Same schema for today and backtest
#   - Date-driven via GAME_DATE / date_key
#   - MC consumes these downstream
# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("roster_active_today"),
  exists("roster_active_backtest")
)

library(dplyr)

# ------------------------------------------------------------
# Helper: build projections from a roster_active dataframe
# ------------------------------------------------------------
build_player_projections <- function(roster_active_df) {
  
  roster_active_df %>%
    
    # ---- core identifiers ----
  transmute(
    date_key       = as.character(date_key),
    GAME_DATE      = as.Date(GAME_DATE),
    
    NBA_TEAM_ID    = as.character(NBA_TEAM_ID),
    ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
    
    NBA_PLAYER_ID  = as.character(NBA_PLAYER_ID),
    ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
    
    status         = status,
    
    # ---- placeholders (filled later) ----
    proj_minutes   = NA_real_,
    proj_usage     = NA_real_,
    
    proj_pts_rate  = NA_real_,
    proj_ast_rate  = NA_real_,
    proj_reb_rate  = NA_real_,
    proj_stl_rate  = NA_real_,
    proj_blk_rate  = NA_real_,
    proj_tov_rate  = NA_real_,
    
    proj_fg3_rate  = NA_real_,
    proj_foul_rate = NA_real_
  )
}

# ============================================================
# A) TODAY projections (snapshot at as_of_date)
# ============================================================
player_projections_today <- build_player_projections(
  roster_active_today
)

message(
  "player_projections_today built | rows=",
  nrow(player_projections_today),
  " | date=", unique(player_projections_today$date_key)
)

# ============================================================
# B) BACKTEST projections (ALL dates < as_of_date)
# ============================================================
player_projections_backtest <- build_player_projections(
  roster_active_backtest
)

message(
  "player_projections_backtest built | rows=",
  nrow(player_projections_backtest),
  " | dates=", length(unique(player_projections_backtest$date_key))
)


# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩
# END PLAYER PROJECTION STAGING
# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#     ooo        ooooo  o8o                              .                           ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `88.       .888'  `"'                            .o8                           `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888b     d'888  oooo  ooo. .oo.   oooo  oooo  .o888oo  .ooooo.   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      8 Y88. .P  888  `888  `888P"Y88b  `888  `888    888   d88' `88b d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      8  `888'   888   888   888   888   888   888    888   888ooo888 `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      8    Y     888   888   888   888   888   888    888 . 888    .o o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o8o        o888o o888o o888o o888o  `V88V"V8P'   "888" `Y8bod8P' 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                                         `"              "'   
#
#
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

# ==========================================================================================================
# MINUTES BASELINE PARAMETER BUILDER
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline minute distribution parameters for both TODAY and BACKTEST.
# For each date, it computes per-quarter minute means and standard deviations using a rolling window of
# recent games that occurred strictly before that date. These statistics describe how minutes are typically
# allocated for each player by quarter, without introducing simulation noise or randomness.
#
# The full roster pool is used as the base universe, while injury status is applied separately to flag
# players as active or out, ensuring team completeness and supporting later minute redistribution logic.
# Out players are retained in the output so their unavailable minutes can be reallocated downstream.
#
# This layer produces deterministic distribution parameters only and does not generate projections.
# All as-of-date minutes, variance, and redistribution are handled later inside the Monte Carlo engine.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Minutes BASELINE PARAMETER BUILDER (NO SIMULATION)
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- c("Q1","Q2","Q3","Q4","Q5","Q6")

key_to_date <- function(k) {
  suppressWarnings(as.Date(gsub("[^0-9]", "", k), "%Y%m%d"))
}

# ------------------------------------------------------------
# Helper: build minutes PARAMETER TABLE for ONE DATE
# ------------------------------------------------------------
build_minutes_params_one_date <- function(
    proj_date,
    roster_pool,
    roster_out,
    n_games_back = 10L
) {
  
  proj_date <- as.Date(proj_date)
  date_key  <- format(proj_date, "%Y%m%d")
  
  # ----------------------------------------------------------
  # A) Historical per-quarter minute params (LEAKAGE-SAFE)
  # ----------------------------------------------------------
  build_q_params <- function(q) {
    col <- paste0("MINS_", q)
    if (!col %in% names(BaseStats_Player_MC)) return(NULL)
    
    BaseStats_Player_MC %>%
      dplyr::mutate(
        GAME_DATE = as.Date(GAME_DATE),
        val       = num0(.data[[col]], NA_real_)
      ) %>%
      dplyr::filter(GAME_DATE < proj_date) %>%
      dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
      dplyr::group_by(ESPN_PLAYER_ID) %>%
      dplyr::slice_tail(n = n_games_back) %>%
      dplyr::mutate(
        days_ago   = as.numeric(as.Date(proj_date) - GAME_DATE),
        rec_weight = exp(-0.1 * days_ago)
      ) %>%
      dplyr::summarise(
        n_games_obs  = n(),
        total_weight = sum(rec_weight, na.rm = TRUE),
        
        # Recency-weighted mean
        mins_mean_raw = ifelse(
          total_weight > 0,
          sum(val * rec_weight, na.rm = TRUE) / total_weight,
          mean(val, na.rm = TRUE)
        ),
        
        # Recency-weighted SD
        mins_sd_raw = ifelse(
          total_weight > 0 & n_games_obs > 1,
          sqrt(sum(rec_weight * (val - mins_mean_raw)^2, na.rm = TRUE) / total_weight),
          sd(val, na.rm = TRUE)
        ),
        
        # Shrink toward league average for small samples
        !!paste0("mins_mean_", q) := ifelse(
          n_games_obs < 5,
          0.7 * mins_mean_raw + 0.3 * 8.0,
          mins_mean_raw
        ),
        !!paste0("mins_sd_", q) := ifelse(
          n_games_obs < 5,
          pmax(mins_sd_raw, 2.0, na.rm = TRUE),
          mins_sd_raw
        ),
        .groups = "drop"
      ) %>%
      dplyr::select(-n_games_obs, -mins_mean_raw, -mins_sd_raw, -total_weight)
  }
  
  mins_params <- Reduce(
    function(x, y) dplyr::full_join(x, y, by = "ESPN_PLAYER_ID"),
    lapply(quarters, build_q_params)
  )
  
  # ----------------------------------------------------------
  # B) Base table from FULL roster pool
  # ----------------------------------------------------------
  base_df <- roster_pool %>%
    dplyr::transmute(
      date_key       = date_key,
      GAME_DATE      = proj_date,
      ESPN_TEAM_ID,
      NBA_TEAM_ID,
      ESPN_PLAYER_ID,
      PLAYER
    ) %>%
    dplyr::distinct()
  
  # ----------------------------------------------------------
  # C) Availability flags from roster_out
  # ----------------------------------------------------------
  out_keys <- if (!is.null(roster_out) && nrow(roster_out) > 0) {
    paste0(roster_out$ESPN_TEAM_ID, "::", roster_out$ESPN_PLAYER_ID)
  } else {
    character()
  }
  
  # ----------------------------------------------------------
  # D) Attach params + flags
  # ----------------------------------------------------------
  df <- base_df %>%
    dplyr::left_join(
      mins_params,
      by = "ESPN_PLAYER_ID"
    ) %>%
    dplyr::mutate(
      is_out    = as.integer(paste0(ESPN_TEAM_ID, "::", ESPN_PLAYER_ID) %in% out_keys),
      is_active = 1L - is_out
    ) %>%
    dplyr::select(
      date_key, GAME_DATE, ESPN_TEAM_ID, NBA_TEAM_ID, ESPN_PLAYER_ID, PLAYER,
      starts_with("mins_mean_"),
      starts_with("mins_sd_"),
      is_out, is_active
    )
  
  # ----------------------------------------------------------
  # E) Redistribute injured player minutes (minutes-weighted)
  # ----------------------------------------------------------
  # Instead of proportional scaling, we use each active player's
  # historical average minutes as the absorption weight.
  # High-minute players absorb more of the injured player's minutes
  # than bench players, which is a better approximation of real substitution.
  
  for (q in quarters) {
    col <- paste0("mins_mean_", q)
    if (!col %in% names(df)) next
    
    cap <- if (q %in% c("Q1","Q2","Q3","Q4")) 60 else 25
    
    df <- df %>%
      group_by(ESPN_TEAM_ID) %>%
      mutate(
        # Total minutes from active players
        active_total = sum(.data[[col]] * is_active, na.rm = TRUE),
        
        # Total minutes being vacated by injured players
        injured_mins = sum(.data[[col]] * is_out, na.rm = TRUE),
        
        # Each active player's share of active minutes (absorption weight)
        absorption_weight = case_when(
          is_active == 0 ~ 0,
          active_total <= 0 ~ 1 / sum(is_active, na.rm = TRUE),
          TRUE ~ .data[[col]] / active_total
        ),
        
        # Redistribute injured minutes proportionally to active players
        !!col := case_when(
          is_active == 0      ~ 0,
          is.na(.data[[col]]) ~ 0,
          active_total <= 0   ~ .data[[col]],
          TRUE ~ .data[[col]] + (injured_mins * absorption_weight)
        )
      ) %>%
      # Final safety clamp to exactly hit cap
      mutate(
        team_total = sum(.data[[col]] * is_active, na.rm = TRUE),
        !!col := case_when(
          is_active == 0  ~ 0,
          team_total <= 0 ~ .data[[col]],
          TRUE            ~ .data[[col]] * (cap / team_total)
        )
      ) %>%
      select(-active_total, -injured_mins, -absorption_weight, -team_total) %>%
      ungroup()
  }
  
  df
}
# ============================================================
# A) TODAY: BUILD MINUTES PARAMETER TABLE
# ============================================================
if (exists("roster_pool_today") && nrow(roster_pool_today) > 0) {
  
  cat("📊 Building TODAY minutes parameters for", as.character(as_of_date), "...\n")
  
  player_minutes_params_today <- build_minutes_params_one_date(
    proj_date    = as.Date(as_of_date),
    roster_pool  = roster_pool_today,
    roster_out   = roster_out_today,
    n_games_back = 10L
  )
  
  message(
    "✅ Minutes baseline params built | TODAY | date=", as.character(as_of_date),
    " | rows=", nrow(player_minutes_params_today)
  )
  
} else {
  player_minutes_params_today <- tibble()
  warning("⚠️  No games today or roster_pool_today missing - player_minutes_params_today is empty")
}

# ============================================================
# B) BACKTEST: BUILD STACKED MINUTES PARAMETER TABLE
# ============================================================
if (exists("roster_pool_backtest_by_date") && length(roster_pool_backtest_by_date) > 0) {
  
  cat("📊 Building BACKTEST minutes parameters...\n")
  
  bt_keys  <- names(roster_pool_backtest_by_date)
  bt_dates <- key_to_date(bt_keys)
  bt_keys  <- bt_keys[!is.na(bt_dates)]
  
  player_minutes_params_backtest <- dplyr::bind_rows(
    lapply(bt_keys, function(kk) {
      build_minutes_params_one_date(
        proj_date   = key_to_date(kk),
        roster_pool = roster_pool_backtest_by_date[[kk]],
        roster_out  = roster_out_backtest_by_date[[kk]]
      )
    })
  )
  
  message(
    "✅ Minutes baseline params built | BACKTEST | dates=", length(bt_keys),
    " | rows=", nrow(player_minutes_params_backtest)
  )
  
} else {
  player_minutes_params_backtest <- tibble()
  warning("⚠️  roster_pool_backtest_by_date missing or empty - player_minutes_params_backtest is empty")
}


# ============================================================
# JOIN: Minutes Parameters to Player Projections
# ============================================================
# This must happen BEFORE shooting profiles are built
# (shooting profiles need mins_mean_Q* columns)

if (nrow(player_projections_backtest) > 0 && 
    exists("player_minutes_params_backtest") && 
    nrow(player_minutes_params_backtest) > 0) {
  
  cat("🔗 Joining minutes parameters to player projections (BACKTEST)...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    left_join(
      player_minutes_params_backtest %>%
        select(
          date_key, ESPN_PLAYER_ID, ESPN_TEAM_ID,
          starts_with("mins_mean_"),
          starts_with("mins_sd_"),
          is_out, is_active
        ),
      by = c("date_key", "ESPN_PLAYER_ID", "ESPN_TEAM_ID")
    )
  
  message("✅ BACKTEST projections enriched with minutes params")
}

if (nrow(player_projections_today) > 0 && 
    exists("player_minutes_params_today") && 
    nrow(player_minutes_params_today) > 0) {
  
  cat("🔗 Joining minutes parameters to player projections (TODAY)...\n")
  
  player_projections_today <- player_projections_today %>%
    left_join(
      player_minutes_params_today %>%
        select(
          date_key, ESPN_PLAYER_ID, ESPN_TEAM_ID,
          starts_with("mins_mean_"),
          starts_with("mins_sd_"),
          is_out, is_active
        ),
      by = c("date_key", "ESPN_PLAYER_ID", "ESPN_TEAM_ID")
    )
  
  message("✅ TODAY projections enriched with minutes params")
}



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Minutes BASELINE PARAMETER BUILDER
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ======================================
# START: Pull in Team Names and Opp Names
# ======================================
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("player_projections_today"),
  exists("player_projections_backtest"),
  exists("nba_schedule_season")
)

stopifnot(
  all(c("team_id","team","opp_id","opp","home_away_sym","home_away") %in% names(nba_schedule_season))
)

# ============================================================
# A) ENRICH TODAY PROJECTIONS
# ============================================================
if (nrow(player_projections_today) > 0) {
  
  cat("📋 Enriching TODAY projections with game context...\n")
  
  player_projections_today <- player_projections_today %>%
    left_join(
      nba_schedule_season %>%
        transmute(
          GAME_DATE    = as.Date(game_date),
          ESPN_TEAM_ID = as.character(team_id),
          ESPN_GAME_ID = as.character(game_id)
        ),
      by = c("GAME_DATE", "ESPN_TEAM_ID")
    ) %>%
    left_join(
      nba_schedule_season %>%
        transmute(
          ESPN_GAME_ID  = as.character(game_id),
          ESPN_TEAM_ID  = as.character(team_id),
          TEAM          = team,
          ESPN_OPP_ID   = as.character(opp_id),
          OPP           = opp,
          HOME_AWAY     = home_away,
          HOME_AWAY_SYM = home_away_sym
        ),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID")
    )
  
  message("✅ TODAY projections enriched | rows=", nrow(player_projections_today))
  
} else {
  message("⏭️  Skipping TODAY enrichment (no games today)")
}

# ============================================================
# B) ENRICH BACKTEST PROJECTIONS
# ============================================================
if (nrow(player_projections_backtest) > 0) {
  
  cat("📋 Enriching BACKTEST projections with game context...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    left_join(
      nba_schedule_season %>%
        transmute(
          GAME_DATE    = as.Date(game_date),
          ESPN_TEAM_ID = as.character(team_id),
          ESPN_GAME_ID = as.character(game_id)
        ),
      by = c("GAME_DATE", "ESPN_TEAM_ID")
    ) %>%
    left_join(
      nba_schedule_season %>%
        transmute(
          ESPN_GAME_ID  = as.character(game_id),
          ESPN_TEAM_ID  = as.character(team_id),
          TEAM          = team,
          ESPN_OPP_ID   = as.character(opp_id),
          OPP           = opp,
          HOME_AWAY     = home_away,
          HOME_AWAY_SYM = home_away_sym
        ),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID")
    )
  
  message("✅ BACKTEST projections enriched | rows=", nrow(player_projections_backtest))
  
} else {
  message("⏭️  Skipping BACKTEST enrichment (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ======================================
# END: Pull in Team Names and Opp Names
# ======================================
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩
# START: DATA VALIDATION
# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩
# ============================================================
# C) DATA VALIDATION
# ============================================================
validate_projections <- function(df, df_name) {
  
  # Skip validation if dataframe is empty
  if (nrow(df) == 0) {
    cat("⚠️ ", df_name, "is empty - skipping validation\n")
    return(invisible(NULL))
  }
  
  # Check for duplicate player-game combinations
  dupes <- df %>%
    group_by(date_key, ESPN_PLAYER_ID, ESPN_TEAM_ID) %>%
    filter(n() > 1) %>%
    ungroup()
  
  if (nrow(dupes) > 0) {
    warning(df_name, ": Found ", nrow(dupes), " duplicate player-game records")
    print(head(dupes %>% select(date_key, ESPN_PLAYER_ID, ESPN_TEAM_ID)))
  }
  
  # Check for missing key identifiers
  missing_player_id <- sum(is.na(df$ESPN_PLAYER_ID))
  missing_team_id   <- sum(is.na(df$ESPN_TEAM_ID))
  missing_date      <- sum(is.na(df$GAME_DATE))
  
  if (missing_player_id > 0) warning(df_name, ": ", missing_player_id, " rows missing ESPN_PLAYER_ID")
  if (missing_team_id > 0)   warning(df_name, ": ", missing_team_id, " rows missing ESPN_TEAM_ID")
  if (missing_date > 0)      warning(df_name, ": ", missing_date, " rows missing GAME_DATE")
  
  # Summary
  cat("✓", df_name, "validation complete:",
      nrow(df), "rows |",
      length(unique(df$ESPN_PLAYER_ID)), "unique players |",
      length(unique(df$date_key)), "unique dates\n")
  
  invisible(NULL)
}

# Run validations
validate_projections(player_projections_today, "player_projections_today")
validate_projections(player_projections_backtest, "player_projections_backtest")

# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩
# END DATA VALIDATION
# 🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩🧩



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#     oooooooooooo  o8o            oooo        .o8         .oooooo.                        oooo       ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888'     `8  `"'            `888       "888        d8P'  `Y8b                       `888       `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888         oooo   .ooooo.   888   .oooo888       888            .ooooo.   .oooo.    888        888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888oooo8    `888  d88' `88b  888  d88' `888       888           d88' `88b `P  )88b   888        888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888    "     888  888ooo888  888  888   888       888     ooooo 888   888  .oP"888   888        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888          888  888    .o  888  888   888       `88.    .88'  888   888 d8(  888   888        888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888o        o888o `Y8bod8P' o888o `Y8bod88P"       `Y8bood8P'   `Y8bod8P' `Y888""8o o888o      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                                                          `"              "'   
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀#                                                                                                                                                                                          

# ==========================================================================================================
# PLAYER SHOOTING RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe, deterministic shooting rate profiles for players using historical
# data. For each date (TODAY and BACKTEST), recent games occurring strictly before that date are used
# to estimate player-specific shooting efficiencies (2PT%, 3PT%, FT%), shot mix (3PA rate), and foul
# drawing rates, ensuring no future information leaks into the model.
#
# Shooting baselines are computed at the player level using a rolling window of recent games to remain
# responsive to form changes. Small samples and missing data are handled via Bayesian shrinkage toward
# league averages. These rates are then expanded into a quarter-aware structure so that shooting skill
# aligns directly with quarter-level minutes.
#
# This layer introduces no randomness or simulation. All variance, noise, and stochastic behavior are
# applied later within the Monte Carlo engine, using these profiles as stable, deterministic inputs.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Player Shooting Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build player shooting baselines (LEAKAGE-SAFE)
# --------------------------------------------------
build_player_shooting_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      PLAYER_NAME    = as.character(PLAYER_NAME),
      
      FGM2 = num0(`2PTM_CGS`, NA_real_),
      FGA2 = num0(`2PTA_CGS`, NA_real_),
      FGM3 = num0(`3PTM_CGS`, NA_real_),
      FGA3 = num0(`3PTA_CGS`, NA_real_),
      FTM  = num0(FTM_CGS,   NA_real_),
      FTA  = num0(FTA_CGS,   NA_real_)
    ) %>%
    filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    group_by(ESPN_PLAYER_ID) %>%  # CHANGED: Removed ESPN_TEAM_ID (trade handling)
    slice_tail(n = n_games_back) %>%
    summarise(
      n_2pt_attempts = sum(FGA2),
      n_3pt_attempts = sum(FGA3),
      n_ft_attempts  = sum(FTA),
      
      FG2_pct_raw = ifelse(sum(FGA2) > 0, sum(FGM2) / sum(FGA2), NA_real_),
      FG3_pct_raw = ifelse(sum(FGA3) > 0, sum(FGM3) / sum(FGA3), NA_real_),
      FT_pct_raw  = ifelse(sum(FTA)  > 0, sum(FTM)  / sum(FTA),  NA_real_),
      
      p3_raw = ifelse(
        (sum(FGA2) + sum(FGA3)) > 0,
        sum(FGA3) / (sum(FGA2) + sum(FGA3)),
        NA_real_
      ),
      
      FTA_per_FGA_raw = ifelse(
        (sum(FGA2) + sum(FGA3)) > 0,
        sum(FTA) / (sum(FGA2) + sum(FGA3)),
        NA_real_
      ),
      
      # ADDED: Fallback to league averages + small sample shrinkage
      FG2_pct = case_when(
        is.na(FG2_pct_raw) ~ 0.52,  # League average 2PT%
        n_2pt_attempts < 10 ~ 0.7 * FG2_pct_raw + 0.3 * 0.52,  # Shrink toward league avg
        TRUE ~ FG2_pct_raw
      ),
      
      FG3_pct = case_when(
        is.na(FG3_pct_raw) ~ 0.36,  # League average 3PT%
        n_3pt_attempts < 10 ~ 0.7 * FG3_pct_raw + 0.3 * 0.36,
        TRUE ~ FG3_pct_raw
      ),
      
      FT_pct = case_when(
        is.na(FT_pct_raw) ~ 0.78,  # League average FT%
        n_ft_attempts < 5 ~ 0.7 * FT_pct_raw + 0.3 * 0.78,
        TRUE ~ FT_pct_raw
      ),
      
      p3 = ifelse(is.na(p3_raw), 0.35, p3_raw),  # League average ~35% of shots are 3s
      
      FTA_per_FGA = ifelse(is.na(FTA_per_FGA_raw), 0.25, FTA_per_FGA_raw),  # League avg
      
      .groups = "drop"
    ) %>%
    select(ESPN_PLAYER_ID, FG2_pct, FG3_pct, FT_pct, p3, FTA_per_FGA)  # Drop raw/count columns
}

# ============================================================
# B) TODAY: Build Shooting Profile
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY shooting profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_player_shooting_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join shooting rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_shooting_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q      = get_mins_q(player_projections_today, q),
        
        FG2_pct     = player_projections_today$FG2_pct,
        FG3_pct     = player_projections_today$FG3_pct,
        FT_pct      = player_projections_today$FT_pct,
        p3          = player_projections_today$p3,
        FTA_per_FGA = player_projections_today$FTA_per_FGA
      )
    })
  )
  
  message(
    "✅ TODAY shooting profile built | rows=", 
    nrow(player_shooting_profile_q_today)
  )
  
} else {
  player_shooting_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY shooting profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build Shooting Profile
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST shooting profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  shoot_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache shooting baselines by date
    if (!exists(k, envir = shoot_cache)) {
      base <- build_player_shooting_baseline(dd, 10L)
      assign(k, base, envir = shoot_cache)
    }
    
    base <- get(k, envir = shoot_cache)
    
    # Join shooting rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q      = get_mins_q(sub, q),
          
          FG2_pct     = sub$FG2_pct,
          FG3_pct     = sub$FG3_pct,
          FT_pct      = sub$FT_pct,
          p3          = sub$p3,
          FTA_per_FGA = sub$FTA_per_FGA
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_shooting_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST shooting profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_shooting_profile_q)
  )
  
} else {
  player_shooting_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST shooting profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Player Shooting Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#     ooooooooo.              o8o                  .                 ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888   `Y88.            `"'                .o8                 `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888   .d88'  .ooooo.  oooo  ooo. .oo.   .o888oo  .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888ooo88P'  d88' `88b `888  `888P"Y88b    888   d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888         888   888  888   888   888    888   `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888         888   888  888   888   888    888 . o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888o        `Y8bod8P' o888o o888o o888o   "888" 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                         `"              "'   
#                                                                                                                                 
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

# ==========================================================================================================
# POINTS PER MINUTE (PPM) RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds deterministic, leakage-safe baseline points-per-minute (PPM) scoring rates for
# players using historical data. For each game date (TODAY and BACKTEST), a rolling window of recent
# games occurring strictly before that date is used to estimate how efficiently a player converts minutes
# into points, ensuring no future information contaminates the baseline.
#
# Player-specific PPM rates are computed at the player level (cross-team to handle trades) and then
# expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation design.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# All variance, shrinkage, and randomness are deferred entirely to the Monte Carlo simulation layer,
# where PPM will later be combined with simulated minutes to generate full scoring distributions.
#
# This layer is strictly a parameter-building step and should remain deterministic.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# START: Points Per Minute Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build deterministic PPM baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_ppm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      PTS_NUM        = num0(PTS_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins     = sum(MINS_NUM, na.rm = TRUE),
      total_pts      = sum(PTS_NUM,  na.rm = TRUE),
      total_weight   = sum(rec_weight, na.rm = TRUE),
      
      # Recency-weighted PPM
      pts_per_min_raw = ifelse(
        sum(MINS_NUM * rec_weight, na.rm = TRUE) > 0,
        sum(PTS_NUM  * rec_weight, na.rm = TRUE) / 
          sum(MINS_NUM * rec_weight, na.rm = TRUE),
        NA_real_
      ),
      
      pts_per_min = case_when(
        is.na(pts_per_min_raw) ~ 0.40,
        total_mins < 50        ~ 0.7 * pts_per_min_raw + 0.3 * 0.40,
        TRUE                   ~ pts_per_min_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, pts_per_min)
}

# ============================================================
# B) TODAY: Build PPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY PPM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_ppm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join PPM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_ppm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q      = get_mins_q(player_projections_today, q),
        pts_per_min = player_projections_today$pts_per_min
      )
    })
  )
  
  message(
    "✅ TODAY PPM profile built | rows=", 
    nrow(player_ppm_profile_q_today)
  )
  
} else {
  player_ppm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY PPM profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build PPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST PPM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  ppm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache PPM baselines by date
    if (!exists(k, envir = ppm_cache)) {
      base <- build_ppm_baseline(dd, 10L)
      assign(k, base, envir = ppm_cache)
    }
    
    base <- get(k, envir = ppm_cache)
    
    # Join PPM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q      = get_mins_q(sub, q),
          pts_per_min = sub$pts_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_ppm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST PPM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_ppm_profile_q)
  )
  
} else {
  player_ppm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST PPM profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# END: Points Per Minute Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#     ooooooooo.              .o8                                               .o8                ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888   `Y88.           "888                                              "888                `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888   .d88'  .ooooo.   888oooo.   .ooooo.  oooo  oooo  ooo. .oo.    .oooo888   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888ooo88P'  d88' `88b  d88' `88b d88' `88b `888  `888  `888P"Y88b  d88' `888  d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888`88b.    888ooo888  888   888 888   888  888   888   888   888  888   888  `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888  `88b.  888    .o  888   888 888   888  888   888   888   888  888   888  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888o  o888o `Y8bod8P'  `Y8bod8P' `Y8bod8P'  `V88V"V8P' o888o o888o `Y8bod88P" 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                                                       `"              "'   
#                                                                                                                                                                                       
#                                                                                                                                                                                                    
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ==========================================================================================================
# REBOUNDS PER MINUTE (RPM) RATE PROFILE - OREB & DREB SPLIT
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline rebounds-per-minute rates for players, split into
# offensive rebounds (OREB) and defensive rebounds (DREB). For each game date (TODAY and BACKTEST),
# a rolling window of recent games occurring strictly before that date is used to estimate how
# frequently a player records each rebound type relative to minutes played.
#
# Player-specific OREB/DREB rates are computed at the player level (cross-team to handle trades) and
# then expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# The resulting rates are deterministic parameters intended to be multiplied by simulated minutes
# inside the Monte Carlo engine. No variance or stochastic behavior is applied here; all randomness
# is centralized within the MC layer to avoid double-counting variance.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Rebounds Per Min Rate Profile (OREB/DREB Split)
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build OREB/DREB/MIN baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_rpm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      OREB_NUM       = num0(OREB_CGS, NA_real_),
      DREB_NUM       = num0(DREB_CGS, NA_real_),
      REB_NUM        = num0(REB_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%  # Cross-team for trade handling
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::summarise(
      total_mins = sum(MINS_NUM),
      total_oreb = sum(OREB_NUM),
      total_dreb = sum(DREB_NUM),
      total_reb  = sum(REB_NUM),
      
      oreb_per_min_raw = ifelse(total_mins > 0, total_oreb / total_mins, NA_real_),
      dreb_per_min_raw = ifelse(total_mins > 0, total_dreb / total_mins, NA_real_),
      reb_per_min_raw  = ifelse(total_mins > 0, total_reb  / total_mins, NA_real_),
      
      # Fallback + small sample shrinkage
      oreb_per_min = case_when(
        is.na(oreb_per_min_raw) ~ 0.04,  # League avg ~0.04 oreb/min (~1.9 oreb/48min)
        total_mins < 50 ~ 0.7 * oreb_per_min_raw + 0.3 * 0.04,
        TRUE ~ oreb_per_min_raw
      ),
      
      dreb_per_min = case_when(
        is.na(dreb_per_min_raw) ~ 0.16,  # League avg ~0.16 dreb/min (~7.7 dreb/48min)
        total_mins < 50 ~ 0.7 * dreb_per_min_raw + 0.3 * 0.16,
        TRUE ~ dreb_per_min_raw
      ),
      
      reb_per_min = case_when(
        is.na(reb_per_min_raw) ~ 0.20,  # League avg ~0.20 total reb/min
        total_mins < 50 ~ 0.7 * reb_per_min_raw + 0.3 * 0.20,
        TRUE ~ reb_per_min_raw
      ),
      
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, oreb_per_min, dreb_per_min, reb_per_min)
}

# ============================================================
# B) TODAY: Build RPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY RPM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_rpm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join RPM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_rpm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q       = get_mins_q(player_projections_today, q),
        oreb_per_min = player_projections_today$oreb_per_min,
        dreb_per_min = player_projections_today$dreb_per_min,
        reb_per_min  = player_projections_today$reb_per_min
      )
    })
  )
  
  message(
    "✅ TODAY RPM profile built | rows=", 
    nrow(player_rpm_profile_q_today)
  )
  
} else {
  player_rpm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY RPM profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build RPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST RPM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  rpm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache RPM baselines by date
    if (!exists(k, envir = rpm_cache)) {
      base <- build_rpm_baseline(dd, 10L)
      assign(k, base, envir = rpm_cache)
    }
    
    base <- get(k, envir = rpm_cache)
    
    # Join RPM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q       = get_mins_q(sub, q),
          oreb_per_min = sub$oreb_per_min,
          dreb_per_min = sub$dreb_per_min,
          reb_per_min  = sub$reb_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_rpm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST RPM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_rpm_profile_q)
  )
  
} else {
  player_rpm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST RPM profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Rebounds Per Min Rate Profile (OREB/DREB Split)
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#           .o.                          o8o               .                 ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#          .888.                         `"'             .o8                 `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#         .8"888.      .oooo.o  .oooo.o oooo   .oooo.o .o888oo  .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#        .8' `888.    d88(  "8 d88(  "8 `888  d88(  "8   888   d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#       .88ooo8888.   `"Y88b.  `"Y88b.   888  `"Y88b.    888   `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      .8'     `888.  o.  )88b o.  )88b  888  o.  )88b   888 . o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o88o     o8888o 8""888P' 8""888P' o888o 8""888P'   "888" 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                                 `"              "'   
#                                                                                                                                                                 
#                                                                                                                                                                 
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ==========================================================================================================
# ASSISTS PER MINUTE (APM) RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline assists-per-minute (AST/MIN) rates for players using
# historical data. For each game date (TODAY and BACKTEST), a rolling window of recent games
# occurring strictly before that date is used to estimate how frequently a player records assists
# relative to minutes played.
#
# Player-specific APM rates are computed at the player level (cross-team to handle trades) and then
# expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation design.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# The resulting AST/MIN rates are deterministic parameters intended to be multiplied by simulated
# minutes inside the Monte Carlo engine. No variance, shrinkage, or stochastic behavior is applied
# here; all randomness is centralized within the MC layer to avoid double-counting variance.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Assists Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build AST/MIN baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_apm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      AST_NUM        = num0(AST_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins   = sum(MINS_NUM, na.rm = TRUE),
      total_weight = sum(rec_weight, na.rm = TRUE),
      
      ast_per_min_raw = ifelse(
        sum(MINS_NUM * rec_weight, na.rm = TRUE) > 0,
        sum(AST_NUM  * rec_weight, na.rm = TRUE) /
          sum(MINS_NUM * rec_weight, na.rm = TRUE),
        NA_real_
      ),
      
      ast_per_min = case_when(
        is.na(ast_per_min_raw) ~ 0.12,
        total_mins < 50        ~ 0.7 * ast_per_min_raw + 0.3 * 0.12,
        TRUE                   ~ ast_per_min_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, ast_per_min)
}

# ============================================================
# B) TODAY: Build APM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY APM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_apm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join APM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_apm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q      = get_mins_q(player_projections_today, q),
        ast_per_min = player_projections_today$ast_per_min
      )
    })
  )
  
  message(
    "✅ TODAY APM profile built | rows=", 
    nrow(player_apm_profile_q_today)
  )
  
} else {
  player_apm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY APM profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build APM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST APM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  apm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache APM baselines by date
    if (!exists(k, envir = apm_cache)) {
      base <- build_apm_baseline(dd, 10L)
      assign(k, base, envir = apm_cache)
    }
    
    base <- get(k, envir = apm_cache)
    
    # Join APM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q      = get_mins_q(sub, q),
          ast_per_min = sub$ast_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_apm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST APM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_apm_profile_q)
  )
  
} else {
  player_apm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST APM profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Assists Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#     oooooooooo.  oooo                      oooo                      ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888'   `Y8b `888                      `888                      `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888     888  888   .ooooo.   .ooooo.   888  oooo   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888oooo888'  888  d88' `88b d88' `"Y8  888 .8P'   d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888    `88b  888  888   888 888        888888.    `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888    .88P  888  888   888 888   .o8  888 `88b.  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888bood8P'  o888o `Y8bod8P' `Y8bod8P' o888o o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                           `"              "'   
#
#
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ==========================================================================================================
# BLOCKS PER MINUTE (BPM) RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline blocks-per-minute (BLK/MIN) rates for players using
# historical data. For each game date (TODAY and BACKTEST), a rolling window of recent games
# occurring strictly before that date is used to estimate how frequently a player records blocks
# relative to minutes played.
#
# Player-specific BPM rates are computed at the player level (cross-team to handle trades) and then
# expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation design.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# The resulting BLK/MIN rates are deterministic parameters intended to be multiplied by simulated
# minutes inside the Monte Carlo engine. No variance, shrinkage, or stochastic behavior is applied
# here; all randomness is centralized within the MC layer to avoid double-counting variance.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Blocks Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build BLK/MIN baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_bpm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      BLK_NUM        = num0(BLK_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins   = sum(MINS_NUM, na.rm = TRUE),
      total_weight = sum(rec_weight, na.rm = TRUE),
      
      blk_per_min_raw = ifelse(
        sum(MINS_NUM * rec_weight, na.rm = TRUE) > 0,
        sum(BLK_NUM  * rec_weight, na.rm = TRUE) /
          sum(MINS_NUM * rec_weight, na.rm = TRUE),
        NA_real_
      ),
      
      blk_per_min = case_when(
        is.na(blk_per_min_raw) ~ 0.025,
        total_mins < 50        ~ 0.7 * blk_per_min_raw + 0.3 * 0.025,
        TRUE                   ~ blk_per_min_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, blk_per_min)
}

# ============================================================
# B) TODAY: Build BPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY BPM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_bpm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join BPM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_bpm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q      = get_mins_q(player_projections_today, q),
        blk_per_min = player_projections_today$blk_per_min
      )
    })
  )
  
  message(
    "✅ TODAY BPM profile built | rows=", 
    nrow(player_bpm_profile_q_today)
  )
  
} else {
  player_bpm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY BPM profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build BPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST BPM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  bpm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache BPM baselines by date
    if (!exists(k, envir = bpm_cache)) {
      base <- build_bpm_baseline(dd, 10L)
      assign(k, base, envir = bpm_cache)
    }
    
    base <- get(k, envir = bpm_cache)
    
    # Join BPM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q      = get_mins_q(sub, q),
          blk_per_min = sub$blk_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_bpm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST BPM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_bpm_profile_q)
  )
  
} else {
  player_bpm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST BPM profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Blocks Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#      .oooooo..o     .                       oooo                ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     d8P'    `Y8   .o8                       `888                `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#     Y88bo.      .o888oo  .ooooo.   .oooo.    888   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      `"Y8888o.    888   d88' `88b `P  )88b   888  d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#          `"Y88b   888   888ooo888  .oP"888   888  `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#     oo     .d8P   888 . 888    .o d8(  888   888  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     8""88888P'    "888" `Y8bod8P' `Y888""8o o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                      `"              "'   
#                                                                                                                               
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ==========================================================================================================
# STEALS PER MINUTE (SPM) RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline steals-per-minute (STL/MIN) rates for players using
# historical data. For each game date (TODAY and BACKTEST), a rolling window of recent games
# occurring strictly before that date is used to estimate how frequently a player records steals
# relative to minutes played.
#
# Player-specific SPM rates are computed at the player level (cross-team to handle trades) and then
# expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation design.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# The resulting STL/MIN rates are deterministic parameters intended to be multiplied by simulated
# minutes inside the Monte Carlo engine. No variance, shrinkage, or stochastic behavior is applied
# here; all randomness is centralized within the MC layer to avoid double-counting variance.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Steals Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build STL/MIN baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_spm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      STL_NUM        = num0(STL_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins   = sum(MINS_NUM, na.rm = TRUE),
      total_weight = sum(rec_weight, na.rm = TRUE),
      
      stl_per_min_raw = ifelse(
        sum(MINS_NUM * rec_weight, na.rm = TRUE) > 0,
        sum(STL_NUM  * rec_weight, na.rm = TRUE) /
          sum(MINS_NUM * rec_weight, na.rm = TRUE),
        NA_real_
      ),
      
      stl_per_min = case_when(
        is.na(stl_per_min_raw) ~ 0.035,
        total_mins < 50        ~ 0.7 * stl_per_min_raw + 0.3 * 0.035,
        TRUE                   ~ stl_per_min_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, stl_per_min)
}

# ============================================================
# B) TODAY: Build SPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY SPM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_spm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join SPM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_spm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q      = get_mins_q(player_projections_today, q),
        stl_per_min = player_projections_today$stl_per_min
      )
    })
  )
  
  message(
    "✅ TODAY SPM profile built | rows=", 
    nrow(player_spm_profile_q_today)
  )
  
} else {
  player_spm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY SPM profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build SPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST SPM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  spm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache SPM baselines by date
    if (!exists(k, envir = spm_cache)) {
      base <- build_spm_baseline(dd, 10L)
      assign(k, base, envir = spm_cache)
    }
    
    base <- get(k, envir = spm_cache)
    
    # Join SPM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q      = get_mins_q(sub, q),
          stl_per_min = sub$stl_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_spm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST SPM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_spm_profile_q)
  )
  
} else {
  player_spm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST SPM profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Steals Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#     ooooooooooooo                                                                                         ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     8'   888   `8                                                                                         `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#          888      oooo  oooo  oooo d8b ooo. .oo.    .ooooo.  oooo    ooo  .ooooo.  oooo d8b  .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#          888      `888  `888  `888""8P `888P"Y88b  d88' `88b  `88.  .8'  d88' `88b `888""8P d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#          888       888   888   888      888   888  888   888   `88..8'   888ooo888  888     `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#          888       888   888   888      888   888  888   888    `888'    888    .o  888     o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#         o888o      `V88V"V8P' d888b    o888o o888o `Y8bod8P'     `8'     `Y8bod8P' d888b    8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                                                                `"              "'   
#                                                                                                                                                                                                
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ==========================================================================================================
# TURNOVERS PER MINUTE (TOVPM) RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline turnovers-per-minute (TOV/MIN) rates for players using
# historical data. For each game date (TODAY and BACKTEST), a rolling window of recent games
# occurring strictly before that date is used to estimate how frequently a player commits turnovers
# relative to minutes played.
#
# Player-specific TOVPM rates are computed at the player level (cross-team to handle trades) and then
# expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation design.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# The resulting TOV/MIN rates are deterministic parameters intended to be multiplied by simulated
# minutes inside the Monte Carlo engine. No variance, shrinkage, or stochastic behavior is applied
# here; all randomness is centralized within the MC layer to avoid double-counting variance.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Turnovers Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Build TOV/MIN baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_tovpm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      TOV_NUM        = num0(TOV_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins   = sum(MINS_NUM, na.rm = TRUE),
      total_weight = sum(rec_weight, na.rm = TRUE),
      
      tov_per_min_raw = ifelse(
        sum(MINS_NUM * rec_weight, na.rm = TRUE) > 0,
        sum(TOV_NUM  * rec_weight, na.rm = TRUE) /
          sum(MINS_NUM * rec_weight, na.rm = TRUE),
        NA_real_
      ),
      
      tov_per_min = case_when(
        is.na(tov_per_min_raw) ~ 0.06,
        total_mins < 50        ~ 0.7 * tov_per_min_raw + 0.3 * 0.06,
        TRUE                   ~ tov_per_min_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, tov_per_min)
}

# ============================================================
# B) TODAY: Build TOVPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY TOVPM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_tovpm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join TOVPM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_tovpm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE      = player_projections_today$GAME_DATE,
        ESPN_GAME_ID   = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID   = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID = player_projections_today$ESPN_PLAYER_ID,
        qtr            = q,
        
        mins_q      = get_mins_q(player_projections_today, q),
        tov_per_min = player_projections_today$tov_per_min
      )
    })
  )
  
  message(
    "✅ TODAY TOVPM profile built | rows=", 
    nrow(player_tovpm_profile_q_today)
  )
  
} else {
  player_tovpm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY TOVPM profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build TOVPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST TOVPM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  tovpm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache TOVPM baselines by date
    if (!exists(k, envir = tovpm_cache)) {
      base <- build_tovpm_baseline(dd, 10L)
      assign(k, base, envir = tovpm_cache)
    }
    
    base <- get(k, envir = tovpm_cache)
    
    # Join TOVPM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE      = sub$GAME_DATE,
          ESPN_GAME_ID   = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID   = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID = sub$ESPN_PLAYER_ID,
          qtr            = q,
          
          mins_q      = get_mins_q(sub, q),
          tov_per_min = sub$tov_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_tovpm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST TOVPM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_tovpm_profile_q)
  )
  
} else {
  player_tovpm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST TOVPM profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Turnovers Per Min Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# ️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️

#
#     oooooooooooo                       oooo                ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888'     `8                       `888                `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888          .ooooo.  oooo  oooo   888   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888oooo8    d88' `88b `888  `888   888  d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888    "    888   888  888   888   888  `"Y88b.        888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888         888   888  888   888   888  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888o        `Y8bod8P'  `V88V"V8P' o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                 `"              "'   
#                                                                                                                                                 
# ️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
# ==========================================================================================================
# FOULS PER MINUTE (FPM) RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline fouls-per-minute (FOULS/MIN) rates for players using
# historical data. For each game date (TODAY and BACKTEST), a rolling window of recent games
# occurring strictly before that date is used to estimate how frequently a player commits fouls
# relative to minutes played.
#
# Player-specific FPM rates are computed at the player level (cross-team to handle trades) and then
# expanded into a quarter-aware structure to align with the quarter-by-quarter MC simulation design.
# Small samples are shrunk toward league averages to prevent overfitting and handle edge cases.
#
# The resulting FOULS/MIN rates are deterministic parameters intended to be multiplied by simulated
# minutes inside the Monte Carlo engine. No variance, shrinkage, or stochastic behavior is applied
# here; all randomness is centralized within the MC layer to avoid double-counting variance.
# ==========================================================================================================

# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
# ⚠️⚠️⚠️ START: Fouls Per Min Rate Profile
# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# Helper: safely extract quarter minutes
# --------------------------------------------------
get_mins_q <- function(df, q) {
  col <- paste0("mins_mean_Q", q)
  if (col %in% names(df)) return(as.numeric(df[[col]]))
  rep(0, nrow(df))
}

# --------------------------------------------------
# A) Choose FOULS column (robust)
# --------------------------------------------------
fouls_col <- if ("PF_CGS" %in% names(BaseStats_Player_MC)) {
  "PF_CGS"
} else if ("FOULS_CGS" %in% names(BaseStats_Player_MC)) {
  "FOULS_CGS"
} else if ("PERSONAL_FOULS_CGS" %in% names(BaseStats_Player_MC)) {
  "PERSONAL_FOULS_CGS"
} else {
  stop("No fouls column found in BaseStats_Player_MC")
}

# --------------------------------------------------
# B) Build FOULS/MIN baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_fpm_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      FOUL_NUM       = num0(.data[[fouls_col]], NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins   = sum(MINS_NUM, na.rm = TRUE),
      total_weight = sum(rec_weight, na.rm = TRUE),
      
      fouls_per_min_raw = ifelse(
        sum(MINS_NUM  * rec_weight, na.rm = TRUE) > 0,
        sum(FOUL_NUM  * rec_weight, na.rm = TRUE) /
          sum(MINS_NUM  * rec_weight, na.rm = TRUE),
        NA_real_
      ),
      
      fouls_per_min = case_when(
        is.na(fouls_per_min_raw) ~ 0.10,
        total_mins < 50          ~ 0.7 * fouls_per_min_raw + 0.3 * 0.10,
        TRUE                     ~ fouls_per_min_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, fouls_per_min)
}

# ============================================================
# C) TODAY: Build FPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY FPM profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_fpm_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join FPM rates to projections
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Expand into quarters
  player_fpm_profile_q_today <- bind_rows(
    lapply(quarters, function(q) {
      tibble(
        GAME_DATE       = player_projections_today$GAME_DATE,
        ESPN_GAME_ID    = player_projections_today$ESPN_GAME_ID,
        ESPN_TEAM_ID    = player_projections_today$ESPN_TEAM_ID,
        ESPN_PLAYER_ID  = player_projections_today$ESPN_PLAYER_ID,
        qtr             = q,
        
        mins_q        = get_mins_q(player_projections_today, q),
        fouls_per_min = player_projections_today$fouls_per_min
      )
    })
  )
  
  message(
    "✅ TODAY FPM profile built | rows=", 
    nrow(player_fpm_profile_q_today),
    " | fouls_col=", fouls_col
  )
  
} else {
  player_fpm_profile_q_today <- tibble()
  message("⏭️  Skipping TODAY FPM profile (no games today)")
}

# ============================================================
# D) BACKTEST: Build FPM Profile (Quarter-Expanded)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST FPM profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  fpm_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache FPM baselines by date
    if (!exists(k, envir = fpm_cache)) {
      base <- build_fpm_baseline(dd, 10L)
      assign(k, base, envir = fpm_cache)
    }
    
    base <- get(k, envir = fpm_cache)
    
    # Join FPM rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    # Expand into quarters
    sub_q <- bind_rows(
      lapply(quarters, function(q) {
        tibble(
          GAME_DATE       = sub$GAME_DATE,
          ESPN_GAME_ID    = sub$ESPN_GAME_ID,
          ESPN_TEAM_ID    = sub$ESPN_TEAM_ID,
          ESPN_PLAYER_ID  = sub$ESPN_PLAYER_ID,
          qtr             = q,
          
          mins_q        = get_mins_q(sub, q),
          fouls_per_min = sub$fouls_per_min
        )
      })
    )
    
    out[[i]] <- sub_q
  }
  
  player_fpm_profile_q <- bind_rows(out)
  
  message(
    "✅ BACKTEST FPM profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_fpm_profile_q),
    " | fouls_col=", fouls_col
  )
  
} else {
  player_fpm_profile_q <- tibble()
  message("⏭️  Skipping BACKTEST FPM profile (no backtest data)")
}

# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
# ⚠️⚠️⚠️ END: Fouls Per Min Rate Profile
# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
#
#
#     ooooo     ooo                                              ooooooooo.                 .                  ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888'     `8'                                              `888   `Y88.             .o8                  `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888       8   .oooo.o  .oooo.    .oooooooo  .ooooo.        888   .d88'  .oooo.   .o888oo  .ooooo.        888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888       8  d88(  "8 `P  )88b  888' `88b  d88' `88b       888ooo88P'  `P  )88b    888   d88' `88b       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888       8  `"Y88b.   .oP"888  888   888  888ooo888       888`88b.     .oP"888    888   888ooo888       888          888     888   888  888     888   888  888ooo888      88    888           88 
#      `88.    .8'  o.  )88b d8(  888  `88bod8P'  888    .o       888  `88b.  d8(  888    888 . 888    .o       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#        `YbodP'    8""888P' `Y888""8o `8oooooo.  `Y8bod8P'      o888o  o888o `Y888""8o   "888" `Y8bod8P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                      d"     YD                                                                                                                                    `"              "'   
#                                      "Y88888P'                                                                                                                                                         
#
#
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# ==========================================================================================================
# USAGE RATE PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline usage rates for players using historical data. Usage rate
# measures the percentage of team possessions a player uses while on the court (via field goal attempts,
# free throw attempts, and turnovers).
#
# Unlike per-minute stats, usage rate is a GAME-LEVEL constant that describes a player's offensive role
# and does not vary by quarter. A high-usage player (e.g., Luka at 35% usage) maintains that role whether
# playing in Q1 or Q4 - the variance comes from minutes played, not from changing usage rates.
#
# Usage rates are computed at the player level (cross-team to handle trades) using a rolling window of
# recent games. Small samples are shrunk toward position-based averages. These rates are NOT quarter-
# expanded because they represent a player's constant offensive role.
#
# The MC simulation will use usage rate to allocate shot attempts among players who are on the court
# during a given quarter, weighted by their usage rate and minutes played.
# ==========================================================================================================

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Usage Rate Profile (GAME-LEVEL CONSTANT)
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

stopifnot(
  exists("BaseStats_Player_MC"),
  exists("num0")
)

# --------------------------------------------------
# A) Build Usage Rate baseline (LEAKAGE-SAFE)
# --------------------------------------------------
build_usage_baseline <- function(cutoff_date, n_games_back = 10L) {
  
  BaseStats_Player_MC %>%
    dplyr::mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      USG_NUM        = num0(USG_CGS,  NA_real_),
      MINS_NUM       = num0(MINS_CGS, NA_real_)
    ) %>%
    dplyr::filter(!is.na(GAME_DATE), GAME_DATE < cutoff_date) %>%
    dplyr::arrange(ESPN_PLAYER_ID, GAME_DATE) %>%
    dplyr::group_by(ESPN_PLAYER_ID) %>%
    dplyr::slice_tail(n = n_games_back) %>%
    dplyr::mutate(
      days_ago   = as.numeric(as.Date(cutoff_date) - GAME_DATE),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    dplyr::summarise(
      total_mins   = sum(MINS_NUM, na.rm = TRUE),
      total_weight = sum(rec_weight, na.rm = TRUE),
      n_games      = n(),
      
      # Usage is already a rate so weight by recency only (not minutes)
      usage_rate_raw = ifelse(
        total_weight > 0,
        sum(USG_NUM * rec_weight, na.rm = TRUE) / total_weight,
        NA_real_
      ),
      
      usage_rate = case_when(
        is.na(usage_rate_raw) ~ 0.20,
        total_mins < 50       ~ 0.7 * usage_rate_raw + 0.3 * 0.20,
        TRUE                  ~ usage_rate_raw
      ),
      .groups = "drop"
    ) %>%
    dplyr::select(ESPN_PLAYER_ID, usage_rate)
}

# ============================================================
# B) TODAY: Build Usage Rate Profile (GAME-LEVEL)
# ============================================================
if (exists("player_projections_today") && nrow(player_projections_today) > 0) {
  
  cat("📊 Building TODAY usage rate profile for", as.character(as_of_date), "...\n")
  
  today_base <- build_usage_baseline(
    cutoff_date = as.Date(as_of_date),
    n_games_back = 10L
  )
  
  # Join usage rates to projections (GAME-LEVEL, not quarter-expanded)
  player_projections_today <- player_projections_today %>%
    left_join(today_base, by = "ESPN_PLAYER_ID")
  
  # Create game-level usage profile (NOT quarter-expanded)
  player_usage_profile_today <- player_projections_today %>%
    select(
      GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, ESPN_PLAYER_ID,
      usage_rate
    ) %>%
    distinct()
  
  message(
    "✅ TODAY usage rate profile built | rows=", 
    nrow(player_usage_profile_today)
  )
  
} else {
  player_usage_profile_today <- tibble()
  message("⏭️  Skipping TODAY usage rate profile (no games today)")
}

# ============================================================
# C) BACKTEST: Build Usage Rate Profile (GAME-LEVEL)
# ============================================================
if (exists("player_projections_backtest") && nrow(player_projections_backtest) > 0) {
  
  cat("📊 Building BACKTEST usage rate profiles...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(GAME_DATE = as.Date(GAME_DATE))
  
  bt_dates <- sort(unique(player_projections_backtest$GAME_DATE))
  bt_dates <- bt_dates[!is.na(bt_dates)]
  
  usage_cache <- new.env(parent = emptyenv())
  out <- vector("list", length(bt_dates))
  
  for (i in seq_along(bt_dates)) {
    
    dd  <- bt_dates[i]
    sub <- player_projections_backtest %>% filter(GAME_DATE == dd)
    if (nrow(sub) == 0) next
    
    k <- format(dd, "%Y%m%d")
    
    # Cache usage baselines by date
    if (!exists(k, envir = usage_cache)) {
      base <- build_usage_baseline(dd, 10L)
      assign(k, base, envir = usage_cache)
    }
    
    base <- get(k, envir = usage_cache)
    
    # Join usage rates to projections for this date
    sub <- sub %>%
      left_join(base, by = "ESPN_PLAYER_ID")
    
    out[[i]] <- sub
  }
  
  # Stack all dates back into player_projections_backtest
  player_projections_backtest <- bind_rows(out)
  
  # Create game-level usage profile (NOT quarter-expanded)
  player_usage_profile_backtest <- player_projections_backtest %>%
    select(
      GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, ESPN_PLAYER_ID,
      usage_rate
    ) %>%
    distinct()
  
  message(
    "✅ BACKTEST usage rate profile built | dates=", length(bt_dates),
    " | rows=", nrow(player_usage_profile_backtest)
  )
  
} else {
  player_usage_profile_backtest <- tibble()
  message("⏭️  Skipping BACKTEST usage rate profile (no backtest data)")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Usage Rate Profile
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠
# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠
#
#
#     ooooo   ooooo                                                  .o.             .o8                                        .                                       ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooo.   o.   
#     `888'   `888'                                                 .888.           "888                                      .o8                                       `888   `Y88.                     888 `"  `"'  `888                  .8' `888   `Y88. `8.  
#      888     888   .ooooo.  ooo. .oo.  .oo.    .ooooo.           .8"888.      .oooo888  oooo    ooo  .oooo.   ooo. .oo.   .o888oo  .oooo.    .oooooooo  .ooooo.        888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'   888   .d88'  `8. 
#      888ooooo888  d88' `88b `888P"Y88bP"Y88b  d88' `88b         .8' `888.    d88' `888   `88.  .8'  `P  )88b  `888P"Y88b    888   `P  )88b  888' `88b  d88' `88b       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88    888ooo88P'    88 
#      888     888  888   888  888   888   888  888ooo888        .88ooo8888.   888   888    `88..8'    .oP"888   888   888    888    .oP"888  888   888  888ooo888       888          888     888   888  888     888   888  888ooo888      88    888           88 
#      888     888  888   888  888   888   888  888    .o       .8'     `888.  888   888     `888'    d8(  888   888   888    888 . d8(  888  `88bod8P'  888    .o       888          888     888   888  888     888   888  888    .o      `8.   888          .8' 
#     o888o   o888o `Y8bod8P' o888o o888o o888o `Y8bod8P'      o88o     o8888o `Y8bod88P"     `8'     `Y888""8o o888o o888o   "888" `Y888""8o `8oooooo.  `Y8bod8P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8. o888o        .8'  
#                                                                                                                                             d"     YD                                                                                      `"              "'   
#                                                                                                                                             "Y88888P'                                                                                                           
#                                                                                                                                                                                                                                                            
# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠
# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠
# ==========================================================================================================
# HOME COURT ADVANTAGE ADJUSTMENTS
# ----------------------------------------------------------------------------------------------------------
# This section applies empirically-derived home court advantage adjustments to team offense profiles.
# Extensive NBA research shows that home teams perform measurably better across multiple dimensions:
#
# SHOOTING EFFICIENCY:
#   - 2PT shooting: ~+1.5% at home (more comfortable rim, familiar depth perception)
#   - 3PT shooting: ~+2.0% at home (greater effect on perimeter shots)
#   - FT shooting: ~+1.0% at home (less pressure, crowd support)
#
# FOUL CALLS:
#   - Free throw rate: ~+5% at home (documented referee home bias)
#
# OVERALL IMPACT:
#   - Home teams win ~58% of NBA games historically
#   - Point spread adjustment: ~2.5 points for home court
#
# NOTE: HCA is applied directly to team_offense_adjusted_q (not player profiles) to ensure
# it flows correctly into the MC simulation. Player-level HCA was removed as HOME_AWAY
# column was not reliably available at that stage of the pipeline.
#
# TODO: Add rest days adjustment, travel distance, back-to-back flags
# ==========================================================================================================

# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠
# 🏠🏠🏠 START: Home Court Advantage Adjustments
# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠

# --------------------------------------------------
# Home Court Adjustment Parameters (Conservative)
# --------------------------------------------------
HCA <- list(
  fg2_mult = 1.015,   # +1.5% on 2PT shooting
  fg3_mult = 1.020,   # +2.0% on 3PT shooting
  ft_mult  = 1.010,   # +1.0% on FT shooting
  fta_mult = 1.050,   # +5% more FT attempts (referee bias)
  mins_mult = 1.010   # +1% minutes (unused for now)
  # TODO: Add tov_mult, oreb_mult when rest/travel features are added
)

# --------------------------------------------------
# Apply HCA to BACKTEST team offense profiles
# --------------------------------------------------
if (exists("team_offense_adjusted_q") && nrow(team_offense_adjusted_q) > 0) {
  
  cat("🏠 Applying home court advantage to BACKTEST team offense profiles...\n")
  
  team_offense_adjusted_q <- team_offense_adjusted_q %>%
    left_join(
      games_backtest %>% select(game_id, home_team_id),
      by = "game_id"
    ) %>%
    mutate(
      is_home          = ESPN_TEAM_ID == home_team_id,
      fg2_pct_adjusted = ifelse(is_home, pmin(fg2_pct_adjusted * HCA$fg2_mult, 1.0), fg2_pct_adjusted),
      fg3_pct_adjusted = ifelse(is_home, pmin(fg3_pct_adjusted * HCA$fg3_mult, 1.0), fg3_pct_adjusted),
      ft_pct_adjusted  = ifelse(is_home, pmin(ft_pct_adjusted  * HCA$ft_mult,  1.0), ft_pct_adjusted),
      fta_per_fga_adjusted = ifelse(is_home, fta_per_fga_adjusted * HCA$fta_mult, fta_per_fga_adjusted)
    ) %>%
    select(-is_home, -home_team_id)
  
  message("✅ Home court advantage applied to BACKTEST team offense profiles")
}

# --------------------------------------------------
# Apply HCA to TODAY team offense profiles
# --------------------------------------------------
if (exists("team_offense_adjusted_q_today") && nrow(team_offense_adjusted_q_today) > 0) {
  
  cat("🏠 Applying home court advantage to TODAY team offense profiles...\n")
  
  team_offense_adjusted_q_today <- team_offense_adjusted_q_today %>%
    left_join(
      canon_today %>% select(game_id, home_team_id),
      by = "game_id"
    ) %>%
    mutate(
      is_home          = ESPN_TEAM_ID == home_team_id,
      fg2_pct_adjusted = ifelse(is_home, pmin(fg2_pct_adjusted * HCA$fg2_mult, 1.0), fg2_pct_adjusted),
      fg3_pct_adjusted = ifelse(is_home, pmin(fg3_pct_adjusted * HCA$fg3_mult, 1.0), fg3_pct_adjusted),
      ft_pct_adjusted  = ifelse(is_home, pmin(ft_pct_adjusted  * HCA$ft_mult,  1.0), ft_pct_adjusted),
      fta_per_fga_adjusted = ifelse(is_home, fta_per_fga_adjusted * HCA$fta_mult, fta_per_fga_adjusted)
    ) %>%
    select(-is_home, -home_team_id)
  
  message("✅ Home court advantage applied to TODAY team offense profiles")
}


# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠
# 🏠🏠🏠 END: Home Court Advantage Adjustments
# 🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠🏠



# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 START: Join Actual Player Stats into Player Backtested Data
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀

# --------------------------------------------------
# Join actual game results to backtest projections
# This enables post-simulation validation by comparing
# projected stats to actual outcomes
# --------------------------------------------------

if (exists("BaseStats_Player_MC") && 
    exists("player_projections_backtest") &&
    nrow(player_projections_backtest) > 0) {
  
  cat("📊 Joining actual player stats to backtest projections...\n")
  
  player_projections_backtest <- player_projections_backtest %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID)
    ) %>%
    left_join(
      BaseStats_Player_MC %>%
        transmute(
          GAME_DATE      = as.Date(GAME_DATE),
          ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
          ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
          
          # Minutes & core stats
          MINS_actual = as.numeric(MINS_CGS),
          PTS_actual  = as.numeric(PTS_CGS),
          AST_actual  = as.numeric(AST_CGS),
          TOV_actual  = as.numeric(TOV_CGS),
          BLK_actual  = as.numeric(BLK_CGS),
          STL_actual  = as.numeric(STL_CGS),
          
          # Rebounds (split)
          OREB_actual = as.numeric(OREB_CGS),
          DREB_actual = as.numeric(DREB_CGS),
          REB_actual  = as.numeric(REB_CGS),
          
          # Shooting (made/attempted)
          FG2M_actual = as.numeric(`2PTM_CGS`),
          FG2A_actual = as.numeric(`2PTA_CGS`),
          FG3M_actual = as.numeric(`3PTM_CGS`),
          FG3A_actual = as.numeric(`3PTA_CGS`),
          FTM_actual  = as.numeric(FTM_CGS),
          FTA_actual  = as.numeric(FTA_CGS)
        ),
      by = c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID")
    )
  
  message("✅ Actual stats joined to backtest projections")
}

# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀
# 🏀🏀🏀 END: Join Actual Player Stats into Player Backtested Data
# 🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀🏀



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#     ooooooooooooo                                            ooooooooo.                       .o88o.  o8o  oooo                     
#     8'   888   `8                                            `888   `Y88.                     888 `"  `"'  `888                     
#          888       .ooooo.   .oooo.   ooo. .oo.  .oo.         888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.   .oooo.o 
#          888      d88' `88b `P  )88b  `888P"Y88bP"Y88b        888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b d88(  "8 
#          888      888ooo888  .oP"888   888   888   888        888          888     888   888  888     888   888  888ooo888 `"Y88b.  
#          888      888    .o d8(  888   888   888   888        888          888     888   888  888     888   888  888    .o o.  )88b 
#         o888o     `Y8bod8P' `Y888""8o o888o o888o o888o      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P' 8""888P' 
#                                                                                                                                
#                                                                                                                                
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬




# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#     ooooooooo.                                                            o8o                                      ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     `888   `Y88.                                                          `"'                                      `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#      888   .d88'  .ooooo.   .oooo.o  .oooo.o  .ooooo.   .oooo.o  .oooo.o oooo   .ooooo.  ooo. .oo.    .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#      888ooo88P'  d88' `88b d88(  "8 d88(  "8 d88' `88b d88(  "8 d88(  "8 `888  d88' `88b `888P"Y88b  d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#      888         888   888 `"Y88b.  `"Y88b.  888ooo888 `"Y88b.  `"Y88b.   888  888   888  888   888  `"Y88b.        888          888     888   888  888     888   888  888ooo888      88        888        88 
#      888         888   888 o.  )88b o.  )88b 888    .o o.  )88b o.  )88b  888  888   888  888   888  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     o888o        `Y8bod8P' 8""888P' 8""888P' `Y8bod8P' 8""888P' 8""888P' o888o `Y8bod8P' o888o o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                                                                         `"               "'   
#                                                                                                                                                                                                         
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

# ==========================================================================================================
# TEAM PACE (POSSESSIONS) PROFILE
# ----------------------------------------------------------------------------------------------------------
# This section builds leakage-safe baseline possession (pace) distributions for teams using historical
# data. For each game date (TODAY and BACKTEST), a rolling window of recent games occurring strictly
# before that date is used to estimate how many possessions each team generates per quarter.
#
# Team-specific pace profiles are computed at the team level using a rolling window of recent games
# (default: last 10 games) and are quarter-aware to capture differences between regulation quarters
# (Q1-Q4) and overtime periods (Q5-Q6). Small samples are handled via league-average fallbacks, and
# variance is clamped to realistic bounds.
#
# Pace is the foundational layer of the MC simulation architecture: possessions determine volume
# (how many chances), while efficiency profiles determine conversion (how good are those chances).
# These profiles are deterministic parameters; all randomness is applied later in the MC engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 START: Team Pace (Possessions) Profile
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

stopifnot(
  exists("BaseStats_Team_MC"),
  exists("nba_schedule_season"),
  exists("as_of_date")
)

# --------------------------------------------------
# Helper Functions
# --------------------------------------------------
get_num_col <- function(df, col) {
  if (!col %in% names(df)) return(rep(NA_real_, nrow(df)))
  suppressWarnings(as.numeric(df[[col]]))
}

clamp_num <- function(x, lo, hi) {
  x <- suppressWarnings(as.numeric(x))
  pmin(pmax(x, lo), hi)
}

parse_game_date <- function(x) {
  d <- suppressWarnings(lubridate::ymd(x))
  if (all(is.na(d))) d <- suppressWarnings(lubridate::mdy(x))
  if (all(is.na(d))) d <- suppressWarnings(as.Date(x))
  d
}

# --------------------------------------------------
# Core Builder Function
# --------------------------------------------------
build_team_poss_profile_q <- function(BaseStats_Team_MC,
                                      slate_games,
                                      qtrs = 1:6,
                                      n_games_back = 10L,
                                      sd_shrink = 0.85) {
  
  # Validate inputs
  stopifnot("GAME_DATE" %in% names(BaseStats_Team_MC))
  stopifnot("ESPN_TEAM_ID" %in% names(BaseStats_Team_MC))
  stopifnot(all(c("ESPN_GAME_ID", "game_date", "ESPN_TEAM_ID") %in% names(slate_games)))
  
  # --- Normalize historical data ---
  hist <- BaseStats_Team_MC %>%
    mutate(
      ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
      game_date_dt = parse_game_date(GAME_DATE)
    ) %>%
    filter(!is.na(ESPN_TEAM_ID), ESPN_TEAM_ID != "", !is.na(game_date_dt)) %>%
    select(ESPN_TEAM_ID, game_date_dt, everything())
  
  # --- Normalize slate games ---
  slate <- as.data.frame(slate_games) %>%
    mutate(
      ESPN_GAME_ID = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
      game_date_dt = parse_game_date(game_date)
    ) %>%
    filter(
      !is.na(ESPN_GAME_ID), ESPN_GAME_ID != "",
      !is.na(ESPN_TEAM_ID), ESPN_TEAM_ID != "",
      !is.na(game_date_dt)
    )
  
  # --- Build long history by quarter ---
  long_list <- lapply(qtrs, function(q) {
    poss_col <- paste0("T_POSS_Q", q)
    pace_col <- paste0("T_PACE_Q", q)
    
    poss_raw <- get_num_col(hist, poss_col)
    pace_raw <- get_num_col(hist, pace_col)
    
    # Prefer T_POSS_Q*, fallback to T_PACE_Q*
    use_poss <- poss_raw
    if (all(is.na(use_poss))) use_poss <- pace_raw
    
    data.frame(
      ESPN_TEAM_ID = hist$ESPN_TEAM_ID,
      game_date_dt = hist$game_date_dt,
      qtr          = as.integer(q),
      poss_val     = suppressWarnings(as.numeric(use_poss)),
      stringsAsFactors = FALSE
    )
  })
  
  hist_long <- bind_rows(long_list) %>%
    filter(!is.na(ESPN_TEAM_ID), ESPN_TEAM_ID != "")
  
  hist_dt <- as.data.table(hist_long)
  setorder(hist_dt, ESPN_TEAM_ID, qtr, game_date_dt)
  
  # --- Expand slate to game/team/qtr grid ---
  grid <- as.data.table(slate)[, .(ESPN_GAME_ID, game_date_dt, ESPN_TEAM_ID)]
  grid <- unique(grid)
  grid <- grid[rep(seq_len(nrow(grid)), each = length(qtrs))]
  grid[, qtr := rep(as.integer(qtrs), times = nrow(unique(as.data.table(slate)[, .(ESPN_GAME_ID, ESPN_TEAM_ID, game_date_dt)])))]
  
  # --- Compute pace profile per (game, team, quarter) using PRIOR games only ---
  out <- grid[, {
    gdt <- game_date_dt[1]
    tid <- ESPN_TEAM_ID[1]
    qq  <- qtr[1]
    
    # Get historical possessions for this team/quarter BEFORE game date
    h <- hist_dt[ESPN_TEAM_ID == tid & qtr == qq & game_date_dt < gdt]
    v <- h$poss_val
    g <- h$game_date_dt
    
    # Remove missing
    keep <- !is.na(v) & v > 0
    v <- v[keep]
    g <- g[keep]
    
    if (length(v) == 0) {
      mu <- NA_real_
      sd <- NA_real_
      n_obs <- 0L
    } else {
      # Take last N games
      idx <- tail(seq_along(v), min(as.integer(n_games_back), length(v)))
      vN  <- v[idx]
      gN  <- g[idx]
      n_obs <- length(vN)
      
      # Recency weights
      days_ago   <- as.numeric(gdt - gN)
      rec_weight <- exp(-0.1 * days_ago)
      total_w    <- sum(rec_weight)
      
      # Weighted mean
      mu <- sum(vN * rec_weight) / total_w
      
      # Weighted SD
      sd <- if (n_obs >= 2) {
        sqrt(sum(rec_weight * (vN - mu)^2) / total_w)
      } else 0
    }
    
    # League average defaults (regulation vs. overtime)
    default_mu <- if (qq >= 5) 10 else 24
    default_sd <- if (qq >= 5) 2  else 3
    
    # Apply fallbacks for missing data
    mu <- ifelse(is.na(mu) | mu <= 0, default_mu, mu)
    sd <- ifelse(is.na(sd) | sd < 0,  default_sd, sd)
    
    # Small sample shrinkage
    if (n_obs > 0 && n_obs < 5) {
      mu <- 0.7 * mu + 0.3 * default_mu
      sd <- pmax(sd, default_sd * 0.8)
    }
    
    # Scale variance
    sd <- sd * sd_shrink
    
    # Clamp to realistic bounds
    mu <- clamp_num(mu, lo = if (qq >= 5) 4 else 12, hi = if (qq >= 5) 20 else 40)
    sd <- clamp_num(sd, lo = 0, hi = if (qq >= 5) 8 else 12)
    
    list(
      POSS_mean = as.numeric(mu),
      POSS_sd   = as.numeric(sd),
      n_games   = as.integer(n_obs)
    )
  }, by = .(ESPN_GAME_ID, game_date_dt, ESPN_TEAM_ID, qtr)]
  
  # Convert to tibble with clean column names
  out <- as_tibble(out) %>%
    mutate(
      ESPN_GAME_ID = as.character(ESPN_GAME_ID),
      GAME_DATE    = as.character(game_date_dt),
      ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
      qtr          = as.integer(qtr),
      POSS_mean    = as.numeric(POSS_mean),
      POSS_sd      = as.numeric(POSS_sd),
      n_games      = as.integer(n_games)
    ) %>%
    select(ESPN_GAME_ID, GAME_DATE, ESPN_TEAM_ID, qtr, POSS_mean, POSS_sd, n_games) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  out
}

# ============================================================
# A) TODAY: Build Team Pace Profile
# ============================================================
if (exists("nba_schedule_season") && nrow(nba_schedule_season) > 0) {
  
  # Filter to today's games only
  slate_games_today <- nba_schedule_season %>%
    filter(as.Date(game_date) == as.Date(as_of_date)) %>%
    transmute(
      ESPN_GAME_ID = as.character(game_id),
      game_date    = as.character(game_date),
      ESPN_TEAM_ID = as.character(team_id)
    ) %>%
    distinct()
  
  if (nrow(slate_games_today) > 0) {
    
    cat("📊 Building TODAY team pace profiles for", as.character(as_of_date), "...\n")
    
    team_poss_profile_q_today <- build_team_poss_profile_q(
      BaseStats_Team_MC = BaseStats_Team_MC,
      slate_games       = slate_games_today,
      qtrs              = 1:6,
      n_games_back      = 10L,
      sd_shrink         = 0.85
    )
    
    message(
      "✅ TODAY team pace profiles built | games=", nrow(slate_games_today) / 2,
      " | rows=", nrow(team_poss_profile_q_today)
    )
    
  } else {
    team_poss_profile_q_today <- tibble()
    message("⏭️  No games today - team_poss_profile_q_today is empty")
  }
  
} else {
  team_poss_profile_q_today <- tibble()
  warning("⚠️  nba_schedule_season missing - cannot build TODAY pace profiles")
}

# ============================================================
# B) BACKTEST: Build Team Pace Profile
# ============================================================
if (exists("nba_schedule_season") && 
    exists("load_backtest_data") && 
    load_backtest_data == TRUE) {
  
  # Filter to backtest games (before as_of_date)
  slate_games_backtest <- nba_schedule_season %>%
    filter(as.Date(game_date) < as.Date(as_of_date)) %>%
    transmute(
      ESPN_GAME_ID = as.character(game_id),
      game_date    = as.character(game_date),
      ESPN_TEAM_ID = as.character(team_id)
    ) %>%
    distinct()
  
  if (nrow(slate_games_backtest) > 0) {
    
    cat("📊 Building BACKTEST team pace profiles...\n")
    
    team_poss_profile_q_backtest <- build_team_poss_profile_q(
      BaseStats_Team_MC = BaseStats_Team_MC,
      slate_games       = slate_games_backtest,
      qtrs              = 1:6,
      n_games_back      = 10L,
      sd_shrink         = 0.85
    )
    
    message(
      "✅ BACKTEST team pace profiles built | games=", nrow(slate_games_backtest) / 2,
      " | rows=", nrow(team_poss_profile_q_backtest)
    )
    
  } else {
    team_poss_profile_q_backtest <- tibble()
    message("⏭️  No backtest games - team_poss_profile_q_backtest is empty")
  }
  
} else {
  team_poss_profile_q_backtest <- tibble()
  message("⏭️  Skipping BACKTEST team pace profiles (load_backtest_data = FALSE)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 END: Team Pace (Possessions) Profile
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#     oooooooooooo  o8o            oooo        .o8         .oooooo.                        oooo       ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     `888'     `8  `"'            `888       "888        d8P'  `Y8b                       `888       `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#      888         oooo   .ooooo.   888   .oooo888       888            .ooooo.   .oooo.    888        888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#      888oooo8    `888  d88' `88b  888  d88' `888       888           d88' `88b `P  )88b   888        888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#      888    "     888  888ooo888  888  888   888       888     ooooo 888   888  .oP"888   888        888          888     888   888  888     888   888  888ooo888      88        888        88 
#      888          888  888    .o  888  888   888       `88.    .88'  888   888 d8(  888   888        888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     o888o        o888o `Y8bod8P' o888o `Y8bod88P"       `Y8bood8P'   `Y8bod8P' `Y888""8o o888o      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                                                          `"               "'   
#                                                                                                                                                                                           
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬                                                                                                                                                                                          

# ==========================================================================================================
# TEAM SHOOTING PARAMETERS (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds quarter-level team shooting parameters by aggregating player shooting rates into
# a single team profile for each game and quarter. It produces team-level estimates for FG2%, FG3%,
# FT%, three-point attempt share, and free-throw rate per field-goal attempt.
#
# AGGREGATION APPROACH:
# All values are exposure-weighted using each player's expected total minutes per game (not quarter-
# specific minutes, to avoid zero-weight issues with bench rotations). Only players who are active
# and have valid shooting data contribute to the team profile. The team shooting profile is derived
# bottom-up from player rates to ensure it reflects the actual lineup composition, making it
# automatically responsive to injuries, trades, and rotation changes.
#
# WHY AGGREGATE FROM PLAYERS:
# Team box score averages assume stable rotations and fail to adjust cleanly for injuries or lineup
# changes. By weighting player shooting rates by projected minutes, team efficiency automatically
# adapts when key shooters are absent, limited, or replaced. This preserves a clean modeling hierarchy
# where players drive efficiency and teams inherit it deterministically. Home court advantage is
# already baked into player rates and automatically flows through to team aggregates.
#
# VARIANCE HANDLING:
# The output is deterministic, quarter-aware team shooting profile with no randomness applied. All
# stochastic shooting behavior is deferred to the Monte Carlo engine, which consumes these parameters
# as fixed inputs.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬                                                                                                                                                                                          
# 🧬🧬🧬 START: Team Shooting Parameters (Aggregated from Players)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬                                                                                                                                                                                          

stopifnot(
  exists("num0")
)

# --------------------------------------------------
# League Average Constants (Fallbacks)
# --------------------------------------------------
LEAGUE_AVG <- list(
  FG2_pct     = 0.52,   # League average 2PT%
  FG3_pct     = 0.36,   # League average 3PT%
  FT_pct      = 0.78,   # League average FT%
  p3          = 0.35,   # League average 3PT attempt rate
  FTA_per_FGA = 0.25    # League average FTA per FGA
)

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
aggregate_team_shooting <- function(player_shooting_profile_q,
                                    player_minutes_params,
                                    min_weight_threshold = 5) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr") 
        %in% names(player_shooting_profile_q)),
    all(c("FG2_pct", "FG3_pct", "FT_pct", "p3", "FTA_per_FGA") 
        %in% names(player_shooting_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID") 
        %in% names(player_minutes_params))
  )
  
  # Calculate total expected minutes per player (sum across all quarters)
  player_total_mins <- player_minutes_params %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      
      # Sum expected minutes across Q1-Q4 (ignore Q5-Q6 OT for weight calculation)
      total_mins = num0(mins_mean_Q1, 0) + 
        num0(mins_mean_Q2, 0) + 
        num0(mins_mean_Q3, 0) + 
        num0(mins_mean_Q4, 0)
    ) %>%
    select(GAME_DATE, ESPN_PLAYER_ID, ESPN_TEAM_ID, total_mins)
  
  # Join total minutes to shooting profile
  shooting_with_weight <- player_shooting_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID)
    ) %>%
    left_join(
      player_total_mins,
      by = c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID")
    ) %>%
    mutate(
      # Weight = total expected minutes (game-level, not quarter-specific)
      # Zero out players without valid shooting data
      has_shooting = !is.na(FG2_pct) | !is.na(FG3_pct) | !is.na(FT_pct),
      weight = ifelse(has_shooting, pmax(num0(total_mins, 0), 0), 0)
    )
  
  # Aggregate to team-quarter level
  team_shooting <- shooting_with_weight %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      total_weight = sum(weight, na.rm = TRUE),
      
      # Weighted means with league average fallbacks
      FG2_pct = case_when(
        total_weight >= min_weight_threshold ~ weighted.mean(FG2_pct, weight, na.rm = TRUE),
        TRUE ~ LEAGUE_AVG$FG2_pct
      ),
      
      FG3_pct = case_when(
        total_weight >= min_weight_threshold ~ weighted.mean(FG3_pct, weight, na.rm = TRUE),
        TRUE ~ LEAGUE_AVG$FG3_pct
      ),
      
      FT_pct = case_when(
        total_weight >= min_weight_threshold ~ weighted.mean(FT_pct, weight, na.rm = TRUE),
        TRUE ~ LEAGUE_AVG$FT_pct
      ),
      
      p3 = case_when(
        total_weight >= min_weight_threshold ~ weighted.mean(p3, weight, na.rm = TRUE),
        TRUE ~ LEAGUE_AVG$p3
      ),
      
      FTA_per_FGA = case_when(
        total_weight >= min_weight_threshold ~ weighted.mean(FTA_per_FGA, weight, na.rm = TRUE),
        TRUE ~ LEAGUE_AVG$FTA_per_FGA
      ),
      
      active_players = sum(weight > 0, na.rm = TRUE),
      used_fallback  = total_weight < min_weight_threshold,
      
      .groups = "drop"
    ) %>%
    select(-total_weight) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_shooting
}

# ============================================================
# A) TODAY: Build Team Shooting Parameters
# ============================================================
if (exists("player_shooting_profile_q_today") && 
    exists("player_minutes_params_today") &&
    nrow(player_shooting_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team shooting parameters...\n")
  
  team_shooting_params_q_today <- aggregate_team_shooting(
    player_shooting_profile_q = player_shooting_profile_q_today,
    player_minutes_params     = player_minutes_params_today,
    min_weight_threshold      = 5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_shooting_params_q_today$ESPN_GAME_ID)),
    all(!is.na(team_shooting_params_q_today$FG2_pct)),
    all(team_shooting_params_q_today$active_players >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_shooting_params_q_today$FG2_pct > 0.70, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high FG2% detected in TODAY team shooting")
  }
  if (any(team_shooting_params_q_today$FG3_pct > 0.50, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high FG3% detected in TODAY team shooting")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_shooting_params_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low minutes weight)"
    )
  }
  
  message(
    "✅ TODAY team shooting parameters built | rows=", 
    nrow(team_shooting_params_q_today)
  )
  
} else {
  team_shooting_params_q_today <- tibble()
  message("⏭️  Skipping TODAY team shooting parameters (no player data)")
}

# ============================================================
# B) BACKTEST: Build Team Shooting Parameters
# ============================================================
if (exists("player_shooting_profile_q") && 
    exists("player_minutes_params_backtest") &&
    nrow(player_shooting_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team shooting parameters...\n")
  
  team_shooting_params_q <- aggregate_team_shooting(
    player_shooting_profile_q = player_shooting_profile_q,
    player_minutes_params     = player_minutes_params_backtest,
    min_weight_threshold      = 5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_shooting_params_q$ESPN_GAME_ID)),
    all(!is.na(team_shooting_params_q$FG2_pct)),
    all(team_shooting_params_q$active_players >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_shooting_params_q$FG2_pct > 0.70, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high FG2% detected in BACKTEST team shooting")
  }
  if (any(team_shooting_params_q$FG3_pct > 0.50, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high FG3% detected in BACKTEST team shooting")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_shooting_params_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low minutes weight)"
    )
  }
  
  message(
    "✅ BACKTEST team shooting parameters built | rows=", 
    nrow(team_shooting_params_q)
  )
  
} else {
  team_shooting_params_q <- tibble()
  message("⏭️  Skipping BACKTEST team shooting parameters (no player data)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬                                                                                                                                                                                          
# 🧬🧬🧬 END: Team Shooting Parameters (Aggregated from Players)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬 



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬                                                                                                                                                                                          
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#     oooooooooo.             o8o                  .                 ooooooooo.                 .                              o ooooooooooooo o.   
#     `888   `Y88.            `"'                .o8                 `888   `Y88.             .o8                            .8' 8'   888   `8 `8.  
#      888   .d88'  .ooooo.  oooo  ooo. .oo.   .o888oo  .oooo.o       888   .d88'  .oooo.   .o888oo  .ooooo.   .oooo.o      .8'       888       `8. 
#      888ooo88P'  d88' `88b `888  `888P"Y88b    888   d88(  "8       888ooo88P'  `P  )88b    888   d88' `88b d88(  "8      88        888        88 
#      888         888   888  888   888   888    888   `"Y88b.        888`88b.     .oP"888    888   888ooo888 `"Y88b.       88        888        88 
#      888         888   888  888   888   888    888 . o.  )88b       888  `88b.  d8(  888    888 . 888    .o o.  )88b      `8.       888       .8' 
#     o888o        `Y8bod8P' o888o o888o o888o   "888" 8""888P'      o888o  o888o `Y888""8o   "888" `Y8bod8P' 8""888P'       `8.     o888o     .8'  
#                                                                                                                             `"               "'   
#                                                                                                                                              
#
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# ==========================================================================================================
# TEAM OFFENSIVE EFFICIENCY (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level offensive efficiency (points per possession) by aggregating player
# scoring rates into expected team output. The process follows a two-step conversion:
#
# STEP 1: INTENSITY (Volume)
# Calculate expected points per quarter by multiplying each player's scoring rate (pts/min) by their
# projected minutes in that quarter. This gives a time-based volume estimate that reflects lineup
# composition and is automatically injury-aware.
#
# STEP 2: EFFICIENCY (Rate)
# Convert the volume estimate into a possession-based rate by dividing expected points by expected
# possessions. This produces points-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY THIS APPROACH:
# Basketball scoring is fundamentally possession-based, not time-based. A team's ability to score
# depends on how many opportunities (possessions) they get, not just how much time they have. By
# calculating efficiency from player profiles, the metric automatically adjusts for injuries,
# lineup changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware offensive efficiency profile with no randomness.
# All stochastic behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 START: Team Offensive Efficiency (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constant (Fallback)
# --------------------------------------------------
LEAGUE_AVG_PTS_PER_POSS <- 1.10  # ~110 points per 100 possessions

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_offensive_efficiency <- function(player_ppm_profile_q,
                                            player_minutes_params,
                                            team_poss_profile_q,
                                            min_expected_pts_threshold = 5) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", "pts_per_min") 
        %in% names(player_ppm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (pts per quarter)
  # --------------------------------------------------
  player_intensity_long <- player_ppm_profile_q %>%
    mutate(
      GAME_DATE    = as.Date(GAME_DATE),
      ESPN_GAME_ID = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected points = pts/min × minutes in quarter
      player_expected_pts = pmax(0, num0(mins_q, 0) * num0(pts_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected pts per quarter)
  # --------------------------------------------------
  team_expected_pts_q <- player_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_pts   = sum(player_expected_pts, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Efficiency (pts per possession)
  # --------------------------------------------------
  team_offensive_efficiency_q <- team_expected_pts_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # Efficiency = expected points / expected possessions
      pts_per_poss = case_when(
        # If we have meaningful expected points and possessions
        expected_pts >= min_expected_pts_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_pts / POSS_mean,
        
        # Fallback to league average
        TRUE ~ LEAGUE_AVG_PTS_PER_POSS
      ),
      
      used_fallback = (expected_pts < min_expected_pts_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_pts, pts_per_poss, active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_offensive_efficiency_q
}

# ============================================================
# A) TODAY: Build Team Offensive Efficiency
# ============================================================
if (exists("player_ppm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_ppm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team offensive efficiency...\n")
  
  team_offensive_efficiency_q_today <- build_team_offensive_efficiency(
    player_ppm_profile_q  = player_ppm_profile_q_today,
    player_minutes_params = player_minutes_params_today,
    team_poss_profile_q   = team_poss_profile_q_today,
    min_expected_pts_threshold = 5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_offensive_efficiency_q_today$ESPN_GAME_ID)),
    all(!is.na(team_offensive_efficiency_q_today$pts_per_poss)),
    all(team_offensive_efficiency_q_today$pts_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_offensive_efficiency_q_today$pts_per_poss > 1.5, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high pts/poss (>1.5) detected in TODAY offensive efficiency")
  }
  if (any(team_offensive_efficiency_q_today$pts_per_poss < 0.7, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low pts/poss (<0.7) detected in TODAY offensive efficiency")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_offensive_efficiency_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected points)"
    )
  }
  
  message(
    "✅ TODAY team offensive efficiency built | rows=", 
    nrow(team_offensive_efficiency_q_today)
  )
  
} else {
  team_offensive_efficiency_q_today <- tibble()
  message("⏭️  Skipping TODAY team offensive efficiency (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Offensive Efficiency
# ============================================================
if (exists("player_ppm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_ppm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team offensive efficiency...\n")
  
  team_offensive_efficiency_q <- build_team_offensive_efficiency(
    player_ppm_profile_q  = player_ppm_profile_q,
    player_minutes_params = player_minutes_params_backtest,
    team_poss_profile_q   = team_poss_profile_q_backtest,
    min_expected_pts_threshold = 5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_offensive_efficiency_q$ESPN_GAME_ID)),
    all(!is.na(team_offensive_efficiency_q$pts_per_poss)),
    all(team_offensive_efficiency_q$pts_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_offensive_efficiency_q$pts_per_poss > 1.5, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high pts/poss (>1.5) detected in BACKTEST offensive efficiency")
  }
  if (any(team_offensive_efficiency_q$pts_per_poss < 0.7, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low pts/poss (<0.7) detected in BACKTEST offensive efficiency")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_offensive_efficiency_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected points)"
    )
  }
  
  message(
    "✅ BACKTEST team offensive efficiency built | rows=", 
    nrow(team_offensive_efficiency_q)
  )
  
} else {
  team_offensive_efficiency_q <- tibble()
  message("⏭️  Skipping BACKTEST team offensive efficiency (missing dependencies)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 END: Team Offensive Efficiency (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#     ooooooooo.              .o8                                               .o8       ooooooooo.                 .                             .o ooooooooooooo o.   
#     `888   `Y88.           "888                                              "888       `888   `Y88.             .o8                            .8' 8'   888   `8 `8.  
#      888   .d88'  .ooooo.   888oooo.   .ooooo.  oooo  oooo  ooo. .oo.    .oooo888        888   .d88'  .oooo.   .o888oo  .ooooo.   .oooo.o      .8'       888       `8. 
#      888ooo88P'  d88' `88b  d88' `88b d88' `88b `888  `888  `888P"Y88b  d88' `888        888ooo88P'  `P  )88b    888   d88' `88b d88(  "8      88        888        88 
#      888`88b.    888ooo888  888   888 888   888  888   888   888   888  888   888        888`88b.     .oP"888    888   888ooo888 `"Y88b.       88        888        88 
#      888  `88b.  888    .o  888   888 888   888  888   888   888   888  888   888        888  `88b.  d8(  888    888 . 888    .o o.  )88b      `8.       888       .8' 
#     o888o  o888o `Y8bod8P'  `Y8bod8P' `Y8bod8P'  `V88V"V8P' o888o o888o `Y8bod88P"      o888o  o888o `Y888""8o   "888" `Y8bod8P' 8""888P'       `8.     o888o     .8'  
#                                                                                                                                                  `"               "'   
#                                                                                                                                                         
#
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

# ==========================================================================================================
# TEAM REBOUNDING RATES (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level rebounding rates (rebounds per possession) by aggregating player
# rebounding rates into expected team output. Rebounding is split into offensive and defensive
# components to capture both second-chance creation (OREB) and defensive stop completion (DREB).
#
# STEP 1: INTENSITY (Volume)
# Calculate expected rebounds per quarter by multiplying each player's rebounding rate (reb/min) by
# their projected minutes in that quarter. This gives a time-based volume estimate that reflects
# lineup composition and is automatically injury-aware.
#
# STEP 2: RATE (Per Possession)
# Convert the volume estimate into a possession-based rate by dividing expected rebounds by expected
# possessions. This produces rebounds-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY POSSESSION-BASED:
# Rebounding opportunities are tied to missed shots, which are tied to possessions. A team's ability
# to rebound depends on how many shot attempts occur (possession-driven), not just clock time. By
# calculating rates from player profiles, the metric automatically adjusts for injuries, lineup
# changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware rebounding profile with no randomness. All stochastic
# behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 START: Team Rebounding Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constants (Fallbacks)
# --------------------------------------------------
LEAGUE_AVG_REB <- list(
  oreb_per_poss = 0.10,  # ~10% offensive rebound rate
  dreb_per_poss = 0.75,  # ~75% defensive rebound rate
  reb_per_poss  = 0.85   # Total rebounds per possession
)

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_rebounding_rates <- function(player_rpm_profile_q,
                                        player_minutes_params,
                                        team_poss_profile_q,
                                        min_expected_reb_threshold = 2) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", 
          "oreb_per_min", "dreb_per_min", "reb_per_min") 
        %in% names(player_rpm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (reb per quarter)
  # --------------------------------------------------
  player_reb_intensity_long <- player_rpm_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_GAME_ID   = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected rebounds = reb/min × minutes in quarter
      player_expected_oreb = pmax(0, num0(mins_q, 0) * num0(oreb_per_min, 0)),
      player_expected_dreb = pmax(0, num0(mins_q, 0) * num0(dreb_per_min, 0)),
      player_expected_reb  = pmax(0, num0(mins_q, 0) * num0(reb_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected reb per quarter)
  # --------------------------------------------------
  team_expected_reb_q <- player_reb_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_oreb  = sum(player_expected_oreb, na.rm = TRUE),
      expected_dreb  = sum(player_expected_dreb, na.rm = TRUE),
      expected_reb   = sum(player_expected_reb, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Rates (reb per possession)
  # --------------------------------------------------
  team_rebounding_rates_q <- team_expected_reb_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # OREB rate = expected offensive rebounds / expected possessions
      oreb_per_poss = case_when(
        expected_oreb >= min_expected_reb_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_oreb / POSS_mean,
        TRUE ~ LEAGUE_AVG_REB$oreb_per_poss
      ),
      
      # DREB rate = expected defensive rebounds / expected possessions
      dreb_per_poss = case_when(
        expected_dreb >= min_expected_reb_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_dreb / POSS_mean,
        TRUE ~ LEAGUE_AVG_REB$dreb_per_poss
      ),
      
      # Total REB rate = expected total rebounds / expected possessions
      reb_per_poss = case_when(
        expected_reb >= min_expected_reb_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_reb / POSS_mean,
        TRUE ~ LEAGUE_AVG_REB$reb_per_poss
      ),
      
      used_fallback = (expected_reb < min_expected_reb_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_oreb, expected_dreb, expected_reb,
           oreb_per_poss, dreb_per_poss, reb_per_poss,
           active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_rebounding_rates_q
}

# ============================================================
# A) TODAY: Build Team Rebounding Rates
# ============================================================
if (exists("player_rpm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_rpm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team rebounding rates...\n")
  
  team_rebounding_rates_q_today <- build_team_rebounding_rates(
    player_rpm_profile_q  = player_rpm_profile_q_today,
    player_minutes_params = player_minutes_params_today,
    team_poss_profile_q   = team_poss_profile_q_today,
    min_expected_reb_threshold = 2
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_rebounding_rates_q_today$ESPN_GAME_ID)),
    all(!is.na(team_rebounding_rates_q_today$reb_per_poss)),
    all(team_rebounding_rates_q_today$reb_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_rebounding_rates_q_today$oreb_per_poss > 0.30, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high OREB rate (>0.30) detected in TODAY rebounding")
  }
  if (any(team_rebounding_rates_q_today$dreb_per_poss > 1.0, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high DREB rate (>1.0) detected in TODAY rebounding")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_rebounding_rates_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected rebounds)"
    )
  }
  
  message(
    "✅ TODAY team rebounding rates built | rows=", 
    nrow(team_rebounding_rates_q_today)
  )
  
} else {
  team_rebounding_rates_q_today <- tibble()
  message("⏭️  Skipping TODAY team rebounding rates (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Rebounding Rates
# ============================================================
if (exists("player_rpm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_rpm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team rebounding rates...\n")
  
  team_rebounding_rates_q <- build_team_rebounding_rates(
    player_rpm_profile_q  = player_rpm_profile_q,
    player_minutes_params = player_minutes_params_backtest,
    team_poss_profile_q   = team_poss_profile_q_backtest,
    min_expected_reb_threshold = 2
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_rebounding_rates_q$ESPN_GAME_ID)),
    all(!is.na(team_rebounding_rates_q$reb_per_poss)),
    all(team_rebounding_rates_q$reb_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_rebounding_rates_q$oreb_per_poss > 0.30, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high OREB rate (>0.30) detected in BACKTEST rebounding")
  }
  if (any(team_rebounding_rates_q$dreb_per_poss > 1.0, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high DREB rate (>1.0) detected in BACKTEST rebounding")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_rebounding_rates_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected rebounds)"
    )
  }
  
  message(
    "✅ BACKTEST team rebounding rates built | rows=", 
    nrow(team_rebounding_rates_q)
  )
  
} else {
  team_rebounding_rates_q <- tibble()
  message("⏭️  Skipping BACKTEST team rebounding rates (missing dependencies)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 END: Team Rebounding Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#          .o.                          o8o               .                 ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#          .888.                         `"'             .o8                 `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#         .8"888.      .oooo.o  .oooo.o oooo   .oooo.o .o888oo  .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#        .8' `888.    d88(  "8 d88(  "8 `888  d88(  "8   888   d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#       .88ooo8888.   `"Y88b.  `"Y88b.   888  `"Y88b.    888   `"Y88b.        888          888     888   888  888     888   888  888ooo888      88        888        88 
#      .8'     `888.  o.  )88b o.  )88b  888  o.  )88b   888 . o.  )88b       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     o88o     o8888o 8""888P' 8""888P' o888o 8""888P'   "888" 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                                 `"               "'   
#                                                                                                                                                                  
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬


# ==========================================================================================================
# TEAM ASSIST RATES (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level assist rates (assists per possession) by aggregating player assist
# rates into expected team output. The process follows a two-step conversion:
#
# STEP 1: INTENSITY (Volume)
# Calculate expected assists per quarter by multiplying each player's assist rate (ast/min) by their
# projected minutes in that quarter. This gives a time-based volume estimate that reflects lineup
# composition and is automatically injury-aware.
#
# STEP 2: RATE (Per Possession)
# Convert the volume estimate into a possession-based rate by dividing expected assists by expected
# possessions. This produces assists-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY POSSESSION-BASED:
# Assist opportunities are tied to made field goals, which are tied to possessions. A team's ability
# to generate assists depends on shot attempts and success rate (possession-driven), not just clock
# time. By calculating rates from player profiles, the metric automatically adjusts for injuries,
# lineup changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware assist profile with no randomness. All stochastic
# behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🎯🎯🎯 START: Team Assist Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constant (Fallback)
# --------------------------------------------------
LEAGUE_AVG_AST_PER_POSS <- 0.60  # ~60% of made FGs are assisted league-wide

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_assist_rates <- function(player_apm_profile_q,
                                    player_minutes_params,
                                    team_poss_profile_q,
                                    min_expected_ast_threshold = 2) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", "ast_per_min") 
        %in% names(player_apm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (ast per quarter)
  # --------------------------------------------------
  player_ast_intensity_long <- player_apm_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_GAME_ID   = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected assists = ast/min × minutes in quarter
      player_expected_ast = pmax(0, num0(mins_q, 0) * num0(ast_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected ast per quarter)
  # --------------------------------------------------
  team_expected_ast_q <- player_ast_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_ast   = sum(player_expected_ast, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Rate (ast per possession)
  # --------------------------------------------------
  team_assist_rates_q <- team_expected_ast_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # Assist rate = expected assists / expected possessions
      ast_per_poss = case_when(
        expected_ast >= min_expected_ast_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_ast / POSS_mean,
        TRUE ~ LEAGUE_AVG_AST_PER_POSS
      ),
      
      used_fallback = (expected_ast < min_expected_ast_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_ast, ast_per_poss, active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_assist_rates_q
}

# ============================================================
# A) TODAY: Build Team Assist Rates
# ============================================================
if (exists("player_apm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_apm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team assist rates...\n")
  
  team_assist_rates_q_today <- build_team_assist_rates(
    player_apm_profile_q  = player_apm_profile_q_today,
    player_minutes_params = player_minutes_params_today,
    team_poss_profile_q   = team_poss_profile_q_today,
    min_expected_ast_threshold = 2
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_assist_rates_q_today$ESPN_GAME_ID)),
    all(!is.na(team_assist_rates_q_today$ast_per_poss)),
    all(team_assist_rates_q_today$ast_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_assist_rates_q_today$ast_per_poss > 0.90, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high assist rate (>0.90) detected in TODAY assists")
  }
  if (any(team_assist_rates_q_today$ast_per_poss < 0.30, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low assist rate (<0.30) detected in TODAY assists")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_assist_rates_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected assists)"
    )
  }
  
  message(
    "✅ TODAY team assist rates built | rows=", 
    nrow(team_assist_rates_q_today)
  )
  
} else {
  team_assist_rates_q_today <- tibble()
  message("⏭️  Skipping TODAY team assist rates (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Assist Rates
# ============================================================
if (exists("player_apm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_apm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team assist rates...\n")
  
  team_assist_rates_q <- build_team_assist_rates(
    player_apm_profile_q  = player_apm_profile_q,
    player_minutes_params = player_minutes_params_backtest,
    team_poss_profile_q   = team_poss_profile_q_backtest,
    min_expected_ast_threshold = 2
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_assist_rates_q$ESPN_GAME_ID)),
    all(!is.na(team_assist_rates_q$ast_per_poss)),
    all(team_assist_rates_q$ast_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_assist_rates_q$ast_per_poss > 0.90, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high assist rate (>0.90) detected in BACKTEST assists")
  }
  if (any(team_assist_rates_q$ast_per_poss < 0.30, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low assist rate (<0.30) detected in BACKTEST assists")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_assist_rates_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected assists)"
    )
  }
  
  message(
    "✅ BACKTEST team assist rates built | rows=", 
    nrow(team_assist_rates_q)
  )
  
} else {
  team_assist_rates_q <- tibble()
  message("⏭️  Skipping BACKTEST team assist rates (missing dependencies)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 END: Team Assist Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#
#     ooooooooooooo                                                                                         ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     8'   888   `8                                                                                         `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#          888      oooo  oooo  oooo d8b ooo. .oo.    .ooooo.  oooo    ooo  .ooooo.  oooo d8b  .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#          888      `888  `888  `888""8P `888P"Y88b  d88' `88b  `88.  .8'  d88' `88b `888""8P d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#          888       888   888   888      888   888  888   888   `88..8'   888ooo888  888     `"Y88b.        888          888     888   888  888     888   888  888ooo888      88        888        88 
#          888       888   888   888      888   888  888   888    `888'    888    .o  888     o.  )88b       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#         o888o      `V88V"V8P' d888b    o888o o888o `Y8bod8P'     `8'     `Y8bod8P' d888b    8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                                                                `"               "'   
#                                                                                                                                                                                   
#
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬 
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

# ==========================================================================================================
# TEAM TURNOVER RATES (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level turnover rates (turnovers per possession) by aggregating player
# turnover rates into expected team output. The process follows a two-step conversion:
#
# STEP 1: INTENSITY (Volume)
# Calculate expected turnovers per quarter by multiplying each player's turnover rate (tov/min) by
# their projected minutes in that quarter. This gives a time-based volume estimate that reflects
# lineup composition and is automatically injury-aware.
#
# STEP 2: RATE (Per Possession)
# Convert the volume estimate into a possession-based rate by dividing expected turnovers by expected
# possessions. This produces turnovers-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY POSSESSION-BASED:
# Turnover opportunities are tied to ball-handling possessions. A team's ability to protect the ball
# depends on the number of possessions they have (possession-driven), not just clock time. By
# calculating rates from player profiles, the metric automatically adjusts for injuries, lineup
# changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware turnover profile with no randomness. All stochastic
# behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 START: Team Turnover Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constant (Fallback)
# --------------------------------------------------
LEAGUE_AVG_TOV_PER_POSS <- 0.14  # ~14% turnover rate league-wide

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_turnover_rates <- function(player_tovpm_profile_q,
                                      player_minutes_params,
                                      team_poss_profile_q,
                                      min_expected_tov_threshold = 1) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", "tov_per_min") 
        %in% names(player_tovpm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (tov per quarter)
  # --------------------------------------------------
  player_tov_intensity_long <- player_tovpm_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_GAME_ID   = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected turnovers = tov/min × minutes in quarter
      player_expected_tov = pmax(0, num0(mins_q, 0) * num0(tov_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected tov per quarter)
  # --------------------------------------------------
  team_expected_tov_q <- player_tov_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_tov   = sum(player_expected_tov, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Rate (tov per possession)
  # --------------------------------------------------
  team_turnover_rates_q <- team_expected_tov_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # Turnover rate = expected turnovers / expected possessions
      tov_per_poss = case_when(
        expected_tov >= min_expected_tov_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_tov / POSS_mean,
        TRUE ~ LEAGUE_AVG_TOV_PER_POSS
      ),
      
      used_fallback = (expected_tov < min_expected_tov_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_tov, tov_per_poss, active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_turnover_rates_q
}

# ============================================================
# A) TODAY: Build Team Turnover Rates
# ============================================================
if (exists("player_tovpm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_tovpm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team turnover rates...\n")
  
  team_turnover_rates_q_today <- build_team_turnover_rates(
    player_tovpm_profile_q = player_tovpm_profile_q_today,
    player_minutes_params  = player_minutes_params_today,
    team_poss_profile_q    = team_poss_profile_q_today,
    min_expected_tov_threshold = 1
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_turnover_rates_q_today$ESPN_GAME_ID)),
    all(!is.na(team_turnover_rates_q_today$tov_per_poss)),
    all(team_turnover_rates_q_today$tov_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_turnover_rates_q_today$tov_per_poss > 0.25, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high turnover rate (>0.25) detected in TODAY turnovers")
  }
  if (any(team_turnover_rates_q_today$tov_per_poss < 0.08, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low turnover rate (<0.08) detected in TODAY turnovers")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_turnover_rates_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected turnovers)"
    )
  }
  
  message(
    "✅ TODAY team turnover rates built | rows=", 
    nrow(team_turnover_rates_q_today)
  )
  
} else {
  team_turnover_rates_q_today <- tibble()
  message("⏭️  Skipping TODAY team turnover rates (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Turnover Rates
# ============================================================
if (exists("player_tovpm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_tovpm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team turnover rates...\n")
  
  team_turnover_rates_q <- build_team_turnover_rates(
    player_tovpm_profile_q = player_tovpm_profile_q,
    player_minutes_params  = player_minutes_params_backtest,
    team_poss_profile_q    = team_poss_profile_q_backtest,
    min_expected_tov_threshold = 1
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_turnover_rates_q$ESPN_GAME_ID)),
    all(!is.na(team_turnover_rates_q$tov_per_poss)),
    all(team_turnover_rates_q$tov_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_turnover_rates_q$tov_per_poss > 0.25, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high turnover rate (>0.25) detected in BACKTEST turnovers")
  }
  if (any(team_turnover_rates_q$tov_per_poss < 0.08, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low turnover rate (<0.08) detected in BACKTEST turnovers")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_turnover_rates_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected turnovers)"
    )
  }
  
  message(
    "✅ BACKTEST team turnover rates built | rows=", 
    nrow(team_turnover_rates_q)
  )
  
} else {
  team_turnover_rates_q <- tibble()
  message("⏭️  Skipping BACKTEST team turnover rates (missing dependencies)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 END: Team Turnover Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#     oooooooooo.  oooo                      oooo                      ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     `888'   `Y8b `888                      `888                      `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#      888     888  888   .ooooo.   .ooooo.   888  oooo   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#      888oooo888'  888  d88' `88b d88' `"Y8  888 .8P'   d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#      888    `88b  888  888   888 888        888888.    `"Y88b.        888          888     888   888  888     888   888  888ooo888      88        888        88 
#      888    .88P  888  888   888 888   .o8  888 `88b.  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     o888bood8P'  o888o `Y8bod8P' `Y8bod8P' o888o o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                           `"               "'   
#                                                                                                                                                 
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

# ==========================================================================================================
# TEAM BLOCK RATES (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level block rates (blocks per possession) by aggregating player block
# rates into expected team output. The process follows a two-step conversion:
#
# STEP 1: INTENSITY (Volume)
# Calculate expected blocks per quarter by multiplying each player's block rate (blk/min) by their
# projected minutes in that quarter. This gives a time-based volume estimate that reflects lineup
# composition and is automatically injury-aware.
#
# STEP 2: RATE (Per Possession)
# Convert the volume estimate into a possession-based rate by dividing expected blocks by expected
# possessions. This produces blocks-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY POSSESSION-BASED:
# Block opportunities are tied to opponent shot attempts, which are tied to possessions. A team's
# ability to block shots depends on defensive opportunities (possession-driven), not just clock time.
# By calculating rates from player profiles, the metric automatically adjusts for injuries, lineup
# changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware block profile with no randomness. All stochastic
# behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬️ START: Team Block Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constant (Fallback)
# --------------------------------------------------
LEAGUE_AVG_BLK_PER_POSS <- 0.12  # ~12% of opponent shot attempts blocked

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_block_rates <- function(player_bpm_profile_q,
                                   player_minutes_params,
                                   team_poss_profile_q,
                                   min_expected_blk_threshold = 0.5) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", "blk_per_min") 
        %in% names(player_bpm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (blk per quarter)
  # --------------------------------------------------
  player_blk_intensity_long <- player_bpm_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_GAME_ID   = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected blocks = blk/min × minutes in quarter
      player_expected_blk = pmax(0, num0(mins_q, 0) * num0(blk_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected blk per quarter)
  # --------------------------------------------------
  team_expected_blk_q <- player_blk_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_blk   = sum(player_expected_blk, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Rate (blk per possession)
  # --------------------------------------------------
  team_block_rates_q <- team_expected_blk_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # Block rate = expected blocks / expected possessions
      blk_per_poss = case_when(
        expected_blk >= min_expected_blk_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_blk / POSS_mean,
        TRUE ~ LEAGUE_AVG_BLK_PER_POSS
      ),
      
      used_fallback = (expected_blk < min_expected_blk_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_blk, blk_per_poss, active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_block_rates_q
}

# ============================================================
# A) TODAY: Build Team Block Rates
# ============================================================
if (exists("player_bpm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_bpm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team block rates...\n")
  
  team_block_rates_q_today <- build_team_block_rates(
    player_bpm_profile_q  = player_bpm_profile_q_today,
    player_minutes_params = player_minutes_params_today,
    team_poss_profile_q   = team_poss_profile_q_today,
    min_expected_blk_threshold = 0.5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_block_rates_q_today$ESPN_GAME_ID)),
    all(!is.na(team_block_rates_q_today$blk_per_poss)),
    all(team_block_rates_q_today$blk_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_block_rates_q_today$blk_per_poss > 0.25, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high block rate (>0.25) detected in TODAY blocks")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_block_rates_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected blocks)"
    )
  }
  
  message(
    "✅ TODAY team block rates built | rows=", 
    nrow(team_block_rates_q_today)
  )
  
} else {
  team_block_rates_q_today <- tibble()
  message("⏭️  Skipping TODAY team block rates (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Block Rates
# ============================================================
if (exists("player_bpm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_bpm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team block rates...\n")
  
  team_block_rates_q <- build_team_block_rates(
    player_bpm_profile_q  = player_bpm_profile_q,
    player_minutes_params = player_minutes_params_backtest,
    team_poss_profile_q   = team_poss_profile_q_backtest,
    min_expected_blk_threshold = 0.5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_block_rates_q$ESPN_GAME_ID)),
    all(!is.na(team_block_rates_q$blk_per_poss)),
    all(team_block_rates_q$blk_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_block_rates_q$blk_per_poss > 0.25, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high block rate (>0.25) detected in BACKTEST blocks")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_block_rates_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected blocks)"
    )
  }
  
  message(
    "✅ BACKTEST team block rates built | rows=", 
    nrow(team_block_rates_q)
  )
  
} else {
  team_block_rates_q <- tibble()
  message("⏭️  Skipping BACKTEST team block rates (missing dependencies)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬️
# 🧬🧬🧬️ END: Team Block Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬️



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#
#      .oooooo..o     .                       oooo                ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     d8P'    `Y8   .o8                       `888                `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#     Y88bo.      .o888oo  .ooooo.   .oooo.    888   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#      `"Y8888o.    888   d88' `88b `P  )88b   888  d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#          `"Y88b   888   888ooo888  .oP"888   888  `"Y88b.        888          888     888   888  888     888   888  888ooo888      88        888        88 
#     oo     .d8P   888 . 888    .o d8(  888   888  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     8""88888P'    "888" `Y8bod8P' `Y888""8o o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                      `"               "'   
#
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# ==========================================================================================================
# TEAM STEAL RATES (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level steal rates (steals per possession) by aggregating player steal
# rates into expected team output. The process follows a two-step conversion:
#
# STEP 1: INTENSITY (Volume)
# Calculate expected steals per quarter by multiplying each player's steal rate (stl/min) by their
# projected minutes in that quarter. This gives a time-based volume estimate that reflects lineup
# composition and is automatically injury-aware.
#
# STEP 2: RATE (Per Possession)
# Convert the volume estimate into a possession-based rate by dividing expected steals by expected
# possessions. This produces steals-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY POSSESSION-BASED:
# Steal opportunities are tied to opponent possessions and ball-handling. A team's ability to
# generate steals depends on defensive opportunities (possession-driven), not just clock time. By
# calculating rates from player profiles, the metric automatically adjusts for injuries, lineup
# changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware steal profile with no randomness. All stochastic
# behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬️
# 🧬🧬🧬️ START: Team Steal Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constant (Fallback)
# --------------------------------------------------
LEAGUE_AVG_STL_PER_POSS <- 0.08  # ~8% steal rate league-wide

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_steal_rates <- function(player_spm_profile_q,
                                   player_minutes_params,
                                   team_poss_profile_q,
                                   min_expected_stl_threshold = 0.5) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", "stl_per_min") 
        %in% names(player_spm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (stl per quarter)
  # --------------------------------------------------
  player_stl_intensity_long <- player_spm_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_GAME_ID   = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected steals = stl/min × minutes in quarter
      player_expected_stl = pmax(0, num0(mins_q, 0) * num0(stl_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected stl per quarter)
  # --------------------------------------------------
  team_expected_stl_q <- player_stl_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_stl   = sum(player_expected_stl, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Rate (stl per possession)
  # --------------------------------------------------
  team_steal_rates_q <- team_expected_stl_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # Steal rate = expected steals / expected possessions
      stl_per_poss = case_when(
        expected_stl >= min_expected_stl_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_stl / POSS_mean,
        TRUE ~ LEAGUE_AVG_STL_PER_POSS
      ),
      
      used_fallback = (expected_stl < min_expected_stl_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_stl, stl_per_poss, active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_steal_rates_q
}

# ============================================================
# A) TODAY: Build Team Steal Rates
# ============================================================
if (exists("player_spm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_spm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team steal rates...\n")
  
  team_steal_rates_q_today <- build_team_steal_rates(
    player_spm_profile_q  = player_spm_profile_q_today,
    player_minutes_params = player_minutes_params_today,
    team_poss_profile_q   = team_poss_profile_q_today,
    min_expected_stl_threshold = 0.5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_steal_rates_q_today$ESPN_GAME_ID)),
    all(!is.na(team_steal_rates_q_today$stl_per_poss)),
    all(team_steal_rates_q_today$stl_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_steal_rates_q_today$stl_per_poss > 0.20, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high steal rate (>0.20) detected in TODAY steals")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_steal_rates_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected steals)"
    )
  }
  
  message(
    "✅ TODAY team steal rates built | rows=", 
    nrow(team_steal_rates_q_today)
  )
  
} else {
  team_steal_rates_q_today <- tibble()
  message("⏭️  Skipping TODAY team steal rates (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Steal Rates
# ============================================================
if (exists("player_spm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_spm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team steal rates...\n")
  
  team_steal_rates_q <- build_team_steal_rates(
    player_spm_profile_q  = player_spm_profile_q,
    player_minutes_params = player_minutes_params_backtest,
    team_poss_profile_q   = team_poss_profile_q_backtest,
    min_expected_stl_threshold = 0.5
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_steal_rates_q$ESPN_GAME_ID)),
    all(!is.na(team_steal_rates_q$stl_per_poss)),
    all(team_steal_rates_q$stl_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_steal_rates_q$stl_per_poss > 0.20, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high steal rate (>0.20) detected in BACKTEST steals")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_steal_rates_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected steals)"
    )
  }
  
  message(
    "✅ BACKTEST team steal rates built | rows=", 
    nrow(team_steal_rates_q)
  )
  
} else {
  team_steal_rates_q <- tibble()
  message("⏭️  Skipping BACKTEST team steal rates (missing dependencies)")
}

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬️ END: Team Steal Rates (From Player Profiles)
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬



# ️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
#
#
#
#     oooooooooooo                       oooo                ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     `888'     `8                       `888                `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#      888          .ooooo.  oooo  oooo   888   .oooo.o       888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#      888oooo8    d88' `88b `888  `888   888  d88(  "8       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#      888    "    888   888  888   888   888  `"Y88b.        888          888     888   888  888     888   888  888ooo888      88        888        88 
#      888         888   888  888   888   888  o.  )88b       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     o888o        `Y8bod8P'  `V88V"V8P' o888o 8""888P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                 `"               "'   
#                                                                                                                                                  
#                                                                                                                                                  
# ️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
# ==========================================================================================================
# TEAM FOUL RATES (AGGREGATED FROM PLAYER PROFILES)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level foul rates (fouls per possession) by aggregating player foul
# rates into expected team output. The process follows a two-step conversion:
#
# STEP 1: INTENSITY (Volume)
# Calculate expected fouls per quarter by multiplying each player's foul rate (fouls/min) by their
# projected minutes in that quarter. This gives a time-based volume estimate that reflects lineup
# composition and is automatically injury-aware.
#
# STEP 2: RATE (Per Possession)
# Convert the volume estimate into a possession-based rate by dividing expected fouls by expected
# possessions. This produces fouls-per-possession, which is pace-independent and can be scaled by
# actual game possessions in the Monte Carlo simulation.
#
# WHY POSSESSION-BASED:
# Foul opportunities are tied to defensive possessions and physicality. A team's tendency to foul
# depends on the number of defensive possessions (possession-driven), not just clock time. By
# calculating rates from player profiles, the metric automatically adjusts for injuries, lineup
# changes, and rotation patterns while remaining scalable to any game pace.
#
# The output is a deterministic, quarter-aware foul profile with no randomness. All stochastic
# behavior is deferred to the Monte Carlo engine.
# ==========================================================================================================

# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
# ⚠️⚠️⚠️ START: Team Foul Rates (From Player Profiles)
# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️

stopifnot(
  exists("num0")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constant (Fallback)
# --------------------------------------------------
LEAGUE_AVG_FOUL_PER_POSS <- 0.20  # ~20% foul rate per possession league-wide

# --------------------------------------------------
# Core Aggregation Function
# --------------------------------------------------
build_team_foul_rates <- function(player_fpm_profile_q,
                                  player_minutes_params,
                                  team_poss_profile_q,
                                  min_expected_foul_threshold = 1) {
  
  # Validate inputs
  stopifnot(
    all(c("GAME_DATE", "ESPN_GAME_ID", "ESPN_TEAM_ID", "ESPN_PLAYER_ID", "qtr", "fouls_per_min") 
        %in% names(player_fpm_profile_q)),
    all(c("GAME_DATE", "ESPN_PLAYER_ID", "ESPN_TEAM_ID", paste0("mins_mean_Q", 1:6)) 
        %in% names(player_minutes_params)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr", "POSS_mean") 
        %in% names(team_poss_profile_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Calculate Player Intensity (fouls per quarter)
  # --------------------------------------------------
  player_foul_intensity_long <- player_fpm_profile_q %>%
    mutate(
      GAME_DATE      = as.Date(GAME_DATE),
      ESPN_GAME_ID   = as.character(ESPN_GAME_ID),
      ESPN_TEAM_ID   = as.character(ESPN_TEAM_ID),
      ESPN_PLAYER_ID = as.character(ESPN_PLAYER_ID)
    ) %>%
    mutate(
      # Expected fouls = fouls/min × minutes in quarter
      player_expected_foul = pmax(0, num0(mins_q, 0) * num0(fouls_per_min, 0))
    )
  
  # --------------------------------------------------
  # STEP 2: Aggregate to Team Level (expected fouls per quarter)
  # --------------------------------------------------
  team_expected_foul_q <- player_foul_intensity_long %>%
    group_by(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr) %>%
    summarise(
      expected_foul  = sum(player_expected_foul, na.rm = TRUE),
      active_players = sum(mins_q > 0, na.rm = TRUE),
      .groups = "drop"
    )
  
  # --------------------------------------------------
  # STEP 3: Convert to Rate (fouls per possession)
  # --------------------------------------------------
  team_foul_rates_q <- team_expected_foul_q %>%
    left_join(
      team_poss_profile_q %>%
        select(ESPN_GAME_ID, ESPN_TEAM_ID, qtr, POSS_mean),
      by = c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr")
    ) %>%
    mutate(
      # Foul rate = expected fouls / expected possessions
      foul_per_poss = case_when(
        expected_foul >= min_expected_foul_threshold & num0(POSS_mean, 0) > 0 ~ 
          expected_foul / POSS_mean,
        TRUE ~ LEAGUE_AVG_FOUL_PER_POSS
      ),
      
      used_fallback = (expected_foul < min_expected_foul_threshold | 
                         num0(POSS_mean, 0) <= 0)
    ) %>%
    select(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr, 
           expected_foul, foul_per_poss, active_players, used_fallback) %>%
    arrange(GAME_DATE, ESPN_GAME_ID, ESPN_TEAM_ID, qtr)
  
  team_foul_rates_q
}

# ============================================================
# A) TODAY: Build Team Foul Rates
# ============================================================
if (exists("player_fpm_profile_q_today") && 
    exists("player_minutes_params_today") &&
    exists("team_poss_profile_q_today") &&
    nrow(player_fpm_profile_q_today) > 0) {
  
  cat("📊 Building TODAY team foul rates...\n")
  
  team_foul_rates_q_today <- build_team_foul_rates(
    player_fpm_profile_q  = player_fpm_profile_q_today,
    player_minutes_params = player_minutes_params_today,
    team_poss_profile_q   = team_poss_profile_q_today,
    min_expected_foul_threshold = 1
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_foul_rates_q_today$ESPN_GAME_ID)),
    all(!is.na(team_foul_rates_q_today$foul_per_poss)),
    all(team_foul_rates_q_today$foul_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_foul_rates_q_today$foul_per_poss > 0.35, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high foul rate (>0.35) detected in TODAY fouls")
  }
  if (any(team_foul_rates_q_today$foul_per_poss < 0.10, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low foul rate (<0.10) detected in TODAY fouls")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_foul_rates_q_today$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected fouls)"
    )
  }
  
  message(
    "✅ TODAY team foul rates built | rows=", 
    nrow(team_foul_rates_q_today)
  )
  
} else {
  team_foul_rates_q_today <- tibble()
  message("⏭️  Skipping TODAY team foul rates (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Foul Rates
# ============================================================
if (exists("player_fpm_profile_q") && 
    exists("player_minutes_params_backtest") &&
    exists("team_poss_profile_q_backtest") &&
    nrow(player_fpm_profile_q) > 0) {
  
  cat("📊 Building BACKTEST team foul rates...\n")
  
  team_foul_rates_q <- build_team_foul_rates(
    player_fpm_profile_q  = player_fpm_profile_q,
    player_minutes_params = player_minutes_params_backtest,
    team_poss_profile_q   = team_poss_profile_q_backtest,
    min_expected_foul_threshold = 1
  )
  
  # Validation
  stopifnot(
    all(!is.na(team_foul_rates_q$ESPN_GAME_ID)),
    all(!is.na(team_foul_rates_q$foul_per_poss)),
    all(team_foul_rates_q$foul_per_poss >= 0)
  )
  
  # Check for unrealistic values
  if (any(team_foul_rates_q$foul_per_poss > 0.35, na.rm = TRUE)) {
    warning("⚠️  Suspiciously high foul rate (>0.35) detected in BACKTEST fouls")
  }
  if (any(team_foul_rates_q$foul_per_poss < 0.10, na.rm = TRUE)) {
    warning("⚠️  Suspiciously low foul rate (<0.10) detected in BACKTEST fouls")
  }
  
  # Report fallback usage
  n_fallback <- sum(team_foul_rates_q$used_fallback)
  if (n_fallback > 0) {
    message(
      "⚠️  ", n_fallback, " team-quarter combinations used league average fallback ",
      "(low expected fouls)"
    )
  }
  
  message(
    "✅ BACKTEST team foul rates built | rows=", 
    nrow(team_foul_rates_q)
  )
  
} else {
  team_foul_rates_q <- tibble()
  message("⏭️  Skipping BACKTEST team foul rates (missing dependencies)")
}

# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️
# ⚠️⚠️⚠️ END: Team Foul Rates (From Player Profiles)
# ⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️⚠️



# 🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹
# START: CLEAN UP SECTION
# 🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹
# ==========================================================================================================
# ENVIRONMENT CLEANUP - REMOVE UNNECESSARY DATAFRAMES
# ----------------------------------------------------------------------------------------------------------
# This section removes intermediate dataframes and objects that are no longer needed for the Monte Carlo
# simulation engine. Keeping only the essential profile outputs and configuration objects reduces memory
# footprint and makes the environment easier to navigate.
#
# WHAT WE'RE KEEPING:
# - All player profiles (*_profile_q, *_profile_q_today)
# - All team profiles (team_*_q, team_*_q_today)
# - Player projections (player_projections_*, player_minutes_params_*)
# - Roster data (roster_*, injury_*)
# - Configuration objects (MC_VAR, LEAGUE_AVG, as_of_date, season_token, etc.)
# - Base data sources (BaseStats_Player_MC, BaseStats_Team_MC, nba_schedule_season)
#
# WHAT WE'RE REMOVING:
# - Intermediate aggregation dataframes
# - Cache objects
# - Temporary processing dataframes
# - Deprecated "intensity" and "profile" sections (replaced by rate sections)
# ==========================================================================================================

# 🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹
# 🧹🧹🧹 START: Environment Cleanup
# 🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹

cat("🧹 Cleaning up environment...\n")

# --------------------------------------------------
# Remove Deprecated Team Profile Sections
# (These were replaced by rate-based sections)
# --------------------------------------------------
rm_if_exists <- function(obj_name) {
  if (exists(obj_name, envir = .GlobalEnv)) {
    rm(list = obj_name, envir = .GlobalEnv)
    cat("  ✓ Removed:", obj_name, "\n")
  }
}

# Old team profiles (from BaseStats_Team_MC - now replaced)
rm_if_exists("team_points_profile_q")
rm_if_exists("team_rebounds_profile_q")
rm_if_exists("team_assists_profile_q")
rm_if_exists("team_steals_profile_q")
rm_if_exists("team_blocks_profile_q")
rm_if_exists("team_turnovers_profile_q")
rm_if_exists("team_fouls_profile_q")

# Old intensity sections (replaced by rate sections)
rm_if_exists("team_points_intensity_q")
rm_if_exists("team_reb_intensity_q")
rm_if_exists("team_ast_intensity_q")
rm_if_exists("team_steal_intensity_q")
rm_if_exists("team_blocks_intensity_q")
rm_if_exists("team_turnovers_intensity_q")
rm_if_exists("team_fouls_intensity_q")

# Intermediate player intensity dataframes
rm_if_exists("player_pts_intensity_base")
rm_if_exists("player_intensity_long")
rm_if_exists("player_reb_intensity_base")
rm_if_exists("player_reb_intensity_long")
rm_if_exists("player_ast_intensity_base")
rm_if_exists("player_ast_intensity_long")
rm_if_exists("player_stl_intensity_base")
rm_if_exists("player_steal_intensity_long")
rm_if_exists("player_blk_intensity_base")
rm_if_exists("player_block_intensity_long")
rm_if_exists("player_tov_intensity_base")
rm_if_exists("player_turnover_intensity_long")
rm_if_exists("player_foul_intensity_base")
rm_if_exists("player_foul_intensity_long")

# --------------------------------------------------
# Remove Intermediate Processing Dataframes
# --------------------------------------------------

# Slate games (no longer needed after profiles built)
rm_if_exists("slate_games")
rm_if_exists("slate_games_today")
rm_if_exists("slate_games_backtest")

# Roster conversion intermediates
rm_if_exists("roster_active_backtest")
rm_if_exists("roster_out_backtest")

# Cache objects
rm_if_exists("apm_cache")
rm_if_exists("bpm_cache")
rm_if_exists("spm_cache")
rm_if_exists("tovpm_cache")
rm_if_exists("fpm_cache")
rm_if_exists("rpm_cache")
rm_if_exists("ppm_cache")
rm_if_exists("shooting_cache")
rm_if_exists("usage_cache")

# --------------------------------------------------
# Remove Helper Function Copies
# --------------------------------------------------
# (These are defined in multiple sections - keep one global copy)
rm_if_exists("get_num_col")
rm_if_exists("parse_game_date")
rm_if_exists("clamp_num")

# --------------------------------------------------
# Remove Temporary Variables
# --------------------------------------------------
rm_if_exists("base")  # Used in old team profile sections
rm_if_exists("hist")  # Used in team pace profile
rm_if_exists("hist_long")
rm_if_exists("hist_dt")
rm_if_exists("grid")
rm_if_exists("quarters")
rm_if_exists("long_list")

# --------------------------------------------------
# Report Final State
# --------------------------------------------------
cat("\n✅ Environment cleanup complete!\n")

# Count remaining objects by category
all_objs <- ls(envir = .GlobalEnv)

player_profiles <- sum(grepl("^player_.*_profile", all_objs))
team_profiles <- sum(grepl("^team_.*_(rates|params|efficiency|shooting|rebounding|poss).*_q", all_objs))
rosters <- sum(grepl("^roster_", all_objs))
base_data <- sum(grepl("^BaseStats_", all_objs))
config <- sum(grepl("^(MC_VAR|LEAGUE_AVG|as_of_date|season_token|load_backtest)", all_objs))

cat("\n📊 Remaining objects:\n")
cat("  Player profiles:  ", player_profiles, "\n")
cat("  Team profiles:    ", team_profiles, "\n")
cat("  Roster data:      ", rosters, "\n")
cat("  Base data sources:", base_data, "\n")
cat("  Config objects:   ", config, "\n")
cat("  Total objects:    ", length(all_objs), "\n")

# 🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹
# 🧹🧹🧹 END: Environment Cleanup
# 🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹🧹



# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#
#     oooooooooo.              .o88o.                                               ooooooooo.                       .o88o.  o8o  oooo                   .o ooooooooooooo o.   
#     `888'   `Y8b             888 `"                                               `888   `Y88.                     888 `"  `"'  `888                  .8' 8'   888   `8 `8.  
#      888      888  .ooooo.  o888oo   .ooooo.  ooo. .oo.    .oooo.o  .ooooo.        888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.       .8'       888       `8. 
#      888      888 d88' `88b  888    d88' `88b `888P"Y88b  d88(  "8 d88' `88b       888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b      88        888        88 
#      888      888 888ooo888  888    888ooo888  888   888  `"Y88b.  888ooo888       888          888     888   888  888     888   888  888ooo888      88        888        88 
#      888     d88' 888    .o  888    888    .o  888   888  o.  )88b 888    .o       888          888     888   888  888     888   888  888    .o      `8.       888       .8' 
#     o888bood8P'   `Y8bod8P' o888o   `Y8bod8P' o888o o888o 8""888P' `Y8bod8P'      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P'       `8.     o888o     .8'  
#                                                                                                                                                        `"               "'   
#                                                                                                                                                                         
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠                                                                                                                                                                         
# ==========================================================================================================
# TEAM DEFENSIVE PROFILES (AGGREGATED FROM OPPONENT PERFORMANCE)
# ----------------------------------------------------------------------------------------------------------
# This section builds team-level defensive profiles by measuring what OPPONENTS do when playing against
# each team. Defensive quality is assessed by aggregating opponent offensive performance across games.
#
# PHILOSOPHY:
# A team's defense is measured by what opponents achieve against them. Elite defenses (Celtics, Heat)
# limit opponent scoring, shooting %, and efficiency. Poor defenses (Wizards, Pistons) allow high
# opponent performance. This is the "flip side" of offensive profiles - instead of measuring what a
# team DOES, we measure what they ALLOW.
#
# APPROACH:
# For each team, we identify all games where opponents played AGAINST them, then aggregate opponent
# offensive statistics (pts/poss, FG%, 3PT%, etc.). This creates a defensive profile showing what
# teams typically allow opponents to achieve.
#
# KEY METRICS:
# - Defensive Efficiency: Opponent points per possession allowed
# - Defensive Shooting: Opponent FG2%, FG3%, FT% allowed
# - Defensive Rebounding: Opponent offensive rebound rate allowed
# - Defensive Turnovers: Opponent turnover rate forced
#
# OUTPUT:
# Team-level defensive profiles (aggregated across games, quarter-aware) that can be used to adjust
# opponent offensive projections in the Monte Carlo simulation.
# ==========================================================================================================

# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠️
# 💠💠💠️ START: Team Defensive Profiles (From Opponent Performance)
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠

stopifnot(
  exists("num0"),
  exists("nba_games_canon_season")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Constants (Fallbacks)
# --------------------------------------------------
LEAGUE_AVG_DEF <- list(
  opp_pts_per_poss = 1.10,      # Opponent scoring allowed
  opp_fg2_pct = 0.52,            # Opponent 2PT% allowed
  opp_fg3_pct = 0.36,            # Opponent 3PT% allowed
  opp_ft_pct = 0.78,             # Opponent FT% allowed
  opp_oreb_rate = 0.10,          # Opponent OREB rate allowed
  opp_tov_rate = 0.14            # Opponent turnover rate forced
)

# --------------------------------------------------
# Helper: Build Defensive Profiles from Canon Games
# --------------------------------------------------
build_team_defensive_profiles <- function(nba_games_canon_season,
                                          team_offensive_efficiency_q,
                                          team_shooting_params_q,
                                          team_rebounding_rates_q,
                                          team_turnover_rates_q,
                                          min_games_threshold = 5) {
  
  # Validate inputs
  stopifnot(
    all(c("game_id", "game_date", "home_team_id", "away_team_id") %in% names(nba_games_canon_season)),
    all(c("ESPN_GAME_ID", "ESPN_TEAM_ID", "qtr") %in% names(team_offensive_efficiency_q))
  )
  
  # --------------------------------------------------
  # STEP 1: Create opponent pairs from canon games
  # --------------------------------------------------
  # Expand canon to create team/opponent perspective for both home and away
  home_perspective <- nba_games_canon_season %>%
    transmute(
      game_id = as.character(game_id),
      game_date = as.Date(game_date),
      team_id = as.character(home_team_id),
      opp_team_id = as.character(away_team_id)
    )
  
  away_perspective <- nba_games_canon_season %>%
    transmute(
      game_id = as.character(game_id),
      game_date = as.Date(game_date),
      team_id = as.character(away_team_id),
      opp_team_id = as.character(home_team_id)
    )
  
  opponent_pairs <- bind_rows(home_perspective, away_perspective) %>%
    filter(!is.na(team_id), team_id != "",
           !is.na(opp_team_id), opp_team_id != "") %>%
    distinct()
  
  # --------------------------------------------------
  # STEP 2: Get opponent offensive performance
  # --------------------------------------------------
  
  # DEFENSIVE EFFICIENCY (opponent pts per poss)
  opp_efficiency <- opponent_pairs %>%
    inner_join(
      team_offensive_efficiency_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          opp_team_id = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          opp_pts_per_poss = as.numeric(pts_per_poss)
        ),
      by = c("game_id", "opp_team_id"),
      relationship = "many-to-many"
    ) %>%
    distinct()
  
  # DEFENSIVE SHOOTING (opponent FG% allowed)
  opp_shooting <- opponent_pairs %>%
    inner_join(
      team_shooting_params_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          opp_team_id = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          opp_fg2_pct = as.numeric(FG2_pct),
          opp_fg3_pct = as.numeric(FG3_pct),
          opp_ft_pct = as.numeric(FT_pct),
          opp_p3 = as.numeric(p3)
        ),
      by = c("game_id", "opp_team_id"),
      relationship = "many-to-many"
    ) %>%
    distinct()
  
  # DEFENSIVE REBOUNDING (opponent OREB allowed)
  opp_rebounding <- opponent_pairs %>%
    inner_join(
      team_rebounding_rates_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          opp_team_id = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          opp_oreb_rate = as.numeric(oreb_per_poss)
        ),
      by = c("game_id", "opp_team_id"),
      relationship = "many-to-many"
    ) %>%
    distinct()
  
  # DEFENSIVE TURNOVERS (opponent TOV rate - higher is better defense)
  opp_turnovers <- opponent_pairs %>%
    inner_join(
      team_turnover_rates_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          opp_team_id = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          opp_tov_rate = as.numeric(tov_per_poss)
        ),
      by = c("game_id", "opp_team_id"),
      relationship = "many-to-many"
    ) %>%
    distinct()
  
  # --------------------------------------------------
  # STEP 3: Aggregate opponent performance by defending team
  # (Recency-weighted - recent games matter more)
  # --------------------------------------------------
  
  # Defensive Efficiency
  def_efficiency <- opp_efficiency %>%
    group_by(team_id, qtr) %>%
    mutate(
      days_ago   = as.numeric(max(game_date) - game_date),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    summarise(
      total_weight = sum(rec_weight, na.rm = TRUE),
      opp_pts_per_poss_allowed = ifelse(
        total_weight > 0,
        sum(opp_pts_per_poss * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_pts_per_poss, na.rm = TRUE)
      ),
      n_games_eff = n_distinct(game_id),
      .groups = "drop"
    ) %>%
    mutate(
      opp_pts_per_poss_allowed = case_when(
        n_games_eff >= min_games_threshold ~ opp_pts_per_poss_allowed,
        TRUE ~ LEAGUE_AVG_DEF$opp_pts_per_poss
      ),
      used_fallback_eff = n_games_eff < min_games_threshold
    ) %>%
    select(-total_weight)
  
  # Defensive Shooting
  def_shooting <- opp_shooting %>%
    group_by(team_id, qtr) %>%
    mutate(
      days_ago   = as.numeric(max(game_date) - game_date),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    summarise(
      total_weight = sum(rec_weight, na.rm = TRUE),
      opp_fg2_pct_allowed = ifelse(
        total_weight > 0,
        sum(opp_fg2_pct * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_fg2_pct, na.rm = TRUE)
      ),
      opp_fg3_pct_allowed = ifelse(
        total_weight > 0,
        sum(opp_fg3_pct * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_fg3_pct, na.rm = TRUE)
      ),
      opp_ft_pct_allowed = ifelse(
        total_weight > 0,
        sum(opp_ft_pct * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_ft_pct, na.rm = TRUE)
      ),
      opp_p3_allowed = ifelse(
        total_weight > 0,
        sum(opp_p3 * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_p3, na.rm = TRUE)
      ),
      n_games_shoot = n_distinct(game_id),
      .groups = "drop"
    ) %>%
    mutate(
      opp_fg2_pct_allowed = case_when(
        n_games_shoot >= min_games_threshold ~ opp_fg2_pct_allowed,
        TRUE ~ LEAGUE_AVG_DEF$opp_fg2_pct
      ),
      opp_fg3_pct_allowed = case_when(
        n_games_shoot >= min_games_threshold ~ opp_fg3_pct_allowed,
        TRUE ~ LEAGUE_AVG_DEF$opp_fg3_pct
      ),
      opp_ft_pct_allowed = case_when(
        n_games_shoot >= min_games_threshold ~ opp_ft_pct_allowed,
        TRUE ~ LEAGUE_AVG_DEF$opp_ft_pct
      ),
      used_fallback_shoot = n_games_shoot < min_games_threshold
    ) %>%
    select(-total_weight)
  
  # Defensive Rebounding
  def_rebounding <- opp_rebounding %>%
    group_by(team_id, qtr) %>%
    mutate(
      days_ago   = as.numeric(max(game_date) - game_date),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    summarise(
      total_weight = sum(rec_weight, na.rm = TRUE),
      opp_oreb_rate_allowed = ifelse(
        total_weight > 0,
        sum(opp_oreb_rate * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_oreb_rate, na.rm = TRUE)
      ),
      n_games_reb = n_distinct(game_id),
      .groups = "drop"
    ) %>%
    mutate(
      opp_oreb_rate_allowed = case_when(
        n_games_reb >= min_games_threshold ~ opp_oreb_rate_allowed,
        TRUE ~ LEAGUE_AVG_DEF$opp_oreb_rate
      ),
      used_fallback_reb = n_games_reb < min_games_threshold
    ) %>%
    select(-total_weight)
  
  # Defensive Turnovers
  def_turnovers <- opp_turnovers %>%
    group_by(team_id, qtr) %>%
    mutate(
      days_ago   = as.numeric(max(game_date) - game_date),
      rec_weight = exp(-0.1 * days_ago)
    ) %>%
    summarise(
      total_weight = sum(rec_weight, na.rm = TRUE),
      opp_tov_rate_forced = ifelse(
        total_weight > 0,
        sum(opp_tov_rate * rec_weight, na.rm = TRUE) / total_weight,
        mean(opp_tov_rate, na.rm = TRUE)
      ),
      n_games_tov = n_distinct(game_id),
      .groups = "drop"
    ) %>%
    mutate(
      opp_tov_rate_forced = case_when(
        n_games_tov >= min_games_threshold ~ opp_tov_rate_forced,
        TRUE ~ LEAGUE_AVG_DEF$opp_tov_rate
      ),
      used_fallback_tov = n_games_tov < min_games_threshold
    ) %>%
    select(-total_weight)
  
  # --------------------------------------------------
  # STEP 4: Combine all defensive metrics
  # --------------------------------------------------
  team_defensive_profiles <- def_efficiency %>%
    full_join(def_shooting, by = c("team_id", "qtr")) %>%
    full_join(def_rebounding, by = c("team_id", "qtr")) %>%
    full_join(def_turnovers, by = c("team_id", "qtr")) %>%
    mutate(
      ESPN_TEAM_ID = as.character(team_id),
      qtr = as.integer(qtr),
      
      # Overall fallback flag
      used_any_fallback = (used_fallback_eff | used_fallback_shoot | 
                             used_fallback_reb | used_fallback_tov)
    ) %>%
    select(
      ESPN_TEAM_ID, qtr,
      opp_pts_per_poss_allowed,
      opp_fg2_pct_allowed, opp_fg3_pct_allowed, opp_ft_pct_allowed, opp_p3_allowed,
      opp_oreb_rate_allowed,
      opp_tov_rate_forced,
      n_games_eff, n_games_shoot, n_games_reb, n_games_tov,
      used_any_fallback
    ) %>%
    arrange(ESPN_TEAM_ID, qtr)
  
  team_defensive_profiles
}

# ============================================================
# A) TODAY: Build Team Defensive Profiles
# ============================================================
if (exists("nba_games_canon_season") &&
    exists("team_offensive_efficiency_q_today") &&
    exists("team_shooting_params_q_today") &&
    exists("team_rebounding_rates_q_today") &&
    exists("team_turnover_rates_q_today")) {
  
  cat("📊 Building TODAY team defensive profiles...\n")
  
  # Filter canon to today's games
  canon_today <- nba_games_canon_season %>%
    filter(as.Date(game_date) == as.Date(as_of_date))
  
  if (nrow(canon_today) > 0) {
    
    team_defensive_profiles_q_today <- build_team_defensive_profiles(
      nba_games_canon_season = canon_today,
      team_offensive_efficiency_q = team_offensive_efficiency_q_today,
      team_shooting_params_q = team_shooting_params_q_today,
      team_rebounding_rates_q = team_rebounding_rates_q_today,
      team_turnover_rates_q = team_turnover_rates_q_today,
      min_games_threshold = 5
    )
    
    # Validation
    stopifnot(
      all(!is.na(team_defensive_profiles_q_today$ESPN_TEAM_ID)),
      all(!is.na(team_defensive_profiles_q_today$opp_pts_per_poss_allowed))
    )
    
    # Report fallback usage
    n_fallback <- sum(team_defensive_profiles_q_today$used_any_fallback, na.rm = TRUE)
    if (n_fallback > 0) {
      message("⚠️  ", n_fallback, " team-quarter combinations used league average fallbacks")
    }
    
    message(
      "✅ TODAY team defensive profiles built | rows=", 
      nrow(team_defensive_profiles_q_today)
    )
    
  } else {
    team_defensive_profiles_q_today <- tibble()
    message("⏭️  No games today - team_defensive_profiles_q_today is empty")
  }
  
} else {
  team_defensive_profiles_q_today <- tibble()
  message("⏭️  Skipping TODAY team defensive profiles (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Team Defensive Profiles
# ============================================================
if (exists("nba_games_canon_season") &&
    exists("team_offensive_efficiency_q") &&
    exists("team_shooting_params_q") &&
    exists("team_rebounding_rates_q") &&
    exists("team_turnover_rates_q")) {
  
  cat("📊 Building BACKTEST team defensive profiles...\n")
  
  # Filter canon to backtest period
  canon_backtest <- nba_games_canon_season %>%
    filter(as.Date(game_date) < as.Date(as_of_date))
  
  if (nrow(canon_backtest) > 0) {
    
    team_defensive_profiles_q <- build_team_defensive_profiles(
      nba_games_canon_season = canon_backtest,
      team_offensive_efficiency_q = team_offensive_efficiency_q,
      team_shooting_params_q = team_shooting_params_q,
      team_rebounding_rates_q = team_rebounding_rates_q,
      team_turnover_rates_q = team_turnover_rates_q,
      min_games_threshold = 5
    )
    
    # Validation
    stopifnot(
      all(!is.na(team_defensive_profiles_q$ESPN_TEAM_ID)),
      all(!is.na(team_defensive_profiles_q$opp_pts_per_poss_allowed))
    )
    
    # Report fallback usage
    n_fallback <- sum(team_defensive_profiles_q$used_any_fallback, na.rm = TRUE)
    if (n_fallback > 0) {
      message("⚠️  ", n_fallback, " team-quarter combinations used league average fallbacks")
    }
    
    message(
      "✅ BACKTEST team defensive profiles built | rows=", 
      nrow(team_defensive_profiles_q)
    )
    
  } else {
    team_defensive_profiles_q <- tibble()
    message("⏭️  No backtest data - team_defensive_profiles_q is empty")
  }
  
} else {
  team_defensive_profiles_q <- tibble()
  message("⏭️  Skipping BACKTEST team defensive profiles (missing dependencies)")
}

# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠️
# 💠💠💠️️ END: Team Defensive Profiles (From Opponent Performance)
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠 ️



# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
#
#
#       .oooooo.                                                                         .        88 oooooooooo.              .o88o.                                                     .o.             .o8      o8o                          .                   .o8       ooooooooo.                       .o88o.  o8o  oooo            
#      d8P'  `Y8b                                                                      .o8       .8' `888'   `Y8b             888 `"                                                    .888.           "888      `"'                        .o8                  "888       `888   `Y88.                     888 `"  `"'  `888            
#     888      888 oo.ooooo.  oo.ooooo.   .ooooo.  ooo. .oo.    .ooooo.  ooo. .oo.   .o888oo    .8'   888      888  .ooooo.  o888oo   .ooooo.  ooo. .oo.    .oooo.o  .ooooo.           .8"888.      .oooo888     oooo oooo  oooo   .oooo.o .o888oo  .ooooo.   .oooo888        888   .d88' oooo d8b  .ooooo.  o888oo  oooo   888   .ooooo.  
#     888      888  888' `88b  888' `88b d88' `88b `888P"Y88b  d88' `88b `888P"Y88b    888     .8'    888      888 d88' `88b  888    d88' `88b `888P"Y88b  d88(  "8 d88' `88b         .8' `888.    d88' `888     `888 `888  `888  d88(  "8   888   d88' `88b d88' `888        888ooo88P'  `888""8P d88' `88b  888    `888   888  d88' `88b 
#     888      888  888   888  888   888 888   888  888   888  888ooo888  888   888    888    .8'     888      888 888ooo888  888    888ooo888  888   888  `"Y88b.  888ooo888        .88ooo8888.   888   888      888  888   888  `"Y88b.    888   888ooo888 888   888        888          888     888   888  888     888   888  888ooo888 
#     `88b    d88'  888   888  888   888 888   888  888   888  888    .o  888   888    888 . .8'      888     d88' 888    .o  888    888    .o  888   888  o.  )88b 888    .o       .8'     `888.  888   888      888  888   888  o.  )88b   888 . 888    .o 888   888        888          888     888   888  888     888   888  888    .o 
#      `Y8bood8P'   888bod8P'  888bod8P' `Y8bod8P' o888o o888o `Y8bod8P' o888o o888o   "888" 88      o888bood8P'   `Y8bod8P' o888o   `Y8bod8P' o888o o888o 8""888P' `Y8bod8P'      o88o     o8888o `Y8bod88P"     888  `V88V"V8P' 8""888P'   "888" `Y8bod8P' `Y8bod88P"      o888o        d888b    `Y8bod8P' o888o   o888o o888o `Y8bod8P' 
#                   888        888                                                                                                                                                                                888                                                                                                                      
#                  o888o      o888o                                                                                                                                                                           .o. 88P                                                                                                                      
#                                                                                                                                                                                                        `Y888P                                                                                   
#
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# ==========================================================================================================
# OPPONENT-ADJUSTED OFFENSIVE PROFILES
# ----------------------------------------------------------------------------------------------------------
# This section creates matchup-specific adjusted offensive profiles by blending team offensive capabilities
# with opponent defensive quality. The adjustment uses a multiplicative factor based on how the opponent's
# defense compares to league average.
#
# ADJUSTMENT FORMULA:
# adjusted_stat = base_stat × (opponent_def_allowed / league_avg_def_allowed)
#
# EXAMPLE:
# Lakers base offensive efficiency: 1.12 pts/poss
# vs. Celtics (allow 1.05 pts/poss - elite defense): 1.12 × (1.05/1.10) = 1.07 pts/poss
# vs. Wizards (allow 1.15 pts/poss - poor defense): 1.12 × (1.15/1.10) = 1.17 pts/poss
#
# PHILOSOPHY:
# Elite defenses suppress opponent offense below their baseline. Poor defenses inflate opponent offense
# above their baseline. The adjustment is applied at the parameter level (shooting %, pts/poss, etc.)
# before simulation, not to final point totals.
#
# OUTPUT:
# Game-specific adjusted profiles that account for opponent defensive quality. These profiles are used
# in the Monte Carlo simulation to produce more realistic matchup-specific projections.
# ==========================================================================================================

# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠 START: Opponent-Adjusted Offensive Profiles
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠

suppressPackageStartupMessages({
  library(dplyr)
  library(tidyr)
})

stopifnot(
  exists("num0"),
  exists("nba_games_canon_season")
)

quarters <- 1:6

# --------------------------------------------------
# League Average Defensive Constants
# --------------------------------------------------
LEAGUE_AVG_DEF_ADJ <- list(
  pts_per_poss = 1.10,
  fg2_pct = 0.52,
  fg3_pct = 0.36,
  oreb_rate = 0.10,
  tov_rate = 0.14
)

# --------------------------------------------------
# Core Adjustment Function
# --------------------------------------------------
build_opponent_adjusted_profiles <- function(nba_games_canon_season,
                                             team_offensive_efficiency_q,
                                             team_shooting_params_q,
                                             team_rebounding_rates_q,
                                             team_turnover_rates_q,
                                             team_defensive_profiles_q,
                                             adjustment_strength = 0.5) {
  
  # Validate inputs
  stopifnot(
    all(c("game_id", "game_date", "home_team_id", "away_team_id") %in% names(nba_games_canon_season)),
    nrow(team_offensive_efficiency_q) > 0,
    nrow(team_defensive_profiles_q) > 0
  )
  
  # --------------------------------------------------
  # STEP 1: Create team/opponent pairs from canon
  # --------------------------------------------------
  home_perspective <- nba_games_canon_season %>%
    transmute(
      game_id = as.character(game_id),
      game_date = as.Date(game_date),
      ESPN_TEAM_ID = as.character(home_team_id),
      OPP_ESPN_TEAM_ID = as.character(away_team_id)
    )
  
  away_perspective <- nba_games_canon_season %>%
    transmute(
      game_id = as.character(game_id),
      game_date = as.Date(game_date),
      ESPN_TEAM_ID = as.character(away_team_id),
      OPP_ESPN_TEAM_ID = as.character(home_team_id)
    )
  
  matchups <- bind_rows(home_perspective, away_perspective) %>%
    filter(!is.na(ESPN_TEAM_ID), ESPN_TEAM_ID != "",
           !is.na(OPP_ESPN_TEAM_ID), OPP_ESPN_TEAM_ID != "") %>%
    distinct()
  
  # --------------------------------------------------
  # STEP 2: Expand to quarters
  # --------------------------------------------------
  matchups_quarters <- matchups %>%
    crossing(qtr = quarters)
  
  # --------------------------------------------------
  # STEP 3: Join offensive profiles
  # --------------------------------------------------
  offense_base <- matchups_quarters %>%
    left_join(
      team_offensive_efficiency_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          pts_per_poss_base = as.numeric(pts_per_poss)
        ),
      by = c("game_id", "ESPN_TEAM_ID", "qtr")
    ) %>%
    left_join(
      team_shooting_params_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          fg2_pct_base = as.numeric(FG2_pct),
          fg3_pct_base = as.numeric(FG3_pct),
          ft_pct_base = as.numeric(FT_pct),
          p3_base = as.numeric(p3),
          fta_per_fga_base = as.numeric(FTA_per_FGA)
        ),
      by = c("game_id", "ESPN_TEAM_ID", "qtr")
    ) %>%
    left_join(
      team_rebounding_rates_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          oreb_per_poss_base = as.numeric(oreb_per_poss)
        ),
      by = c("game_id", "ESPN_TEAM_ID", "qtr")
    ) %>%
    left_join(
      team_turnover_rates_q %>%
        transmute(
          game_id = as.character(ESPN_GAME_ID),
          ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          tov_per_poss_base = as.numeric(tov_per_poss)
        ),
      by = c("game_id", "ESPN_TEAM_ID", "qtr")
    )
  
  # --------------------------------------------------
  # STEP 4: Join opponent defensive profiles
  # --------------------------------------------------
  offense_with_opp_def <- offense_base %>%
    left_join(
      team_defensive_profiles_q %>%
        transmute(
          OPP_ESPN_TEAM_ID = as.character(ESPN_TEAM_ID),
          qtr = as.integer(qtr),
          opp_def_pts_allowed = as.numeric(opp_pts_per_poss_allowed),
          opp_def_fg2_allowed = as.numeric(opp_fg2_pct_allowed),
          opp_def_fg3_allowed = as.numeric(opp_fg3_pct_allowed),
          opp_def_oreb_allowed = as.numeric(opp_oreb_rate_allowed),
          opp_def_tov_forced = as.numeric(opp_tov_rate_forced)
        ),
      by = c("OPP_ESPN_TEAM_ID", "qtr")
    )
  
  # --------------------------------------------------
  # STEP 5: Calculate adjustment factors
  # --------------------------------------------------
  adjusted_profiles <- offense_with_opp_def %>%
    mutate(
      # Adjustment factors (opponent def / league avg)
      adj_factor_pts = (num0(opp_def_pts_allowed, LEAGUE_AVG_DEF_ADJ$pts_per_poss) / 
                          LEAGUE_AVG_DEF_ADJ$pts_per_poss),
      adj_factor_fg2 = (num0(opp_def_fg2_allowed, LEAGUE_AVG_DEF_ADJ$fg2_pct) / 
                          LEAGUE_AVG_DEF_ADJ$fg2_pct),
      adj_factor_fg3 = (num0(opp_def_fg3_allowed, LEAGUE_AVG_DEF_ADJ$fg3_pct) / 
                          LEAGUE_AVG_DEF_ADJ$fg3_pct),
      adj_factor_oreb = (num0(opp_def_oreb_allowed, LEAGUE_AVG_DEF_ADJ$oreb_rate) / 
                           LEAGUE_AVG_DEF_ADJ$oreb_rate),
      adj_factor_tov = (num0(opp_def_tov_forced, LEAGUE_AVG_DEF_ADJ$tov_rate) / 
                          LEAGUE_AVG_DEF_ADJ$tov_rate),
      
      # Apply adjustment strength parameter (1.0 = full adjustment, 0.5 = half, etc.)
      adj_factor_pts = 1 + (adj_factor_pts - 1) * adjustment_strength,
      adj_factor_fg2 = 1 + (adj_factor_fg2 - 1) * adjustment_strength,
      adj_factor_fg3 = 1 + (adj_factor_fg3 - 1) * adjustment_strength,
      adj_factor_oreb = 1 + (adj_factor_oreb - 1) * adjustment_strength,
      adj_factor_tov = 1 + (adj_factor_tov - 1) * adjustment_strength,
      
      # Apply adjustments
      pts_per_poss_adjusted = pts_per_poss_base * adj_factor_pts,
      fg2_pct_adjusted = pmin(fg2_pct_base * adj_factor_fg2, 1.0),  # Cap at 100%
      fg3_pct_adjusted = pmin(fg3_pct_base * adj_factor_fg3, 1.0),
      oreb_per_poss_adjusted = oreb_per_poss_base * adj_factor_oreb,
      tov_per_poss_adjusted = tov_per_poss_base * adj_factor_tov,
      
      # FT% typically not adjusted by opponent (refs, not defense)
      ft_pct_adjusted = ft_pct_base,
      
      # Other stats pass through
      p3_adjusted = p3_base,
      fta_per_fga_adjusted = fta_per_fga_base
    ) %>%
    select(
      game_id, game_date, ESPN_TEAM_ID, OPP_ESPN_TEAM_ID, qtr,
      
      # Base values
      pts_per_poss_base, fg2_pct_base, fg3_pct_base, ft_pct_base, 
      oreb_per_poss_base, tov_per_poss_base, p3_base, fta_per_fga_base,
      
      # Adjustment factors
      adj_factor_pts, adj_factor_fg2, adj_factor_fg3, adj_factor_oreb, adj_factor_tov,
      
      # Adjusted values
      pts_per_poss_adjusted, fg2_pct_adjusted, fg3_pct_adjusted, ft_pct_adjusted,
      oreb_per_poss_adjusted, tov_per_poss_adjusted, p3_adjusted, fta_per_fga_adjusted
    ) %>%
    arrange(game_date, game_id, ESPN_TEAM_ID, qtr)
  
  adjusted_profiles
}

# ============================================================
# A) TODAY: Build Opponent-Adjusted Profiles
# ============================================================
if (exists("nba_games_canon_season") &&
    exists("team_offensive_efficiency_q_today") &&
    exists("team_shooting_params_q_today") &&
    exists("team_rebounding_rates_q_today") &&
    exists("team_turnover_rates_q_today") &&
    exists("team_defensive_profiles_q_today")) {
  
  cat("📊 Building TODAY opponent-adjusted offensive profiles...\n")
  
  # Filter canon to today
  canon_today <- nba_games_canon_season %>%
    filter(as.Date(game_date) == as.Date(as_of_date))
  
  if (nrow(canon_today) > 0) {
    
    team_offense_adjusted_q_today <- build_opponent_adjusted_profiles(
      nba_games_canon_season = canon_today,
      team_offensive_efficiency_q = team_offensive_efficiency_q_today,
      team_shooting_params_q = team_shooting_params_q_today,
      team_rebounding_rates_q = team_rebounding_rates_q_today,
      team_turnover_rates_q = team_turnover_rates_q_today,
      team_defensive_profiles_q = team_defensive_profiles_q_today,
      adjustment_strength = 1.0  # Full adjustment (can be tuned 0.0 to 1.0)
    )
    
    # Validation
    stopifnot(
      all(!is.na(team_offense_adjusted_q_today$game_id)),
      all(!is.na(team_offense_adjusted_q_today$ESPN_TEAM_ID))
    )
    
    message(
      "✅ TODAY opponent-adjusted profiles built | rows=", 
      nrow(team_offense_adjusted_q_today)
    )
    
  } else {
    team_offense_adjusted_q_today <- tibble()
    message("⏭️  No games today - team_offense_adjusted_q_today is empty")
  }
  
} else {
  team_offense_adjusted_q_today <- tibble()
  message("⏭️  Skipping TODAY opponent-adjusted profiles (missing dependencies)")
}

# ============================================================
# B) BACKTEST: Build Opponent-Adjusted Profiles
# ============================================================
if (exists("nba_games_canon_season") &&
    exists("team_offensive_efficiency_q") &&
    exists("team_shooting_params_q") &&
    exists("team_rebounding_rates_q") &&
    exists("team_turnover_rates_q") &&
    exists("team_defensive_profiles_q")) {
  
  cat("📊 Building BACKTEST opponent-adjusted offensive profiles...\n")
  
  # Filter canon to backtest period
  canon_backtest <- nba_games_canon_season %>%
    filter(as.Date(game_date) < as.Date(as_of_date))
  
  if (nrow(canon_backtest) > 0) {
    
    team_offense_adjusted_q <- build_opponent_adjusted_profiles(
      nba_games_canon_season = canon_backtest,
      team_offensive_efficiency_q = team_offensive_efficiency_q,
      team_shooting_params_q = team_shooting_params_q,
      team_rebounding_rates_q = team_rebounding_rates_q,
      team_turnover_rates_q = team_turnover_rates_q,
      team_defensive_profiles_q = team_defensive_profiles_q,
      adjustment_strength = 1.0
    )
    
    # Validation
    stopifnot(
      all(!is.na(team_offense_adjusted_q$game_id)),
      all(!is.na(team_offense_adjusted_q$ESPN_TEAM_ID))
    )
    
    message(
      "✅ BACKTEST opponent-adjusted profiles built | rows=", 
      nrow(team_offense_adjusted_q)
    )
    
  } else {
    team_offense_adjusted_q <- tibble()
    message("⏭️  No backtest data - team_offense_adjusted_q is empty")
  }
  
} else {
  team_offense_adjusted_q <- tibble()
  message("⏭️  Skipping BACKTEST opponent-adjusted profiles (missing dependencies)")
}

# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠
# 💠💠💠END: Opponent-Adjusted Offensive Profiles
# 💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠💠



# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
#
#
#     ooo        ooooo                           .                    .oooooo.                      oooo                  .oooooo..o               .                          
#     `88.       .888'                         .o8                   d8P'  `Y8b                     `888                 d8P'    `Y8             .o8                          
#      888b     d'888   .ooooo.  ooo. .oo.   .o888oo  .ooooo.       888           .oooo.   oooo d8b  888   .ooooo.       Y88bo.       .ooooo.  .o888oo oooo  oooo  oo.ooooo.  
#      8 Y88. .P  888  d88' `88b `888P"Y88b    888   d88' `88b      888          `P  )88b  `888""8P  888  d88' `88b       `"Y8888o.  d88' `88b   888   `888  `888   888' `88b 
#      8  `888'   888  888   888  888   888    888   888ooo888      888           .oP"888   888      888  888   888           `"Y88b 888ooo888   888    888   888   888   888 
#      8    Y     888  888   888  888   888    888 . 888    .o      `88b    ooo  d8(  888   888      888  888   888      oo     .d8P 888    .o   888 .  888   888   888   888 
#     o8o        o888o `Y8bod8P' o888o o888o   "888" `Y8bod8P'       `Y8bood8P'  `Y888""8o d888b    o888o `Y8bod8P'      8""88888P'  `Y8bod8P'   "888"  `V88V"V8P'  888bod8P' 
#                                                                                                                                                                   888       
#                                                                                                                                                                 o888o      
#                                                                                                                                                                        
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# ==========================================================================================================
# MONTE CARLO SIMULATION ENGINE - TEAM-LEVEL GAME PROJECTIONS
# ----------------------------------------------------------------------------------------------------------
# This section implements a parametric Monte Carlo simulation for NBA game outcomes. The engine uses
# team-level rate profiles (pace, offensive efficiency, shooting, rebounding, turnovers, etc.) to
# simulate game scores through repeated random sampling.
#
# ARCHITECTURE:
# The simulation is RATE-BASED (parametric), not event-based. Team offensive efficiency (pts/poss) is
# used directly rather than simulating individual possessions. This approach is:
# - Faster (no possession-by-possession loops)
# - Simpler (fewer moving parts)
# - Standard (used by professional models like 538, BPI)
#
# SIMULATION FLOW:
# 1. Draw possessions for each team/quarter from pace profiles
# 2. Apply possession adjustments (turnovers reduce scoring possessions)
# 3. Apply bonus possessions (offensive rebounds create extra opportunities)
# 4. Calculate expected points (effective_poss × pts_per_poss_adjusted)
# 5. Add variance (possession-based stochastic noise)
# 6. Apply free throw bonus (fouling creates extra points)
# 7. Sum across quarters to get game total
# 8. Repeat 10,000 times to build outcome distribution
#
# GAME REALISM FEATURES:
# - Blowout drag (Q4 possessions reduced in lopsided games)
# - Intentional fouling (trailing team fouls to extend game)
# - Overtime handling (if scores tied after regulation)
# - Quarter-specific variance (different uncertainty per period)
#
# KEY FUNCTIONS:
# - simulate_quarter_offense() - Simulates one team's offense for one quarter
# - simulate_game_once() - Simulates one complete game (both teams, all quarters)
# - run_monte_carlo() - Runs N simulations and aggregates results
# ==========================================================================================================

# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲 START: Monte Carlo Simulation Engine
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲

suppressPackageStartupMessages({
  library(dplyr)
  library(tibble)
  library(purrr)
})

stopifnot(
  exists("num0")
)

# --------------------------------------------------
# MC Configuration Parameters
# --------------------------------------------------
if (!exists("MC_CONFIG")) {
  MC_CONFIG <- list(
    # Variance parameters
    poss_variance_scale = 1.5,        # Scale factor for possession variance (1.0 = use profile sd as-is)
    pts_per_poss_variance = 0.15,     # Base variance in pts/poss (±0.08 pts per possession)
    
    # Bonus/adjustment strengths
    oreb_bonus_pct = 0.70,            # % of OREBs that create extra possessions
    tov_reduction_pct = 1.00,         # % of turnovers that eliminate scoring possessions
    foul_bonus_leverage = 1.00,       # Leverage for free throw bonus (1.0 = full effect)
    
    # Blowout behavior
    blowout_threshold = 18,           # Point margin triggering blowout drag
    blowout_poss_reduction = 0.15,    # % reduction in Q4 possessions during blowout
    
    # Endgame fouling
    intentional_foul_margin = 6,      # Max margin for intentional fouling
    intentional_foul_max_trips = 6,   # Max foul trips in close Q4
    intentional_foul_strength = 1.0,  # Probability multiplier for fouling behavior
    
    # Overtime
    ot_poss_mean = 10,                # Mean possessions per OT period
    ot_poss_sd = 2,                   # SD of possessions per OT
    max_ot_periods = 2                # Max OT periods to simulate
  )
}

# --------------------------------------------------
# Helper Functions
# --------------------------------------------------

# Clamp value between 0 and 1
clamp01 <- function(x) pmin(pmax(x, 0), 1)

# Draw integer from normal distribution (for attempts/possessions)
draw_attempts <- function(mu, sd = 0) {
  if (is.na(mu) || mu <= 0) return(0L)
  sd <- max(0, num0(sd, 0))
  as.integer(pmax(0, round(rnorm(1, mean = mu, sd = sd))))
}

# --------------------------------------------------
# Possession Drawing
# --------------------------------------------------
draw_team_possessions_q <- function(team_id, qtr, game_id, 
                                    team_poss_profile_q,
                                    variance_scale = MC_CONFIG$poss_variance_scale) {
  
  # Look up possession parameters for this team/game/quarter
  poss_params <- team_poss_profile_q %>%
    filter(
      ESPN_TEAM_ID == !!team_id,
      ESPN_GAME_ID == !!game_id,
      qtr == !!qtr
    ) %>%
    slice(1)
  
  if (nrow(poss_params) == 0) {
    # Fallback: use league average (24 poss for regulation, 10 for OT)
    mu <- if (qtr <= 4) 24 else 10
    sd <- if (qtr <= 4) 3 else 2
  } else {
    mu <- num0(poss_params$POSS_mean, 24)
    sd <- num0(poss_params$POSS_sd, 3) * variance_scale
  }
  
  draw_attempts(mu, sd)
}

# --------------------------------------------------
# Quarter Offense Simulation
# --------------------------------------------------
simulate_quarter_offense <- function(team_id, opp_team_id, qtr, game_id,
                                     team_offense_adjusted_q,
                                     team_poss_profile_q,
                                     team_rebounding_rates_q,
                                     team_turnover_rates_q,
                                     team_foul_rates_q,
                                     is_b2b = 0) {
  
  # STEP 1: Draw base possessions
  base_poss <- draw_team_possessions_q(team_id, qtr, game_id, team_poss_profile_q)
  
  # STEP 2: Get opponent-adjusted shooting profile
  off_params <- team_offense_adjusted_q %>%
    filter(
      game_id == !!game_id,
      ESPN_TEAM_ID == !!team_id,
      OPP_ESPN_TEAM_ID == !!opp_team_id,
      qtr == !!qtr
    ) %>%
    slice(1)
  
  # STEP 3: Extract shooting parameters
  fg2_pct   <- num0(off_params$fg2_pct_adjusted,       0.54)
  fg3_pct   <- num0(off_params$fg3_pct_adjusted,       0.36)
  ft_pct    <- num0(off_params$ft_pct_adjusted,        0.78)
  p3        <- num0(off_params$p3_adjusted,             0.42)
  fta_rate  <- num0(off_params$fta_per_fga_adjusted,   0.29)
  tov_rate  <- num0(off_params$tov_per_poss_adjusted,  0.14)
  oreb_rate <- num0(off_params$oreb_per_poss_adjusted, 0.10)
  
  # STEP 3B: Foul bonus — opponent fouls generate extra FTAs for this team
  # Uses opponent's foul rate scaled by foul_bonus_leverage
  foul_params <- team_foul_rates_q %>%
    filter(
      ESPN_GAME_ID == !!game_id,
      ESPN_TEAM_ID == !!opp_team_id,  # opponent's fouls benefit this team
      qtr == !!qtr
    ) %>%
    slice(1)
  
  opp_foul_rate <- num0(foul_params$foul_per_poss, 0.10)
  fta_rate <- fta_rate + (opp_foul_rate * MC_CONFIG$foul_bonus_leverage * 0.10)
  # 0.10 scalar converts fouls/min to a reasonable FTA rate contribution
  
  # STEP 3C: Apply back-to-back fatigue penalty
  if (isTRUE(is_b2b == 1)) {
    fatigue_mult <- 0.97
    fg2_pct  <- fg2_pct  * fatigue_mult
    fg3_pct  <- fg3_pct  * fatigue_mult
    ft_pct   <- ft_pct   * fatigue_mult
    fta_rate <- fta_rate * fatigue_mult
    tov_rate <- min(tov_rate * 1.05, 0.30)
  }
  
  # STEP 4: Calculate turnovers
  # tov_reduction_pct = 1.0 means full effect, < 1.0 dampens turnover impact
  effective_tov_rate <- clamp01(tov_rate * MC_CONFIG$tov_reduction_pct)
  turnovers <- rbinom(1, base_poss, effective_tov_rate)
  
  # STEP 5: Offensive rebound bonus possessions
  scoring_poss <- base_poss - turnovers
  expected_oreb <- scoring_poss * oreb_rate
  bonus_poss <- rbinom(1, round(expected_oreb), MC_CONFIG$oreb_bonus_pct)
  
  # STEP 6: Effective scoring possessions
  effective_poss <- max(0, scoring_poss + bonus_poss)
  
  # STEP 7: Calculate pts per possession from shooting
  pts_per_fga <- (1 - p3) * fg2_pct * 2 + p3 * fg3_pct * 3
  pts_per_fga_with_fts <- pts_per_fga + fta_rate * ft_pct
  fga_per_poss <- 0.88
  pts_per_poss <- pts_per_fga_with_fts * fga_per_poss
  
  # STEP 8: Expected points + variance
  expected_pts <- effective_poss * pts_per_poss
  pts_sd <- sqrt(effective_poss) * MC_CONFIG$pts_per_poss_variance
  actual_pts <- max(0, rnorm(1, mean = expected_pts, sd = pts_sd))
  
  list(
    points        = round(actual_pts),
    base_poss     = base_poss,
    turnovers     = turnovers,
    bonus_poss    = bonus_poss,
    effective_poss = effective_poss,
    pts_per_poss  = pts_per_poss,
    expected_pts  = expected_pts
  )
}

# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
#
#     PHASE 5: PLAYER-LEVEL MONTE CARLO SIMULATION ENGINE
#
#     WHAT CHANGED:
#       - simulate_quarter_offense_player() replaces simulate_quarter_offense()
#       - Instead of one team-level efficiency number, we loop over active players
#       - Each possession: sample a player by usage*minutes weight, fire their
#         personal fg2_pct / fg3_pct / ft_pct / p3 profiles
#       - Per-player stats accumulate each sim → player props fall out naturally
#       - run_mc_simulations_batch() updated to accept player profiles + toggle
#
#     WHAT IS IDENTICAL:
#       - draw_team_possessions_q()      ← untouched
#       - OT resolution block            ← untouched
#       - Blowout drag / intentional foul ← untouched
#       - aggregate_quarter_results()    ← untouched
#       - calculate_summary_stats()      ← untouched
#       - calculate_calibration()        ← untouched
#       - Platt scaling section          ← untouched
#
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲


# ===========================================================================
# SECTION 1: PLAYER-LEVEL QUARTER OFFENSE SIMULATION
# ---------------------------------------------------------------------------
# Drop-in replacement for simulate_quarter_offense().
# Inputs are identical EXCEPT two new args are added:
#   player_shooting_profile_q   — quarter-expanded player shooting profiles
#   player_usage_profile        — game-level player usage rates
#
# Logic:
#   1. Draw possessions (same as before via draw_team_possessions_q)
#   2. Get active players for this team/game/quarter with mins_q > 0
#   3. Join usage rates, compute usage_weight = usage_rate * mins_q
#   4. For each possession: sample a player by weight, fire their shooting
#   5. Accumulate per-player stats (pts, fga, fgm, ftm, etc.)
#   6. Return same list structure as old function PLUS player_stats tibble
# ===========================================================================

# ===========================================================================
# simulate_quarter_offense_player() — OPTION B (FULL PLAYER-LEVEL)
# ---------------------------------------------------------------------------
# All box score stats (pts, reb, ast, blk, stl, fouls) tracked per player.
# Replaces all team-level estimates in the quarter results tibble.
#
# New arguments vs team version:
#   player_shooting_profile_q  — FG2%, FG3%, FT%, p3, FTA_per_FGA per player/qtr
#   player_usage_profile       — usage_rate per player/game
#   player_apm_profile_q       — ast_per_min per player/qtr
#   player_bpm_profile_q       — blk_per_min per player/qtr
#   player_spm_profile_q       — stl_per_min per player/qtr
#   player_rpm_profile_q       — reb_per_min (+ oreb/dreb) per player/qtr
#   player_fpm_profile_q       — fouls_per_min per player/qtr
#
# Returns same list structure as team version PLUS player_stats tibble.
# The quarter results tibble now uses player-derived reb/ast/blk/stl/fouls
# instead of team-level possession estimates.
# ===========================================================================

simulate_quarter_offense_player <- function(
    team_id,
    opp_team_id,
    qtr,
    game_id,
    team_offense_adjusted_q,
    team_poss_profile_q,
    team_rebounding_rates_q,
    team_turnover_rates_q,
    team_foul_rates_q,
    player_shooting_profile_q,
    player_usage_profile,
    player_apm_profile_q,
    player_bpm_profile_q,
    player_spm_profile_q,
    player_rpm_profile_q,
    player_fpm_profile_q,
    is_b2b = 0
) {
  
  # --------------------------------------------------
  # STEP 1: Draw base possessions (unchanged)
  # --------------------------------------------------
  base_poss <- draw_team_possessions_q(team_id, qtr, game_id, team_poss_profile_q)
  
  # --------------------------------------------------
  # STEP 2: Get opponent-adjusted team profile
  # (tov_rate, oreb_rate, foul bonus — still team-level)
  # --------------------------------------------------
  off_params <- team_offense_adjusted_q %>%
    filter(
      game_id          == !!game_id,
      ESPN_TEAM_ID     == !!team_id,
      OPP_ESPN_TEAM_ID == !!opp_team_id,
      qtr              == !!qtr
    ) %>%
    slice(1)
  
  tov_rate  <- num0(off_params$tov_per_poss_adjusted,  0.14)
  oreb_rate <- num0(off_params$oreb_per_poss_adjusted, 0.10)
  fta_rate  <- num0(off_params$fta_per_fga_adjusted,   0.29)
  
  # Foul bonus — opponent fouls generate extra FTAs for this team
  foul_params <- team_foul_rates_q %>%
    filter(
      ESPN_GAME_ID == !!game_id,
      ESPN_TEAM_ID == !!opp_team_id,
      qtr          == !!qtr
    ) %>%
    slice(1)
  
  opp_foul_rate <- num0(foul_params$foul_per_poss, 0.10)
  fta_rate <- fta_rate + (opp_foul_rate * MC_CONFIG$foul_bonus_leverage * 0.10)
  
  # B2B fatigue on team-level rates
  if (isTRUE(is_b2b == 1)) {
    fatigue_mult <- 0.97
    tov_rate  <- tov_rate  * fatigue_mult
    oreb_rate <- oreb_rate * fatigue_mult
    fta_rate  <- fta_rate  * fatigue_mult
  }
  
  # --------------------------------------------------
  # STEP 3: Turnovers + effective possessions
  # --------------------------------------------------
  effective_tov_rate <- clamp01(tov_rate * MC_CONFIG$tov_reduction_pct)
  turnovers          <- rbinom(1, base_poss, effective_tov_rate)
  scoring_poss       <- base_poss - turnovers
  expected_oreb      <- scoring_poss * oreb_rate
  bonus_poss         <- rbinom(1, max(0L, round(expected_oreb)), MC_CONFIG$oreb_bonus_pct)
  effective_poss     <- max(0L, scoring_poss + bonus_poss)
  
  # Empty result template
  empty_player_stats <- tibble(
    ESPN_PLAYER_ID = character(),
    sim_pts   = numeric(),
    sim_fga   = integer(), sim_fgm   = integer(),
    sim_fga3  = integer(), sim_fgm3  = integer(),
    sim_fta   = integer(), sim_ftm   = integer(),
    sim_reb   = numeric(), sim_oreb  = numeric(), sim_dreb = numeric(),
    sim_ast   = numeric(),
    sim_blk   = numeric(),
    sim_stl   = numeric(),
    sim_fouls = numeric()
  )
  
  if (effective_poss == 0L) {
    return(list(
      points         = 0L,
      base_poss      = base_poss,
      turnovers      = turnovers,
      bonus_poss     = bonus_poss,
      effective_poss = 0L,
      pts_per_poss   = 0,
      expected_pts   = 0,
      # Team-level box score totals (used by quarter results tibble)
      team_reb   = 0, team_ast   = 0,
      team_blk   = 0, team_stl   = 0, team_fouls = 0,
      player_stats = empty_player_stats
    ))
  }
  
  # --------------------------------------------------
  # STEP 4: Get active players — shooting profile is the anchor
  # (mins_q > 0 filters to players with projected minutes)
  # --------------------------------------------------
  players_q <- player_shooting_profile_q %>%
    filter(
      ESPN_GAME_ID == !!game_id,
      ESPN_TEAM_ID == !!team_id,
      qtr          == !!qtr,
      mins_q       >  0
    )
  
  # --------------------------------------------------
  # FALLBACK: no player profiles — revert to team-level
  # --------------------------------------------------
  if (nrow(players_q) == 0) {
    fg2_pct_t <- num0(off_params$fg2_pct_adjusted, 0.54)
    fg3_pct_t <- num0(off_params$fg3_pct_adjusted, 0.36)
    ft_pct_t  <- num0(off_params$ft_pct_adjusted,  0.78)
    p3_t      <- num0(off_params$p3_adjusted,       0.42)
    
    if (isTRUE(is_b2b == 1)) {
      fg2_pct_t <- fg2_pct_t * 0.97
      fg3_pct_t <- fg3_pct_t * 0.97
      ft_pct_t  <- ft_pct_t  * 0.97
    }
    
    pts_per_fga  <- (1 - p3_t) * fg2_pct_t * 2 + p3_t * fg3_pct_t * 3
    pts_per_poss <- (pts_per_fga + fta_rate * ft_pct_t) * 0.88
    expected_pts <- effective_poss * pts_per_poss
    pts_sd       <- sqrt(effective_poss) * MC_CONFIG$pts_per_poss_variance
    actual_pts   <- max(0, rnorm(1, mean = expected_pts, sd = pts_sd))
    
    return(list(
      points         = round(actual_pts),
      base_poss      = base_poss,
      turnovers      = turnovers,
      bonus_poss     = bonus_poss,
      effective_poss = effective_poss,
      pts_per_poss   = pts_per_poss,
      expected_pts   = expected_pts,
      team_reb       = round(effective_poss * 0.85),
      team_ast       = round(effective_poss * 0.60),
      team_blk       = round(effective_poss * 0.05),
      team_stl       = round(effective_poss * 0.08),
      team_fouls     = round(effective_poss * 0.20),
      player_stats   = empty_player_stats
    ))
  }
  
  # --------------------------------------------------
  # STEP 5: Join all player rate profiles
  # --------------------------------------------------
  
  # Usage rates
  players_q <- players_q %>%
    left_join(
      player_usage_profile %>%
        filter(ESPN_GAME_ID == !!game_id, ESPN_TEAM_ID == !!team_id) %>%
        select(ESPN_PLAYER_ID, usage_rate),
      by = "ESPN_PLAYER_ID"
    ) %>%
    # Assists
    left_join(
      player_apm_profile_q %>%
        filter(ESPN_GAME_ID == !!game_id, ESPN_TEAM_ID == !!team_id, qtr == !!qtr) %>%
        select(ESPN_PLAYER_ID, ast_per_min),
      by = "ESPN_PLAYER_ID"
    ) %>%
    # Blocks
    left_join(
      player_bpm_profile_q %>%
        filter(ESPN_GAME_ID == !!game_id, ESPN_TEAM_ID == !!team_id, qtr == !!qtr) %>%
        select(ESPN_PLAYER_ID, blk_per_min),
      by = "ESPN_PLAYER_ID"
    ) %>%
    # Steals
    left_join(
      player_spm_profile_q %>%
        filter(ESPN_GAME_ID == !!game_id, ESPN_TEAM_ID == !!team_id, qtr == !!qtr) %>%
        select(ESPN_PLAYER_ID, stl_per_min),
      by = "ESPN_PLAYER_ID"
    ) %>%
    # Rebounds (oreb + dreb + total)
    left_join(
      player_rpm_profile_q %>%
        filter(ESPN_GAME_ID == !!game_id, ESPN_TEAM_ID == !!team_id, qtr == !!qtr) %>%
        select(ESPN_PLAYER_ID, reb_per_min, oreb_per_min, dreb_per_min),
      by = "ESPN_PLAYER_ID"
    ) %>%
    # Fouls
    left_join(
      player_fpm_profile_q %>%
        filter(ESPN_GAME_ID == !!game_id, ESPN_TEAM_ID == !!team_id, qtr == !!qtr) %>%
        select(ESPN_PLAYER_ID, fouls_per_min),
      by = "ESPN_PLAYER_ID"
    ) %>%
    mutate(
      # Fallback defaults for NAs
      usage_rate   = num0(usage_rate,   0.20),
      ast_per_min  = num0(ast_per_min,  0.05),   # ~2.4 ast/48min league avg
      blk_per_min  = num0(blk_per_min,  0.01),   # ~0.5 blk/48min league avg
      stl_per_min  = num0(stl_per_min,  0.015),  # ~0.7 stl/48min league avg
      reb_per_min  = num0(reb_per_min,  0.08),   # ~3.8 reb/48min league avg
      oreb_per_min = num0(oreb_per_min, 0.02),
      dreb_per_min = num0(dreb_per_min, 0.06),
      fouls_per_min= num0(fouls_per_min,0.07),   # ~3.4 fouls/48min league avg
      
      # Apply B2B fatigue to shooting profiles
      FG2_pct = if (isTRUE(is_b2b == 1)) FG2_pct * 0.97 else FG2_pct,
      FG3_pct = if (isTRUE(is_b2b == 1)) FG3_pct * 0.97 else FG3_pct,
      FT_pct  = if (isTRUE(is_b2b == 1)) FT_pct  * 0.97 else FT_pct,
      
      # Usage weight for shot allocation
      usage_weight = usage_rate * mins_q
    )
  
  total_weight <- sum(players_q$usage_weight, na.rm = TRUE)
  if (total_weight <= 0) {
    players_q  <- players_q %>% mutate(usage_weight = 1)
    total_weight <- nrow(players_q)
  }
  players_q <- players_q %>% mutate(prob = usage_weight / total_weight)
  
  # --------------------------------------------------
  # STEP 6: Pre-extract arrays for fast per-possession loop
  # --------------------------------------------------
  n_players  <- nrow(players_q)
  player_ids <- players_q$ESPN_PLAYER_ID
  probs      <- players_q$prob
  mins_vec   <- players_q$mins_q
  
  # Shooting
  fg2_vec <- players_q$FG2_pct
  fg3_vec <- players_q$FG3_pct
  ft_vec  <- players_q$FT_pct
  p3_vec  <- players_q$p3
  fta_vec <- players_q$FTA_per_FGA
  
  # Box score rates (per minute)
  ast_vec   <- players_q$ast_per_min
  blk_vec   <- players_q$blk_per_min
  stl_vec   <- players_q$stl_per_min
  reb_vec   <- players_q$reb_per_min
  oreb_vec  <- players_q$oreb_per_min
  dreb_vec  <- players_q$dreb_per_min
  fouls_vec <- players_q$fouls_per_min
  
  # --------------------------------------------------
  # STEP 7: Per-player expected non-scoring stats
  # These are time-based (per minute × projected minutes)
  # and do NOT vary per possession — computed once per quarter sim
  # --------------------------------------------------
  
  # Quarter duration baseline: ~12 min regulation, ~5 min OT
  # mins_q already reflects each player's projected share
  acc_reb   <- pmax(0, rnorm(n_players, mean = reb_vec   * mins_vec, sd = sqrt(reb_vec   * mins_vec + 0.01)))
  acc_oreb  <- pmax(0, rnorm(n_players, mean = oreb_vec  * mins_vec, sd = sqrt(oreb_vec  * mins_vec + 0.01)))
  acc_dreb  <- pmax(0, rnorm(n_players, mean = dreb_vec  * mins_vec, sd = sqrt(dreb_vec  * mins_vec + 0.01)))
  acc_ast   <- pmax(0, rnorm(n_players, mean = ast_vec   * mins_vec, sd = sqrt(ast_vec   * mins_vec + 0.01)))
  acc_blk   <- pmax(0, rnorm(n_players, mean = blk_vec   * mins_vec, sd = sqrt(blk_vec   * mins_vec + 0.01)))
  acc_stl   <- pmax(0, rnorm(n_players, mean = stl_vec   * mins_vec, sd = sqrt(stl_vec   * mins_vec + 0.01)))
  acc_fouls <- pmax(0, rnorm(n_players, mean = fouls_vec * mins_vec, sd = sqrt(fouls_vec * mins_vec + 0.01)))
  
  # --------------------------------------------------
  # STEP 8: Simulate possessions — sample player, fire shooting profile
  # --------------------------------------------------
  acc_pts  <- numeric(n_players)
  acc_fga  <- integer(n_players)
  acc_fgm  <- integer(n_players)
  acc_fga3 <- integer(n_players)
  acc_fgm3 <- integer(n_players)
  acc_fta  <- integer(n_players)
  acc_ftm  <- integer(n_players)
  
  fga_per_poss <- 0.88
  n_fga <- max(0L, round(effective_poss * fga_per_poss))
  
  if (n_fga > 0) {
    shooter_idx <- sample.int(n_players, size = n_fga, replace = TRUE, prob = probs)
    
    for (idx in seq_len(n_fga)) {
      p <- shooter_idx[idx]
      
      is_three <- runif(1) < p3_vec[p]
      
      if (is_three) {
        made <- runif(1) < fg3_vec[p]
        pts  <- if (made) 3L else 0L
        acc_fga3[p] <- acc_fga3[p] + 1L
        acc_fgm3[p] <- acc_fgm3[p] + as.integer(made)
      } else {
        made <- runif(1) < fg2_vec[p]
        pts  <- if (made) 2L else 0L
      }
      
      acc_fga[p] <- acc_fga[p] + 1L
      acc_fgm[p] <- acc_fgm[p] + as.integer(made)
      acc_pts[p] <- acc_pts[p] + pts
      
      # Free throws
      draws_ft <- runif(1) < fta_vec[p]
      if (draws_ft) {
        n_ft    <- 2L
        ft_made <- rbinom(1, n_ft, ft_vec[p])
        acc_fta[p] <- acc_fta[p] + n_ft
        acc_ftm[p] <- acc_ftm[p] + ft_made
        acc_pts[p] <- acc_pts[p] + ft_made
      }
    }
  }
  
  # --------------------------------------------------
  # STEP 9: Apply variance to total points
  # --------------------------------------------------
  total_pts  <- sum(acc_pts)
  pts_sd     <- sqrt(effective_poss) * MC_CONFIG$pts_per_poss_variance
  actual_pts <- max(0, rnorm(1, mean = total_pts, sd = pts_sd))
  
  # Scale player pts proportionally to noise-adjusted total
  scale_factor   <- if (total_pts > 0) actual_pts / total_pts else 1
  acc_pts_scaled <- acc_pts * scale_factor
  
  pts_per_poss <- if (effective_poss > 0) actual_pts / effective_poss else 0
  
  # --------------------------------------------------
  # STEP 10: Build player stats tibble
  # --------------------------------------------------
  player_stats <- tibble(
    ESPN_PLAYER_ID = player_ids,
    sim_pts   = acc_pts_scaled,
    sim_fga   = acc_fga,
    sim_fgm   = acc_fgm,
    sim_fga3  = acc_fga3,
    sim_fgm3  = acc_fgm3,
    sim_fta   = acc_fta,
    sim_ftm   = acc_ftm,
    sim_reb   = acc_reb,
    sim_oreb  = acc_oreb,
    sim_dreb  = acc_dreb,
    sim_ast   = acc_ast,
    sim_blk   = acc_blk,
    sim_stl   = acc_stl,
    sim_fouls = acc_fouls
  ) %>%
    filter(sim_fga > 0 | sim_fta > 0 | sim_reb > 0 | sim_ast > 0)
  
  # --------------------------------------------------
  # STEP 11: Team-level totals (sum of player stats)
  # Used directly in the quarter results tibble
  # --------------------------------------------------
  team_reb   <- sum(acc_reb)
  team_ast   <- sum(acc_ast)
  team_blk   <- sum(acc_blk)
  team_stl   <- sum(acc_stl)
  team_fouls <- sum(acc_fouls)
  
  list(
    points         = round(actual_pts),
    base_poss      = base_poss,
    turnovers      = turnovers,
    bonus_poss     = bonus_poss,
    effective_poss = effective_poss,
    pts_per_poss   = pts_per_poss,
    expected_pts   = total_pts,
    # Team totals for quarter results tibble
    team_reb       = team_reb,
    team_ast       = team_ast,
    team_blk       = team_blk,
    team_stl       = team_stl,
    team_fouls     = team_fouls,
    player_stats   = player_stats
  )
}


# ===========================================================================
# UPDATED QUARTER RESULTS ROWS
# ---------------------------------------------------------------------------
# Replace the quarter_results tibble construction inside
# run_mc_simulations_batch with this. The only change is reb/ast/blk/stl/fouls
# now come from home_result$team_reb etc. instead of possession estimates.
# ===========================================================================

# HOME quarter result row:
# quarter_results[[length(quarter_results) + 1]] <- tibble(
#   game_id      = game$game_id,
#   game_date    = game$game_date,
#   sim_index    = sim_i,
#   team_id      = game$home_team_id,
#   opp_id       = game$away_team_id,
#   qtr          = qtr,
#   pts_proj_q   = home_result$points,
#   tov_proj_q   = home_result$turnovers,
#   reb_proj_q   = round(home_result$team_reb),      # ← CHANGED
#   ast_proj_q   = round(home_result$team_ast),      # ← CHANGED
#   blk_proj_q   = round(home_result$team_blk),      # ← CHANGED
#   stl_proj_q   = round(home_result$team_stl),      # ← CHANGED
#   fouls_proj_q = round(home_result$team_fouls)     # ← CHANGED
# )

# AWAY quarter result row:
# quarter_results[[length(quarter_results) + 1]] <- tibble(
#   game_id      = game$game_id,
#   game_date    = game$game_date,
#   sim_index    = sim_i,
#   team_id      = game$away_team_id,
#   opp_id       = game$home_team_id,
#   qtr          = qtr,
#   pts_proj_q   = away_result$points,
#   tov_proj_q   = away_result$turnovers,
#   reb_proj_q   = round(away_result$team_reb),      # ← CHANGED
#   ast_proj_q   = round(away_result$team_ast),      # ← CHANGED
#   blk_proj_q   = round(away_result$team_blk),      # ← CHANGED
#   stl_proj_q   = round(away_result$team_stl),      # ← CHANGED
#   fouls_proj_q = round(away_result$team_fouls)     # ← CHANGED
# )


# ===========================================================================
# UPDATED run_mc_simulations_batch() FUNCTION SIGNATURE
# ---------------------------------------------------------------------------
# Add these 5 new arguments after player_usage_profile:
#
#   player_apm_profile_q  = player_apm_profile_q,
#   player_bpm_profile_q  = player_bpm_profile_q,
#   player_spm_profile_q  = player_spm_profile_q,
#   player_rpm_profile_q  = player_rpm_profile_q,
#   player_fpm_profile_q  = player_fpm_profile_q,
#
# And pass them through to simulate_quarter_offense_player() at each call site.
# ===========================================================================


# ===========================================================================
# UPDATED EXECUTION CALL
# ---------------------------------------------------------------------------
# Add the 5 new profile arguments to the mc_sims_backtest_q call:
# ===========================================================================

# mc_sims_backtest_q <- run_mc_simulations_batch(
#   games_to_simulate = games_backtest,
#
#   # Team-level profiles (unchanged)
#   team_offense_adjusted_q = team_offense_adjusted_q,
#   team_poss_profile_q     = team_poss_profile_q_backtest,
#   team_rebounding_rates_q = team_rebounding_rates_q,
#   team_turnover_rates_q   = team_turnover_rates_q,
#   team_foul_rates_q       = team_foul_rates_q,
#   team_shooting_params_q  = team_shooting_params_q,
#   team_assist_rates_q     = team_assist_rates_q,
#   team_block_rates_q      = team_block_rates_q,
#   team_steal_rates_q      = team_steal_rates_q,
#
#   # Player shooting + usage (from Phase 5 Section 3)
#   player_shooting_profile_q = player_shooting_profile_q,
#   player_usage_profile      = player_usage_profile_backtest,
#
#   # NEW: Player box score rate profiles
#   player_apm_profile_q = player_apm_profile_q,
#   player_bpm_profile_q = player_bpm_profile_q,
#   player_spm_profile_q = player_spm_profile_q,
#   player_rpm_profile_q = player_rpm_profile_q,
#   player_fpm_profile_q = player_fpm_profile_q,
#
#   sim_mode     = "player",
#   n_sims       = 1000,
#   use_parallel = TRUE
# )

# --------------------------------------------------
# Blowout Drag (Q4 Adjustment)
# --------------------------------------------------
apply_blowout_drag <- function(base_poss, margin_after_q3) {
  
  # If margin is small, no adjustment
  if (abs(margin_after_q3) < MC_CONFIG$blowout_threshold) {
    return(base_poss)
  }
  
  # Reduce possessions in Q4 during blowouts
  reduction_factor <- 1 - MC_CONFIG$blowout_poss_reduction
  adjusted_poss <- round(base_poss * reduction_factor)
  
  max(adjusted_poss, 1L)  # At least 1 possession
}

# --------------------------------------------------
# Intentional Fouling (Q4 Endgame)
# --------------------------------------------------
apply_intentional_fouling <- function(home_pts, away_pts, home_ft_pct, away_ft_pct) {
  
  margin <- home_pts - away_pts
  
  # Only foul in close games
  if (abs(margin) > MC_CONFIG$intentional_foul_margin) {
    return(list(home_ft_pts = 0, away_ft_pts = 0, foul_trips = 0))
  }
  
  # Determine number of foul trips (random, scaled by config)
  foul_trips <- rbinom(
    1, 
    MC_CONFIG$intentional_foul_max_trips, 
    MC_CONFIG$intentional_foul_strength * 0.5
  )
  
  if (foul_trips == 0) {
    return(list(home_ft_pts = 0, away_ft_pts = 0, foul_trips = 0))
  }
  
  # Trailing team fouls the leading team
  if (margin > 0) {
    # Home winning, away fouls home
    fta <- foul_trips * 2  # Assume 2 FTs per trip
    ftm <- rbinom(1, fta, clamp01(home_ft_pct))
    return(list(home_ft_pts = ftm, away_ft_pts = 0, foul_trips = foul_trips))
  } else {
    # Away winning, home fouls away
    fta <- foul_trips * 2
    ftm <- rbinom(1, fta, clamp01(away_ft_pct))
    return(list(home_ft_pts = 0, away_ft_pts = ftm, foul_trips = foul_trips))
  }
}

# --------------------------------------------------
# Single Game Simulation
# --------------------------------------------------
simulate_game_once <- function(game_id, home_team_id, away_team_id,
                               team_offense_adjusted_q,
                               team_poss_profile_q,
                               team_rebounding_rates_q,
                               team_turnover_rates_q,
                               team_foul_rates_q,
                               team_shooting_params_q) {
  
  # Initialize quarter results
  home_pts_by_q <- numeric(4)
  away_pts_by_q <- numeric(4)
  
  # Simulate Q1-Q3
  for (qtr in 1:3) {
    
    home_result <- simulate_quarter_offense(
      team_id = home_team_id,
      opp_team_id = away_team_id,
      qtr = qtr,
      game_id = game_id,
      team_offense_adjusted_q = team_offense_adjusted_q,
      team_poss_profile_q = team_poss_profile_q,
      team_rebounding_rates_q = team_rebounding_rates_q,
      team_turnover_rates_q = team_turnover_rates_q,
      team_foul_rates_q = team_foul_rates_q
    )
    
    away_result <- simulate_quarter_offense(
      team_id = away_team_id,
      opp_team_id = home_team_id,
      qtr = qtr,
      game_id = game_id,
      team_offense_adjusted_q = team_offense_adjusted_q,
      team_poss_profile_q = team_poss_profile_q,
      team_rebounding_rates_q = team_rebounding_rates_q,
      team_turnover_rates_q = team_turnover_rates_q,
      team_foul_rates_q = team_foul_rates_q
    )
    
    home_pts_by_q[qtr] <- home_result$points
    away_pts_by_q[qtr] <- away_result$points
  }
  
  # Check for blowout before Q4
  margin_after_q3 <- sum(home_pts_by_q[1:3]) - sum(away_pts_by_q[1:3])
  
  # Simulate Q4 (with potential blowout drag)
  home_q4 <- simulate_quarter_offense(
    team_id = home_team_id,
    opp_team_id = away_team_id,
    qtr = 4,
    game_id = game_id,
    team_offense_adjusted_q = team_offense_adjusted_q,
    team_poss_profile_q = team_poss_profile_q,
    team_rebounding_rates_q = team_rebounding_rates_q,
    team_turnover_rates_q = team_turnover_rates_q,
    team_foul_rates_q = team_foul_rates_q
  )
  
  away_q4 <- simulate_quarter_offense(
    team_id = away_team_id,
    opp_team_id = home_team_id,
    qtr = 4,
    game_id = game_id,
    team_offense_adjusted_q = team_offense_adjusted_q,
    team_poss_profile_q = team_poss_profile_q,
    team_rebounding_rates_q = team_rebounding_rates_q,
    team_turnover_rates_q = team_turnover_rates_q,
    team_foul_rates_q = team_foul_rates_q
  )
  
  # Apply blowout drag to Q4 possessions if needed
  if (abs(margin_after_q3) >= MC_CONFIG$blowout_threshold) {
    # Reduce Q4 scoring proportionally
    drag_factor <- 1 - MC_CONFIG$blowout_poss_reduction
    home_q4$points <- round(home_q4$points * drag_factor)
    away_q4$points <- round(away_q4$points * drag_factor)
  }
  
  home_pts_by_q[4] <- home_q4$points
  away_pts_by_q[4] <- away_q4$points
  
  home_total <- pmax(65, pmin(160, home_total))
  away_total <- pmax(65, pmin(160, away_total))
  
  # Calculate regulation totals
  home_total <- sum(home_pts_by_q)
  away_total <- sum(away_pts_by_q)
  
  # Get FT% for intentional fouling
  home_ft_pct <- team_shooting_params_q %>%
    filter(ESPN_TEAM_ID == !!home_team_id, qtr == 4) %>%
    slice(1) %>%
    pull(FT_pct) %>%
    num0(0.78)
  
  away_ft_pct <- team_shooting_params_q %>%
    filter(ESPN_TEAM_ID == !!away_team_id, qtr == 4) %>%
    slice(1) %>%
    pull(FT_pct) %>%
    num0(0.78)
  
  # Apply intentional fouling if close
  foul_result <- apply_intentional_fouling(home_total, away_total, home_ft_pct, away_ft_pct)
  
  home_total <- home_total + foul_result$home_ft_pts
  away_total <- away_total + foul_result$away_ft_pts
  
  # Return result
  tibble(
    game_id = game_id,
    home_team_id = home_team_id,
    away_team_id = away_team_id,
    home_points = home_total,
    away_points = away_total,
    margin = home_total - away_total,
    home_win = home_total > away_total,
    home_q1 = home_pts_by_q[1],
    home_q2 = home_pts_by_q[2],
    home_q3 = home_pts_by_q[3],
    home_q4 = home_pts_by_q[4],
    away_q1 = away_pts_by_q[1],
    away_q2 = away_pts_by_q[2],
    away_q3 = away_pts_by_q[3],
    away_q4 = away_pts_by_q[4],
    intentional_foul_trips = foul_result$foul_trips
  )
}

# --------------------------------------------------
# Monte Carlo Runner
# --------------------------------------------------
run_monte_carlo <- function(game_id, home_team_id, away_team_id,
                            team_offense_adjusted_q,
                            team_poss_profile_q,
                            team_rebounding_rates_q,
                            team_turnover_rates_q,
                            team_foul_rates_q,
                            team_shooting_params_q,
                            n_sims = 10000) {
  
  cat("🎲 Running", n_sims, "simulations for game", game_id, "\n")
  cat("   ", home_team_id, "(home) vs.", away_team_id, "(away)\n")
  
  # Run simulations
  results <- map_dfr(1:n_sims, function(i) {
    if (i %% 1000 == 0) cat("  Completed", i, "simulations...\n")
    
    simulate_game_once(
      game_id = game_id,
      home_team_id = home_team_id,
      away_team_id = away_team_id,
      team_offense_adjusted_q = team_offense_adjusted_q,
      team_poss_profile_q = team_poss_profile_q,
      team_rebounding_rates_q = team_rebounding_rates_q,
      team_turnover_rates_q = team_turnover_rates_q,
      team_foul_rates_q = team_foul_rates_q,
      team_shooting_params_q = team_shooting_params_q
    )
  })
  
  cat("✅ Simulations complete!\n\n")
  
  # Return results
  results
}

# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲 END: Monte Carlo Simulation Engine
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲



# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
#
#
#     ooo        ooooo                           .                    .oooooo.                      oooo                  .oooooo..o  o8o                                oooo                .    o8o                        
#     `88.       .888'                         .o8                   d8P'  `Y8b                     `888                 d8P'    `Y8  `"'                                `888              .o8    `"'                        
#      888b     d'888   .ooooo.  ooo. .oo.   .o888oo  .ooooo.       888           .oooo.   oooo d8b  888   .ooooo.       Y88bo.      oooo  ooo. .oo.  .oo.   oooo  oooo   888   .oooo.   .o888oo oooo   .ooooo.  ooo. .oo.   
#      8 Y88. .P  888  d88' `88b `888P"Y88b    888   d88' `88b      888          `P  )88b  `888""8P  888  d88' `88b       `"Y8888o.  `888  `888P"Y88bP"Y88b  `888  `888   888  `P  )88b    888   `888  d88' `88b `888P"Y88b  
#      8  `888'   888  888   888  888   888    888   888ooo888      888           .oP"888   888      888  888   888           `"Y88b  888   888   888   888   888   888   888   .oP"888    888    888  888   888  888   888  
#      8    Y     888  888   888  888   888    888 . 888    .o      `88b    ooo  d8(  888   888      888  888   888      oo     .d8P  888   888   888   888   888   888   888  d8(  888    888 .  888  888   888  888   888  
#     o8o        o888o `Y8bod8P' o888o o888o   "888" `Y8bod8P'       `Y8bood8P'  `Y888""8o d888b    o888o `Y8bod8P'      8""88888P'  o888o o888o o888o o888o  `V88V"V8P' o888o `Y888""8o   "888" o888o `Y8bod8P' o888o o888o 
#                                                                                                                                                                                                            
#
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# ==========================================================================================================
# MONTE CARLO SIMULATION SYSTEM - BACKTEST & PRODUCTION
# ----------------------------------------------------------------------------------------------------------
# This section implements the complete MC simulation workflow including:
# - Backtest simulations on historical games
# - Calibration analysis
# - Production simulations for today's games
# - Summary statistics and quarter-by-quarter breakdowns
# - Parallel processing for performance
# ==========================================================================================================

# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲 START: MC Simulation Runner
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲

suppressPackageStartupMessages({
  library(dplyr)
  library(tibble)
  library(purrr)
  library(future.apply)
  library(data.table)
})

# Set seed for reproducibility
set.seed(123)

# --------------------------------------------------
# Helper: Aggregate Quarter Results to Game Level
# --------------------------------------------------
aggregate_quarter_results <- function(sim_results) {
  
  # Sum across quarters to get game totals
  game_totals <- sim_results %>%
    group_by(game_id, game_date, sim_index, team_id, opp_id) %>%
    summarise(
      pts_total = sum(pts_proj_q, na.rm = TRUE),
      tov_total = sum(tov_proj_q, na.rm = TRUE),
      reb_total = sum(reb_proj_q, na.rm = TRUE),
      ast_total = sum(ast_proj_q, na.rm = TRUE),
      blk_total = sum(blk_proj_q, na.rm = TRUE),
      stl_total = sum(stl_proj_q, na.rm = TRUE),
      fouls_total = sum(fouls_proj_q, na.rm = TRUE),
      .groups = "drop"
    )
  
  game_totals
}

# --------------------------------------------------
# Helper: Calculate Summary Statistics
# --------------------------------------------------
calculate_summary_stats <- function(game_totals, nba_games_canon_season) {
  
  # Separate home and away results
  home_results <- game_totals %>%
    inner_join(
      nba_games_canon_season %>%
        transmute(
          game_id = as.character(game_id),
          home_team_id = as.character(home_team_id),
          away_team_id = as.character(away_team_id)
        ),
      by = c("game_id", "team_id" = "home_team_id", "opp_id" = "away_team_id")
    ) %>%
    select(game_id, game_date, sim_index, 
           home_team_id = team_id, 
           home_pts = pts_total,
           home_tov = tov_total,
           home_reb = reb_total,
           home_ast = ast_total,
           home_blk = blk_total,
           home_stl = stl_total)
  
  away_results <- game_totals %>%
    inner_join(
      nba_games_canon_season %>%
        transmute(
          game_id = as.character(game_id),
          home_team_id = as.character(home_team_id),
          away_team_id = as.character(away_team_id)
        ),
      by = c("game_id", "team_id" = "away_team_id", "opp_id" = "home_team_id")
    ) %>%
    select(game_id, game_date, sim_index, 
           away_team_id = team_id, 
           away_pts = pts_total,
           away_tov = tov_total,
           away_reb = reb_total,
           away_ast = ast_total,
           away_blk = blk_total,
           away_stl = stl_total)
  
  # Join home and away
  combined <- home_results %>%
    inner_join(away_results, by = c("game_id", "game_date", "sim_index"))
  
  # Calculate summary statistics per game
  summary <- combined %>%
    group_by(game_id, game_date, home_team_id, away_team_id) %>%
    summarise(
      n_sims = n(),
      
      # Home team stats
      home_pts_mean = mean(home_pts, na.rm = TRUE),
      home_pts_median = median(home_pts, na.rm = TRUE),
      home_pts_sd = sd(home_pts, na.rm = TRUE),
      home_pts_p5 = quantile(home_pts, 0.05, na.rm = TRUE),
      home_pts_p95 = quantile(home_pts, 0.95, na.rm = TRUE),
      
      home_tov_mean = mean(home_tov, na.rm = TRUE),
      home_reb_mean = mean(home_reb, na.rm = TRUE),
      home_ast_mean = mean(home_ast, na.rm = TRUE),
      home_blk_mean = mean(home_blk, na.rm = TRUE),
      home_stl_mean = mean(home_stl, na.rm = TRUE),
      
      # Away team stats
      away_pts_mean = mean(away_pts, na.rm = TRUE),
      away_pts_median = median(away_pts, na.rm = TRUE),
      away_pts_sd = sd(away_pts, na.rm = TRUE),
      away_pts_p5 = quantile(away_pts, 0.05, na.rm = TRUE),
      away_pts_p95 = quantile(away_pts, 0.95, na.rm = TRUE),
      
      away_tov_mean = mean(away_tov, na.rm = TRUE),
      away_reb_mean = mean(away_reb, na.rm = TRUE),
      away_ast_mean = mean(away_ast, na.rm = TRUE),
      away_blk_mean = mean(away_blk, na.rm = TRUE),
      away_stl_mean = mean(away_stl, na.rm = TRUE),
      
      # Win probability
      home_wins = sum(home_pts > away_pts, na.rm = TRUE),
      away_wins = sum(away_pts > home_pts, na.rm = TRUE),
      ties = sum(home_pts == away_pts, na.rm = TRUE),
      
      home_win_prob = home_wins / n_sims,
      away_win_prob = away_wins / n_sims,
      
      # Spread and total
      spread_mean = mean(home_pts - away_pts, na.rm = TRUE),
      spread_median = median(home_pts - away_pts, na.rm = TRUE),
      spread_sd = sd(home_pts - away_pts, na.rm = TRUE),
      
      total_mean = mean(home_pts + away_pts, na.rm = TRUE),
      total_median = median(home_pts + away_pts, na.rm = TRUE),
      total_sd = sd(home_pts + away_pts, na.rm = TRUE),
      
      .groups = "drop"
    )
  
  summary
}

# --------------------------------------------------
# Helper: Calculate Quarter-by-Quarter Breakdown
# --------------------------------------------------
calculate_quarter_breakdown <- function(sim_results) {
  
  quarter_stats <- sim_results %>%
    group_by(game_id, game_date, team_id, qtr) %>%
    summarise(
      pts_mean = mean(pts_proj_q, na.rm = TRUE),
      pts_median = median(pts_proj_q, na.rm = TRUE),
      pts_sd = sd(pts_proj_q, na.rm = TRUE),
      
      tov_mean = mean(tov_proj_q, na.rm = TRUE),
      reb_mean = mean(reb_proj_q, na.rm = TRUE),
      ast_mean = mean(ast_proj_q, na.rm = TRUE),
      blk_mean = mean(blk_proj_q, na.rm = TRUE),
      stl_mean = mean(stl_proj_q, na.rm = TRUE),
      fouls_mean = mean(fouls_proj_q, na.rm = TRUE),
      
      .groups = "drop"
    ) %>%
    arrange(game_id, team_id, qtr)
  
  quarter_stats
}

# --------------------------------------------------
# Helper: Calculate Calibration
# --------------------------------------------------
calculate_calibration <- function(summary, nba_games_canon_season) {
  
  # Join with actual results
  calibration_data <- summary %>%
    inner_join(
      nba_games_canon_season %>%
        transmute(
          game_id = as.character(game_id),
          actual_home_pts = as.numeric(home_score),
          actual_away_pts = as.numeric(away_score),
          actual_home_win = home_winner
        ),
      by = "game_id"
    ) %>%
    mutate(
      predicted_winner = if_else(home_win_prob > 0.5, "home", "away"),
      actual_winner = if_else(actual_home_win, "home", "away"),
      correct_pick = predicted_winner == actual_winner,
      
      home_pts_error = home_pts_mean - actual_home_pts,
      away_pts_error = away_pts_mean - actual_away_pts,
      
      # Assign to win probability buckets
      win_prob_bucket = case_when(
        home_win_prob < 0.10 ~ "0.00-0.10",
        home_win_prob < 0.20 ~ "0.10-0.20",
        home_win_prob < 0.30 ~ "0.20-0.30",
        home_win_prob < 0.40 ~ "0.30-0.40",
        home_win_prob < 0.50 ~ "0.40-0.50",
        home_win_prob < 0.60 ~ "0.50-0.60",
        home_win_prob < 0.70 ~ "0.60-0.70",
        home_win_prob < 0.80 ~ "0.70-0.80",
        home_win_prob < 0.90 ~ "0.80-0.90",
        TRUE ~ "0.90-1.00"
      )
    )
  
  # Calculate calibration by bucket
  calibration_summary <- calibration_data %>%
    group_by(win_prob_bucket) %>%
    summarise(
      n_games = n(),
      predicted_win_pct = mean(home_win_prob, na.rm = TRUE),
      actual_win_pct = mean(actual_home_win, na.rm = TRUE),
      calibration_error = actual_win_pct - predicted_win_pct,
      .groups = "drop"
    ) %>%
    arrange(win_prob_bucket)
  
  list(
    calibration_summary = calibration_summary,
    calibration_data = calibration_data
  )
}

# ===========================================================================
# SECTION 2: UPDATED run_mc_simulations_batch()
# ---------------------------------------------------------------------------
# Identical to original EXCEPT:
#   - Two new arguments: player_shooting_profile_q, player_usage_profile
#   - sim_mode toggle: "player" (new) or "team" (original fallback)
#   - Call sites for simulate_quarter_offense replaced with
#     simulate_quarter_offense_player
#   - Player props accumulator added — outputs mc_player_props_raw
# ===========================================================================

run_mc_simulations_batch <- function(games_to_simulate,
                                     team_offense_adjusted_q,
                                     team_poss_profile_q,
                                     team_rebounding_rates_q,
                                     team_turnover_rates_q,
                                     team_foul_rates_q,
                                     team_shooting_params_q,
                                     team_assist_rates_q,
                                     team_block_rates_q,
                                     team_steal_rates_q,
                                     # ── NEW: player-level inputs ──────────
                                     player_shooting_profile_q = NULL,
                                     player_usage_profile      = NULL,
                                     sim_mode = "player",   # "player" | "team"
                                     # ─────────────────────────────────────
                                     n_sims     = MC_CONFIG$n_sims_per_game,
                                     use_parallel = TRUE) {
  
  # Validate sim_mode
  sim_mode <- match.arg(sim_mode, c("player", "team"))
  
  # If player mode requested but profiles missing, warn and fall back
  if (sim_mode == "player" && 
      (is.null(player_shooting_profile_q) || is.null(player_usage_profile))) {
    warning("sim_mode = 'player' but player profiles not supplied. Falling back to 'team' mode.")
    sim_mode <- "team"
  }
  
  n_games <- nrow(games_to_simulate)
  cat("🎲 Starting MC simulations for", n_games, "games\n")
  cat("   Simulations per game:", n_sims, "\n")
  cat("   Sim mode:", sim_mode, "\n")
  cat("   Parallel processing:", use_parallel, "\n\n")
  
  start_time <- Sys.time()
  
  if (use_parallel) {
    future::plan(future::multisession, workers = parallel::detectCores() - 1)
  }
  
  all_sim_results <- if (use_parallel) {
    
    future.apply::future_lapply(
      seq_len(n_games),
      function(i) {
        
        game <- games_to_simulate[i, ]
        
        if (i %% 10 == 0 || i == 1) {
          elapsed   <- as.numeric(difftime(Sys.time(), start_time, units = "mins"))
          remaining <- n_games - i
          rate      <- i / max(elapsed, 0.001)
          eta       <- remaining / rate
          cat(sprintf("  [%d/%d] | Elapsed: %.1f min | ETA: %.1f min\n",
                      i, n_games, elapsed, eta))
        }
        
        # ── Run n_sims for this game ──────────────────────────────────────
        sim_rows <- map_dfr(seq_len(n_sims), function(sim_i) {
          
          quarter_results  <- list()
          player_props_sim <- list()   # NEW: collect player stats per sim
          
          # ── Regulation Q1-Q4 ─────────────────────────────────────────
          for (qtr in 1:4) {
            
            if (sim_mode == "player") {
              
              home_result <- simulate_quarter_offense_player(
                team_id                 = game$home_team_id,
                opp_team_id             = game$away_team_id,
                qtr                     = qtr,
                game_id                 = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q     = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q   = team_turnover_rates_q,
                team_foul_rates_q       = team_foul_rates_q,
                player_shooting_profile_q = player_shooting_profile_q,
                player_usage_profile    = player_usage_profile,
                is_b2b                  = game$home_b2b
              )
              
              away_result <- simulate_quarter_offense_player(
                team_id                 = game$away_team_id,
                opp_team_id             = game$home_team_id,
                qtr                     = qtr,
                game_id                 = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q     = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q   = team_turnover_rates_q,
                team_foul_rates_q       = team_foul_rates_q,
                player_shooting_profile_q = player_shooting_profile_q,
                player_usage_profile    = player_usage_profile,
                is_b2b                  = game$away_b2b
              )
              
            } else {
              
              # ── Original team-level simulation (unchanged) ──────────
              home_result <- simulate_quarter_offense(
                team_id                 = game$home_team_id,
                opp_team_id             = game$away_team_id,
                qtr                     = qtr,
                game_id                 = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q     = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q   = team_turnover_rates_q,
                team_foul_rates_q       = team_foul_rates_q,
                is_b2b                  = game$home_b2b
              )
              
              away_result <- simulate_quarter_offense(
                team_id                 = game$away_team_id,
                opp_team_id             = game$home_team_id,
                qtr                     = qtr,
                game_id                 = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q     = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q   = team_turnover_rates_q,
                team_foul_rates_q       = team_foul_rates_q,
                is_b2b                  = game$away_b2b
              )
            }
            
            # ── Assist / block / steal lookups (unchanged) ────────────
            home_ast <- team_assist_rates_q %>%
              filter(ESPN_GAME_ID == game$game_id,
                     ESPN_TEAM_ID == game$home_team_id, qtr == !!qtr) %>%
              slice(1) %>% pull(ast_per_poss) %>% num0(0.60)
            
            home_blk <- team_block_rates_q %>%
              filter(ESPN_GAME_ID == game$game_id,
                     ESPN_TEAM_ID == game$home_team_id, qtr == !!qtr) %>%
              slice(1) %>% pull(blk_per_poss) %>% num0(0.05)
            
            home_stl <- team_steal_rates_q %>%
              filter(ESPN_GAME_ID == game$game_id,
                     ESPN_TEAM_ID == game$home_team_id, qtr == !!qtr) %>%
              slice(1) %>% pull(stl_per_poss) %>% num0(0.08)
            
            away_ast <- team_assist_rates_q %>%
              filter(ESPN_GAME_ID == game$game_id,
                     ESPN_TEAM_ID == game$away_team_id, qtr == !!qtr) %>%
              slice(1) %>% pull(ast_per_poss) %>% num0(0.60)
            
            away_blk <- team_block_rates_q %>%
              filter(ESPN_GAME_ID == game$game_id,
                     ESPN_TEAM_ID == game$away_team_id, qtr == !!qtr) %>%
              slice(1) %>% pull(blk_per_poss) %>% num0(0.05)
            
            away_stl <- team_steal_rates_q %>%
              filter(ESPN_GAME_ID == game$game_id,
                     ESPN_TEAM_ID == game$away_team_id, qtr == !!qtr) %>%
              slice(1) %>% pull(stl_per_poss) %>% num0(0.08)
            
            # ── Quarter results rows (unchanged structure) ────────────
            quarter_results[[length(quarter_results) + 1]] <- tibble(
              game_id      = game$game_id,
              game_date    = game$game_date,
              sim_index    = sim_i,
              team_id      = game$home_team_id,
              opp_id       = game$away_team_id,
              qtr          = qtr,
              pts_proj_q   = home_result$points,
              tov_proj_q   = home_result$turnovers,
              reb_proj_q   = round(home_result$effective_poss * 0.85),
              ast_proj_q   = round(home_result$effective_poss * home_ast),
              blk_proj_q   = round(home_result$effective_poss * home_blk),
              stl_proj_q   = round(home_result$effective_poss * home_stl),
              fouls_proj_q = round(home_result$effective_poss * 0.20)
            )
            
            quarter_results[[length(quarter_results) + 1]] <- tibble(
              game_id      = game$game_id,
              game_date    = game$game_date,
              sim_index    = sim_i,
              team_id      = game$away_team_id,
              opp_id       = game$home_team_id,
              qtr          = qtr,
              pts_proj_q   = away_result$points,
              tov_proj_q   = away_result$turnovers,
              reb_proj_q   = round(away_result$effective_poss * 0.85),
              ast_proj_q   = round(away_result$effective_poss * away_ast),
              blk_proj_q   = round(away_result$effective_poss * away_blk),
              stl_proj_q   = round(away_result$effective_poss * away_stl),
              fouls_proj_q = round(away_result$effective_poss * 0.20)
            )
            
            # ── NEW: Collect player-level stats for props ─────────────
            if (sim_mode == "player") {
              
              if (nrow(home_result$player_stats) > 0) {
                player_props_sim[[length(player_props_sim) + 1]] <- 
                  home_result$player_stats %>%
                  mutate(
                    game_id      = game$game_id,
                    sim_index    = sim_i,
                    team_id      = game$home_team_id,
                    qtr          = qtr
                  )
              }
              
              if (nrow(away_result$player_stats) > 0) {
                player_props_sim[[length(player_props_sim) + 1]] <- 
                  away_result$player_stats %>%
                  mutate(
                    game_id      = game$game_id,
                    sim_index    = sim_i,
                    team_id      = game$away_team_id,
                    qtr          = qtr
                  )
              }
            }
          }
          
          # ── OVERTIME RESOLUTION (unchanged) ──────────────────────────
          reg_df <- bind_rows(quarter_results)
          
          home_reg_pts <- reg_df %>%
            filter(team_id == game$home_team_id) %>%
            summarise(total = sum(pts_proj_q, na.rm = TRUE)) %>%
            pull(total)
          
          away_reg_pts <- reg_df %>%
            filter(team_id == game$away_team_id) %>%
            summarise(total = sum(pts_proj_q, na.rm = TRUE)) %>%
            pull(total)
          
          ot_period <- 0
          
          while (home_reg_pts == away_reg_pts &&
                 ot_period < MC_CONFIG$max_ot_periods) {
            
            ot_period <- ot_period + 1
            qtr_ot    <- 4 + ot_period
            
            home_ot_poss <- max(0, round(rnorm(1, MC_CONFIG$ot_poss_mean, MC_CONFIG$ot_poss_sd)))
            away_ot_poss <- max(0, round(rnorm(1, MC_CONFIG$ot_poss_mean, MC_CONFIG$ot_poss_sd)))
            
            # OT reuses Q4 profiles (same as original)
            if (sim_mode == "player") {
              home_ot <- simulate_quarter_offense_player(
                team_id = game$home_team_id, opp_team_id = game$away_team_id,
                qtr = 4, game_id = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q = team_turnover_rates_q,
                team_foul_rates_q = team_foul_rates_q,
                player_shooting_profile_q = player_shooting_profile_q,
                player_usage_profile = player_usage_profile,
                is_b2b = game$home_b2b
              )
              away_ot <- simulate_quarter_offense_player(
                team_id = game$away_team_id, opp_team_id = game$home_team_id,
                qtr = 4, game_id = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q = team_turnover_rates_q,
                team_foul_rates_q = team_foul_rates_q,
                player_shooting_profile_q = player_shooting_profile_q,
                player_usage_profile = player_usage_profile,
                is_b2b = game$away_b2b
              )
            } else {
              home_ot <- simulate_quarter_offense(
                team_id = game$home_team_id, opp_team_id = game$away_team_id,
                qtr = 4, game_id = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q = team_turnover_rates_q,
                team_foul_rates_q = team_foul_rates_q,
                is_b2b = game$home_b2b
              )
              away_ot <- simulate_quarter_offense(
                team_id = game$away_team_id, opp_team_id = game$home_team_id,
                qtr = 4, game_id = game$game_id,
                team_offense_adjusted_q = team_offense_adjusted_q,
                team_poss_profile_q = team_poss_profile_q,
                team_rebounding_rates_q = team_rebounding_rates_q,
                team_turnover_rates_q = team_turnover_rates_q,
                team_foul_rates_q = team_foul_rates_q,
                is_b2b = game$away_b2b
              )
            }
            
            ot_scale     <- home_ot_poss / max(home_ot$base_poss, 1)
            home_ot_pts  <- max(0, round(home_ot$points * ot_scale))
            ot_scale     <- away_ot_poss / max(away_ot$base_poss, 1)
            away_ot_pts  <- max(0, round(away_ot$points * ot_scale))
            
            if (ot_period == MC_CONFIG$max_ot_periods && home_ot_pts == away_ot_pts) {
              if (runif(1) > 0.5) home_ot_pts <- home_ot_pts + 1L else away_ot_pts <- away_ot_pts + 1L
            }
            
            home_reg_pts <- home_reg_pts + home_ot_pts
            away_reg_pts <- away_reg_pts + away_ot_pts
            
            quarter_results[[length(quarter_results) + 1]] <- tibble(
              game_id = game$game_id, game_date = game$game_date,
              sim_index = sim_i, team_id = game$home_team_id, opp_id = game$away_team_id,
              qtr = qtr_ot, pts_proj_q = home_ot_pts, tov_proj_q = 0L,
              reb_proj_q = 0L, ast_proj_q = 0L, blk_proj_q = 0L,
              stl_proj_q = 0L, fouls_proj_q = 0L
            )
            
            quarter_results[[length(quarter_results) + 1]] <- tibble(
              game_id = game$game_id, game_date = game$game_date,
              sim_index = sim_i, team_id = game$away_team_id, opp_id = game$home_team_id,
              qtr = qtr_ot, pts_proj_q = away_ot_pts, tov_proj_q = 0L,
              reb_proj_q = 0L, ast_proj_q = 0L, blk_proj_q = 0L,
              stl_proj_q = 0L, fouls_proj_q = 0L
            )
          }
          
          # ── Combine quarter results + attach player props ─────────────
          qtr_df <- bind_rows(quarter_results)
          
          if (sim_mode == "player" && length(player_props_sim) > 0) {
            attr(qtr_df, "player_props") <- bind_rows(player_props_sim)
          }
          
          qtr_df
        })
        
        sim_rows
      },
      future.seed = TRUE
    )
    
  } else {
    
    lapply(seq_len(n_games), function(i) {
      # Non-parallel version identical to parallel — omitted for brevity
      # Replace with same logic above if needed
      stop("Non-parallel mode not implemented in Phase 5. Set use_parallel = TRUE.")
    })
  }
  
  if (use_parallel) future::plan(future::sequential)
  
  total_time <- as.numeric(difftime(Sys.time(), start_time, units = "mins"))
  
  cat("\n✅ Combining results...\n")
  all_results <- bind_rows(all_sim_results)
  
  cat("=", rep("=", 60), "\n", sep = "")
  cat("✅ MC SIMULATIONS COMPLETE!\n")
  cat("=", rep("=", 60), "\n", sep = "")
  cat("  Total games:", n_games, "\n")
  cat("  Total sims:", n_games * n_sims, "\n")
  cat("  Total rows:", nrow(all_results), "\n")
  cat("  Total time:", round(total_time, 1), "minutes\n")
  cat("  Rate:", round(n_games / total_time, 1), "games/min\n\n")
  
  all_results
}

# ==========================================================================================================
# SETUP PARALLEL PROCESSING - 8 CORES
# ==========================================================================================================

suppressPackageStartupMessages({
  library(future)
  library(future.apply)
})

# Set to 8 cores explicitly
cores_to_use <- 7
cat("🚀 Configuring parallel processing with", cores_to_use, "cores\n\n")

# Set up parallel plan
future::plan(future::multisession, workers = cores_to_use)

# Verify setup
cat("✓ Parallel backend configured with", cores_to_use, "workers\n\n")


# ==========================================================================================================
# EXECUTE BACKTEST MC SIMULATIONS
# ==========================================================================================================

cat("=", rep("=", 80), "\n", sep = "")
cat("BACKTEST MC SIMULATIONS - STARTING\n")
cat("=", rep("=", 80), "\n\n", sep = "")


# --------------------------------------------------
# STEP 1: Build rest features from BaseStats_Player_MC
# --------------------------------------------------
cat("📈 Building rest day features...\n")

game_rest_features <- BaseStats_Player_MC %>%
  mutate(GAME_DATE = as.Date(GAME_DATE)) %>%
  group_by(ESPN_GAME_ID, ESPN_TEAM_ID) %>%
  slice(1) %>%
  ungroup() %>%
  select(ESPN_GAME_ID, ESPN_TEAM_ID, TOT_RST_DYS, IS_B2B) %>%
  inner_join(
    nba_games_canon_season %>%
      transmute(
        ESPN_GAME_ID = as.character(game_id),
        home_team_id = as.character(home_team_id),
        away_team_id = as.character(away_team_id)
      ),
    by = "ESPN_GAME_ID"
  ) %>%
  mutate(is_home = ESPN_TEAM_ID == home_team_id) %>%
  select(ESPN_GAME_ID, ESPN_TEAM_ID, TOT_RST_DYS, IS_B2B, is_home) %>%
  pivot_wider(
    id_cols = ESPN_GAME_ID,
    names_from = is_home,
    values_from = c(TOT_RST_DYS, IS_B2B)
  ) %>%
  rename(
    game_id = ESPN_GAME_ID,
    home_rest_days = TOT_RST_DYS_TRUE,
    away_rest_days = TOT_RST_DYS_FALSE,
    home_b2b = IS_B2B_TRUE,
    away_b2b = IS_B2B_FALSE
  ) %>%
  mutate(
    home_rest_days = as.numeric(home_rest_days),
    away_rest_days = as.numeric(away_rest_days),
    home_b2b = as.integer(home_b2b),
    away_b2b = as.integer(away_b2b),
    rest_diff = home_rest_days - away_rest_days
  )

cat("✅ Rest features built for", nrow(game_rest_features), "games\n\n")

# --------------------------------------------------
# STEP 1: Prepare games to simulate
# --------------------------------------------------
cat("📋 Preparing historical games...\n")

games_backtest <- nba_games_canon_season %>%
  filter(as.Date(game_date) < as.Date(as_of_date)) %>%
  transmute(
    game_id = as.character(game_id),
    game_date = as.Date(game_date),
    home_team_id = as.character(home_team_id),
    away_team_id = as.character(away_team_id)
  ) %>%
  distinct() %>%
  left_join(
    game_rest_features %>%
      mutate(game_id = as.character(game_id)),
    by = "game_id"
  )

cat("✓ Found", nrow(games_backtest), "historical games to simulate\n\n")

# ===========================================================================
# SECTION 3: UPDATED EXECUTION BLOCK
# ---------------------------------------------------------------------------
# Replace the existing run_mc_simulations_batch() call at line ~8103
# with this. Everything after it (aggregation, calibration, Platt) is
# completely unchanged.
# ===========================================================================

# --------------------------------------------------
# Run simulation — PLAYER MODE
# --------------------------------------------------
mc_sims_backtest_q <- run_mc_simulations_batch(
  games_to_simulate = games_backtest,
  
  # Team-level profiles (unchanged)
  team_offense_adjusted_q = team_offense_adjusted_q,
  team_poss_profile_q     = team_poss_profile_q_backtest,
  team_rebounding_rates_q = team_rebounding_rates_q,
  team_turnover_rates_q   = team_turnover_rates_q,
  team_foul_rates_q       = team_foul_rates_q,
  team_shooting_params_q  = team_shooting_params_q,
  team_assist_rates_q     = team_assist_rates_q,
  team_block_rates_q      = team_block_rates_q,
  team_steal_rates_q      = team_steal_rates_q,
  
  # NEW: Player-level profiles
  player_shooting_profile_q = player_shooting_profile_q,   # from player script
  player_usage_profile      = player_usage_profile_backtest, # from player script
  sim_mode                  = "player",
  
  n_sims       = 1000,
  use_parallel = TRUE
)

# --------------------------------------------------
# NEW: Extract player props (run after mc_sims_backtest_q)
# --------------------------------------------------
# mc_player_props <- summarize_player_props(all_sim_results)
# NOTE: See implementation note below about extracting attrs from parallel results

# --------------------------------------------------
# Everything below this line is UNCHANGED
# --------------------------------------------------
# aggregate_quarter_results(), calculate_summary_stats(),
# calculate_calibration(), Platt scaling — all identical

# Then run aggregation and calibration as before
# --------------------------------------------------
# STEP 3: Aggregate results
# --------------------------------------------------


# ===========================================================================
# SECTION 4: PLAYER PROPS SUMMARIZER
# ---------------------------------------------------------------------------
# After running simulations, call this to collapse player_stats across
# all sims into per-player prop distributions (mean, median, p5, p95).
#
# Input:  mc_sims_backtest_q  (output of run_mc_simulations_batch)
# Output: mc_player_props     (one row per player per game)
# ===========================================================================

summarize_player_props <- function(sim_results) {
  
  # Extract player_props attributes from each sim chunk
  props_list <- lapply(sim_results, function(chunk) {
    attr(chunk, "player_props")
  })
  props_list <- Filter(Negate(is.null), props_list)
  
  if (length(props_list) == 0) {
    message("No player props found in sim results. Was sim_mode = 'player'?")
    return(tibble())
  }
  
  props_raw <- bind_rows(props_list)
  
  # Aggregate across quarters first (per sim, per player, per game)
  props_by_sim <- props_raw %>%
    group_by(game_id, sim_index, ESPN_PLAYER_ID, team_id) %>%
    summarise(
      pts  = sum(sim_pts,  na.rm = TRUE),
      fga  = sum(sim_fga,  na.rm = TRUE),
      fgm  = sum(sim_fgm,  na.rm = TRUE),
      fga3 = sum(sim_fga3, na.rm = TRUE),
      fgm3 = sum(sim_fgm3, na.rm = TRUE),
      fta  = sum(sim_fta,  na.rm = TRUE),
      ftm  = sum(sim_ftm,  na.rm = TRUE),
      .groups = "drop"
    )
  
  # Summarise across sims → prop distributions
  mc_player_props <- props_by_sim %>%
    group_by(game_id, ESPN_PLAYER_ID, team_id) %>%
    summarise(
      n_sims    = n(),
      
      pts_mean  = mean(pts,  na.rm = TRUE),
      pts_median= median(pts, na.rm = TRUE),
      pts_sd    = sd(pts,    na.rm = TRUE),
      pts_p10   = quantile(pts, 0.10, na.rm = TRUE),
      pts_p25   = quantile(pts, 0.25, na.rm = TRUE),
      pts_p75   = quantile(pts, 0.75, na.rm = TRUE),
      pts_p90   = quantile(pts, 0.90, na.rm = TRUE),
      
      fga_mean  = mean(fga,  na.rm = TRUE),
      fgm_mean  = mean(fgm,  na.rm = TRUE),
      fg_pct    = mean(ifelse(fga > 0, fgm / fga, NA_real_), na.rm = TRUE),
      
      fg3a_mean = mean(fga3, na.rm = TRUE),
      fg3m_mean = mean(fgm3, na.rm = TRUE),
      fg3_pct   = mean(ifelse(fga3 > 0, fgm3 / fga3, NA_real_), na.rm = TRUE),
      
      fta_mean  = mean(fta,  na.rm = TRUE),
      ftm_mean  = mean(ftm,  na.rm = TRUE),
      ft_pct    = mean(ifelse(fta > 0, ftm / fta, NA_real_), na.rm = TRUE),
      
      .groups = "drop"
    ) %>%
    arrange(game_id, desc(pts_mean))
  
  mc_player_props
}


cat("\n📊 Calculating summary statistics...\n")

mc_game_totals_backtest <- aggregate_quarter_results(mc_sims_backtest_q)

mc_results_summary <- calculate_summary_stats(
  mc_game_totals_backtest, 
  nba_games_canon_season
)

mc_results_quarters <- calculate_quarter_breakdown(mc_sims_backtest_q)

# --------------------------------------------------
# STEP 4: Calculate calibration
# --------------------------------------------------
cat("📈 Calculating calibration analysis...\n")

calibration_results <- calculate_calibration(
  mc_results_summary, 
  nba_games_canon_season
)

mc_calibration <- calibration_results$calibration_summary
mc_backtest_validation <- calibration_results$calibration_data

# --------------------------------------------------
# STEP 5: Reset parallel plan
# --------------------------------------------------
future::plan(future::sequential)
cat("✓ Parallel processing shut down\n\n")

# --------------------------------------------------
# STEP 6: Display results
# --------------------------------------------------
cat("=", rep("=", 80), "\n", sep = "")
cat("✅ BACKTEST COMPLETE!\n")
cat("=", rep("=", 80), "\n\n", sep = "")

cat("RESULTS SUMMARY:\n")
cat("  Total games simulated:", nrow(mc_results_summary), "\n")
cat("  Total simulation rows:", nrow(mc_sims_backtest_q), "\n")
cat("  Simulations per game:", 1000, "\n\n")

cat("CALIBRATION ANALYSIS:\n")
print(mc_calibration)
cat("\n")

cat("MODEL ACCURACY:\n")
cat("  Overall win probability accuracy:", 
    round(mean(mc_backtest_validation$correct_pick, na.rm = TRUE) * 100, 1), "%\n")
cat("  Mean absolute error (home pts):", 
    round(mean(abs(mc_backtest_validation$home_pts_error), na.rm = TRUE), 2), "points\n")
cat("  Mean absolute error (away pts):", 
    round(mean(abs(mc_backtest_validation$away_pts_error), na.rm = TRUE), 2), "points\n\n")

cat("OUTPUT DATAFRAMES CREATED:\n")
cat("  mc_results_summary       - Game-level predictions\n")
cat("  mc_results_quarters      - Quarter-by-quarter breakdown\n")
cat("  mc_sims_backtest_q       - Full simulation data\n")
cat("  mc_calibration           - Calibration analysis\n")
cat("  mc_backtest_validation   - Predicted vs actual comparison\n\n")

cat("View results with: View(mc_results_summary)\n\n")


# 1. How many games simulated?
nrow(mc_results_summary)

# 2. Overall accuracy
mean(mc_backtest_validation$correct_pick, na.rm = TRUE) * 100

# 3. Point prediction error
mean(abs(mc_backtest_validation$home_pts_error), na.rm = TRUE)

# 4. Calibration table
print(mc_calibration)

# 5. Sample predictions vs actuals
mc_backtest_validation %>%
  select(home_team_id, away_team_id, 
         home_win_prob, predicted_winner, actual_winner, correct_pick,
         home_pts_mean, actual_home_pts, home_pts_error) %>%
  head(20)

# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲 END: MC Simulation Runner
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲



# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲 START: MC Simulation Reviewer
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲

# Key accuracy metrics
cat("Mean home pts predicted:", mean(mc_results_summary$home_pts_mean, na.rm = TRUE), "\n")
cat("Mean away pts predicted:", mean(mc_results_summary$away_pts_mean, na.rm = TRUE), "\n")

cat("Overall accuracy:", round(mean(mc_backtest_validation$correct_pick, na.rm = TRUE) * 100, 1), "%\n")
cat("MAE home:", round(mean(abs(mc_backtest_validation$home_pts_error), na.rm = TRUE), 2), "\n")
cat("MAE away:", round(mean(abs(mc_backtest_validation$away_pts_error), na.rm = TRUE), 2), "\n")


mean(mc_results_summary$home_pts_mean, na.rm = TRUE)


player_minutes_params_backtest %>%
  filter(is_active == 1) %>%
  group_by(date_key, ESPN_TEAM_ID) %>%
  summarise(total_Q1 = sum(mins_mean_Q1, na.rm = TRUE), .groups = "drop") %>%
  summarise(mean_Q1 = mean(total_Q1), max_Q1 = max(total_Q1))


MC_CONFIG$tov_reduction_pct
MC_CONFIG$foul_bonus_leverage
MC_CONFIG$oreb_bonus_pct


# Build error analysis dataframe
error_analysis <- mc_backtest_validation %>%
  mutate(
    home_pts_error = home_pts_mean - actual_home_pts,
    abs_error = abs(home_pts_error),
    game_number = row_number(),  # proxy for time in season
    is_high_conf = home_win_prob > 0.70 | home_win_prob < 0.30
  )

# Linear regression on what predicts error magnitude
error_model <- lm(abs_error ~ home_win_prob + game_number + is_high_conf, 
                  data = error_analysis)
summary(error_model)

# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲
# 🎲🎲🎲 END: MC Simulation Reviewer
# 🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲🎲



# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
#
#     ooooooooo.   oooo                .       .        ooo        ooooo                 .o8            oooo         .o ooooo        ooooooooo.   o.   
#     `888   `Y88. `888              .o8     .o8        `88.       .888'                "888            `888        .8' `888'        `888   `Y88. `8.  
#      888   .d88'  888   .oooo.   .o888oo .o888oo       888b     d'888   .ooooo.   .oooo888   .ooooo.   888       .8'   888          888   .d88'  `8. 
#      888ooo88P'   888  `P  )88b    888     888         8 Y88. .P  888  d88' `88b d88' `888  d88' `88b  888       88    888          888ooo88P'    88 
#      888          888   .oP"888    888     888         8  `888'   888  888   888 888   888  888ooo888  888       88    888          888`88b.      88 
#      888          888  d8(  888    888 .   888 .       8    Y     888  888   888 888   888  888    .o  888       `8.   888       o  888  `88b.   .8' 
#     o888o        o888o `Y888""8o   "888"   "888"      o8o        o888o `Y8bod8P' `Y8bod88P" `Y8bod8P' o888o       `8. o888ooooood8 o888o  o888o .8'  
#                                                                                                                    `"                           "'   
#                                                                                                                                                 
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬                                                                                                                                                 
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

# ==========================================================================================================
# PLATT SCALING CALIBRATION
# ----------------------------------------------------------------------------------------------------------
# Calibrates raw MC win probabilities using logistic regression (Platt Scaling).
# Takes raw simulation win probabilities and corrects for systematic overconfidence
# by learning from historical outcomes. Rest features included for future improvement
# as sample size grows.
# ==========================================================================================================

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 START: Platt Scaling Calibration
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬

# --------------------------------------------------
# STEP 2: Join rest features to validation data
# --------------------------------------------------
mc_backtest_validation <- mc_backtest_validation %>%
  left_join(game_rest_features, by = "game_id")

# --------------------------------------------------
# STEP 3: Fit Platt scaling model with rest features
# --------------------------------------------------
cat("📈 Fitting Platt scaling model...\n")

platt_model_full <- glm(
  as.integer(actual_winner == "home") ~
    home_win_prob +
    rest_diff +
    home_b2b +
    away_b2b,
  data = mc_backtest_validation,
  family = binomial(link = "logit")
)

summary(platt_model_full)

# --------------------------------------------------
# STEP 4: Apply calibrated probabilities
# --------------------------------------------------
mc_backtest_validation <- mc_backtest_validation %>%
  mutate(
    home_win_prob_calibrated = predict(platt_model_full, type = "response")
  )

# --------------------------------------------------
# STEP 5: Compare accuracy before and after
# --------------------------------------------------
accuracy_comparison <- mc_backtest_validation %>%
  mutate(
    raw_correct = (home_win_prob > 0.5) == (actual_winner == "home"),
    cal_correct = (home_win_prob_calibrated > 0.5) == (actual_winner == "home")
  ) %>%
  summarise(
    raw_accuracy = mean(raw_correct, na.rm = TRUE),
    calibrated_accuracy = mean(cal_correct, na.rm = TRUE)
  )

cat("Raw accuracy:        ", round(accuracy_comparison$raw_accuracy * 100, 1), "%\n")
cat("Calibrated accuracy: ", round(accuracy_comparison$calibrated_accuracy * 100, 1), "%\n\n")

# --------------------------------------------------
# STEP 6: Check calibration buckets
# --------------------------------------------------
calib_data <- mc_backtest_validation %>%
  mutate(home_won = as.integer(actual_winner == "home"))

calib_data %>%
  mutate(bucket = cut(home_win_prob_calibrated, breaks = seq(0, 1, 0.1))) %>%
  group_by(bucket) %>%
  summarise(
    predicted = mean(home_win_prob_calibrated),
    actual = mean(home_won),
    n = n()
  ) %>%
  print()

# --------------------------------------------------
# STEP 7: Save model and create apply function
# --------------------------------------------------
saveRDS(platt_model_full, file.path(base_path, "platt_calibration_model.rds"))

apply_calibration <- function(raw_win_prob, rest_diff = 0,
                              home_b2b = 0, away_b2b = 0,
                              model = platt_model_full) {
  predict(model,
          newdata = data.frame(
            home_win_prob = raw_win_prob,
            rest_diff = rest_diff,
            home_b2b = home_b2b,
            away_b2b = away_b2b
          ),
          type = "response")
}

# Test calibration function
cat("Calibrated win prob for 0.80 raw:", round(apply_calibration(0.80), 3), "\n")
cat("Calibrated win prob for 0.90 raw:", round(apply_calibration(0.90), 3), "\n")
cat("Calibrated win prob for 0.50 raw:", round(apply_calibration(0.50), 3), "\n")

cat("✅ Platt calibration model saved\n\n")

# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬
# 🧬🧬🧬 END: Platt Scaling Calibration
# 🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬🧬